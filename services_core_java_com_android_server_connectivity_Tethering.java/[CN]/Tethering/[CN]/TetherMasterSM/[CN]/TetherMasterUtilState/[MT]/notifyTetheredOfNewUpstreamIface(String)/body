{
  if (DBG)   Log.d(TAG,"notifying tethered with iface =" + ifaceName);
  mCurrentUpstreamIface=ifaceName;
  for (  TetherInterfaceSM sm : mNotifyList) {
    sm.sendMessage(TetherInterfaceSM.CMD_TETHER_CONNECTION_CHANGED,ifaceName);
  }
}
