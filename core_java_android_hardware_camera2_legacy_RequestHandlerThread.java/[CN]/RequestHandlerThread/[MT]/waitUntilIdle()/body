{
  Looper looper=waitAndGetHandler().getLooper();
  if (looper.isIdling()) {
    return;
  }
  mIdle.close();
  looper.getQueue().addIdleHandler(mIdleHandler);
  if (looper.isIdling()) {
    return;
  }
  mIdle.block();
}
