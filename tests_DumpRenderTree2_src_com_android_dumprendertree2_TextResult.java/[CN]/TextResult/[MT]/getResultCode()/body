{
  if (mResultCode == null) {
    if (mExpectedResult == null) {
      mResultCode=AbstractResult.ResultCode.NO_EXPECTED_RESULT;
    }
 else {
      mResultCode=resultsMatch() ? AbstractResult.ResultCode.RESULTS_MATCH : AbstractResult.ResultCode.RESULTS_DIFFER;
    }
  }
  return mResultCode;
}
