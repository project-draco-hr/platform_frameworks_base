{
  AuthorityInfo authority=mServices.get(service).get(userId);
  if (authority == null) {
    if (tag != null) {
      if (DEBUG) {
        Log.v(TAG,tag + " No authority info found for " + service+ " for user "+ userId);
      }
    }
    return null;
  }
  return authority;
}
