{
  target=info;
  ident=id;
  enabled=info.target_provider ? SYNC_ENABLED_DEFAULT : true;
  if (info.target_service) {
    this.syncable=1;
  }
  periodicSyncs=new ArrayList<PeriodicSync>();
  defaultInitialisation();
}
