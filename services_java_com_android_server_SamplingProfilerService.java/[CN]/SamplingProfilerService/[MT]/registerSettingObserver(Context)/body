{
  ContentResolver contentResolver=context.getContentResolver();
  contentResolver.registerContentObserver(Settings.Secure.getUriFor(Settings.Secure.SAMPLING_PROFILER_HZ),false,new SamplingProfilerSettingsObserver(contentResolver));
}
