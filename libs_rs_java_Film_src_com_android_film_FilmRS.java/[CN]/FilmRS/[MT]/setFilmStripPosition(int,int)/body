{
  if (x < 50) {
    x=50;
  }
  if (x > 270) {
    x=270;
  }
  float anim=((float)x - 50) / 270.f;
  mPos.translate=2f * anim + 0.5f;
  mPos.rotate=(anim * 40);
  mPos.focus=((float)y) / 16.f - 10.f;
  mPos.triangleOffsetCount=mFSM.mTriangleOffsetsCount;
  mAllocPos.data(mPos);
}
