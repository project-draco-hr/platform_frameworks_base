{
  if (x < 50) {
    x=50;
  }
  if (x > 270) {
    x=270;
  }
  float anim=((float)x - 50) / 270.f;
  mBufferPos[POS_TRANSLATE]=2f * anim + 0.5f;
  mBufferPos[POS_ROTATE]=(anim * 40);
  mBufferPos[POS_FOCUS]=((float)y) / 16.f - 8;
  mAllocPos.data(mBufferPos);
}
