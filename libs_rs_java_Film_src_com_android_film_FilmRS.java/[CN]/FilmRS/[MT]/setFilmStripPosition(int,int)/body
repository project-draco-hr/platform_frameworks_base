{
  if (x < 50) {
    x=50;
  }
  if (x > 270) {
    x=270;
  }
  float anim=((float)x - 50) / 270.f;
  mBufferPos[0]=2f * anim + 0.5f;
  mBufferPos[1]=(anim * 40);
  mBufferPos[2]=((float)y) / 16.f - 8;
  mAllocPos.data(mBufferPos);
}
