{
  if (x < 0) {
    x=0;
  }
  if (x > 50) {
    x=50;
  }
  float anim=((float)x) / 50.f;
  mBufferPos[0]=-2f * anim - .2f;
  mBufferPos[1]=-90 + (anim * 40);
  mBufferPos[2]=((float)y) / 16.f - 8;
  mAllocPos.data(mBufferPos);
}
