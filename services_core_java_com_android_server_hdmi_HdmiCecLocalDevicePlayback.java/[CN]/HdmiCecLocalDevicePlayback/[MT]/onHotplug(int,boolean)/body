{
  assertRunOnServiceThread();
  mCecMessageCache.flushAll();
  if (connected && mService.isPowerStandbyOrTransient()) {
    mService.wakeUp();
  }
  if (!connected) {
    getWakeLock().release();
  }
}
