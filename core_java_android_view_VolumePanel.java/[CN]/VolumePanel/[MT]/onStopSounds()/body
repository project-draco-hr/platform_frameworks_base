{
synchronized (this) {
    for (int i=AudioManager.NUM_STREAMS - 1; i >= 0; i--) {
      ToneGenerator toneGen=mToneGenerators[i];
      if (toneGen != null) {
        toneGen.stopTone();
      }
    }
  }
}
