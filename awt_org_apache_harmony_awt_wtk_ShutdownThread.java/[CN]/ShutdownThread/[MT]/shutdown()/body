{
synchronized (this) {
    shouldStop=true;
    notifyAll();
    try {
      wait();
    }
 catch (    InterruptedException e) {
      throw new RuntimeException(Messages.getString("awt.27"));
    }
  }
}
