{
  TextLine tl;
synchronized (cached) {
    for (int i=cached.length; --i >= 0; ) {
      if (cached[i] != null) {
        tl=cached[i];
        cached[i]=null;
        return tl;
      }
    }
  }
  tl=new TextLine();
  return tl;
}
