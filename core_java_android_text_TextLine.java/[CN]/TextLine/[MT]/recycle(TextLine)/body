{
  tl.mText=null;
  tl.mPaint=null;
  tl.mDirections=null;
synchronized (cached) {
    for (int i=0; i < cached.length; ++i) {
      if (cached[i] == null) {
        cached[i]=tl;
        break;
      }
    }
  }
  return null;
}
