{
  if (runIsRtl) {
    swapRun(start,limit);
  }
  if (mCharsValid) {
    c.drawText(mChars,start,limit - start,x,y,wp);
  }
 else {
    c.drawText(mText,mStart + start,mStart + limit,x,y,wp);
  }
  if (runIsRtl) {
    swapRun(start,limit);
  }
}
