{
  try {
synchronized (mManagerCallback) {
      if (mService != null) {
        return mService.getState();
      }
    }
  }
 catch (  RemoteException e) {
    Log.e(TAG,"",e);
  }
  return STATE_OFF;
}
