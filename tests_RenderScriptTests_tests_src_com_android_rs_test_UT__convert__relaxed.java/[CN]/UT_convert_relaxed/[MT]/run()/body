{
  RenderScript pRS=RenderScript.create(mCtx);
  ScriptC_convert_relaxed s=new ScriptC_convert_relaxed(pRS,mRes,R.raw.convert_relaxed);
  pRS.setMessageHandler(mRsMessage);
  s.invoke_convert_test();
  pRS.finish();
  waitForMessage();
  pRS.destroy();
}
