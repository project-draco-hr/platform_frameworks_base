{
  if (mDatabase == null) {
    return false;
  }
  Cursor cursor=mDatabase.query(mTableNames[tableId],ID_PROJECTION,null,null,null,null,null);
  boolean ret=cursor.moveToFirst() == true;
  cursor.close();
  return ret;
}
