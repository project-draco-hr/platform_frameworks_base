{
  mW=displayWidth;
  mH=displayHeight;
  mCurLeft=0;
  mCurTop=0;
  mCurRight=displayWidth;
  mCurBottom=displayHeight;
  if (mStatusBar != null) {
    mStatusBar.computeFrameLw(0,0,displayWidth,displayHeight,0,0,displayWidth,displayHeight);
    mCurTop=mStatusBar.getFrameLw().bottom;
  }
}
