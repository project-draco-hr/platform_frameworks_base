{
  RecentsConfiguration config=RecentsConfiguration.getInstance();
  mBarView.setAlpha(0f);
  mBarView.animate().alpha(1f).setStartDelay(300).setInterpolator(config.defaultBezierInterpolator).setDuration(config.taskBarEnterAnimDuration).withLayer().start();
}
