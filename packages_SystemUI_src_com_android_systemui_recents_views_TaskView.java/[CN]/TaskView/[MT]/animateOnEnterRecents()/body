{
  RecentsConfiguration config=RecentsConfiguration.getInstance();
  mBarView.setAlpha(0f);
  mBarView.animate().alpha(1f).setStartDelay(235).setInterpolator(BakedBezierInterpolator.INSTANCE).setDuration(config.taskBarEnterAnimDuration).withLayer().start();
}
