{
  Console.log(Constants.DebugFlags.App.EnableBackgroundTaskLoading,"[TaskView|onTaskDataChanged]",task);
  if (mTask == task) {
    mThumbnailView.rebindToTask(mTask,true);
    mIconView.rebindToTask(mTask,true);
  }
}
