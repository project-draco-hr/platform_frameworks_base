{
  mTask=t;
  if (t.icon != null) {
    setImageDrawable(t.icon);
    if (animate) {
      setAlpha(0f);
      animate().alpha(1f).setDuration(Constants.Values.TaskView.Animation.TaskDataUpdatedFadeDuration).start();
    }
  }
}
