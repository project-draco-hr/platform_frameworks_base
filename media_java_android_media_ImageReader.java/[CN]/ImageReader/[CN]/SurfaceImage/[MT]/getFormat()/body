{
  throwISEIfImageIsInvalid();
  int readerFormat=ImageReader.this.getImageFormat();
  mFormat=(readerFormat == ImageFormat.PRIVATE) ? readerFormat : nativeGetFormat(readerFormat);
  return mFormat;
}
