{
  try {
    if (mNetworkInfo != null) {
      mConnService.setProvisioningNotificationVisible(false,mNetworkInfo.getType(),null,null);
    }
  }
 catch (  RemoteException e) {
    log("setNotificationOff: " + e);
  }
}
