{
  int uid=UserHandle.myUserId();
synchronized (mLocationSettingsLock) {
    return isLocationProviderEnabledForUser(cr,LocationManager.NETWORK_PROVIDER,uid) || isLocationProviderEnabledForUser(cr,LocationManager.GPS_PROVIDER,uid);
  }
}
