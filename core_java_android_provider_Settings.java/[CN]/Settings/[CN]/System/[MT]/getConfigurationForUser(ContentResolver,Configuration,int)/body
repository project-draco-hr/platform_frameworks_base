{
  outConfig.fontScale=Settings.System.getFloatForUser(cr,FONT_SCALE,outConfig.fontScale,userHandle);
  if (outConfig.fontScale < 0) {
    outConfig.fontScale=1;
  }
  outConfig.setLocales(LocaleList.forLanguageTags(Settings.System.getStringForUser(cr,SYSTEM_LOCALES,userHandle)));
}
