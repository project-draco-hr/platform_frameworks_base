{
  if (mUuid.equals(ALL_0_UUID)) {
    return 0;
  }
 else   if (mUuid.equals(ALL_F_UUID)) {
    return -1;
  }
 else {
    int id=Math.abs(mUuid.hashCode()) + 1;
    if (id <= 0) {
      id=1;
    }
    return id;
  }
}
