{
  if (mIface.equals(iface) && mLinkUp != up) {
    Log.d(TAG,"Interface " + iface + " link "+ (up ? "up" : "down"));
    mLinkUp=up;
    if (up) {
      mTracker.reconnect();
    }
 else {
      mTracker.disconnect();
    }
  }
}
