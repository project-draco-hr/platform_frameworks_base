{
  Rect oldClipRect=tr.getClipRect();
  tr.setClipRect(oldClipRect.left,mFromRect.top + (int)((mToRect.top - mFromRect.top) * it),oldClipRect.right,mFromRect.bottom + (int)((mToRect.bottom - mFromRect.bottom) * it));
}
