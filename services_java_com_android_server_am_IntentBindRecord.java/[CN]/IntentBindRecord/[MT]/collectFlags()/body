{
  int flags=0;
  if (apps.size() > 0) {
    for (    AppBindRecord app : apps.values()) {
      if (app.connections.size() > 0) {
        for (        ConnectionRecord conn : app.connections) {
          flags|=conn.flags;
        }
      }
    }
  }
  return flags;
}
