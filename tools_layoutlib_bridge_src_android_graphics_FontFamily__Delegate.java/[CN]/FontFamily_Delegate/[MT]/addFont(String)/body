{
  Font font=loadFont(path);
  if (font == null) {
    return false;
  }
  FontInfo fontInfo=new FontInfo();
  fontInfo.mFont=font;
  addFontMetadata(fontInfo,path);
  mFonts.add(fontInfo);
  return true;
}
