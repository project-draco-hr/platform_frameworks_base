{
  View actionView=item.getActionView();
  actionView=actionView != null && !item.hasCollapsibleActionView() ? actionView : super.getItemView(item,convertView,parent);
  actionView.setVisibility(item.isActionViewExpanded() ? View.GONE : View.VISIBLE);
  return actionView;
}
