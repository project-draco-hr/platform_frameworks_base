{
  Looper.prepare();
  hh=new HandlerHelper(this);
  if (setupRoutine != null) {
    setupRoutine.run();
  }
synchronized (this) {
    looper=Looper.myLooper();
    this.notify();
  }
  Looper.loop();
}
