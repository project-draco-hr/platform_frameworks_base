{
  TestHandler ret;
  ret=new TestHandler();
  ret.setupRoutine=doSetup;
synchronized (ret) {
    new Thread(ret,name).start();
    while (ret.looper == null) {
      try {
        ret.wait();
      }
 catch (      InterruptedException ex) {
      }
    }
  }
  return ret;
}
