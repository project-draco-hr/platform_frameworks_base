{
  if (mActiveActions != null && mStaleLayoutId == mActiveActions.getLayoutId()) {
    if (LOGD)     Log.d(TAG,"after animation, layoutId matched so we're recycling old view");
    mActiveActions.reapply(mLocalContext,mStaleView);
    mStalePrepared=true;
  }
}
