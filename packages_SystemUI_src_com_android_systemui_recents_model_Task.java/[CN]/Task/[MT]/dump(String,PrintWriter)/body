{
  writer.print(prefix);
  writer.print(key);
  if (affiliationTaskId != key.id) {
    writer.print(" ");
    writer.print("affTaskId=" + affiliationTaskId);
  }
  writer.print(" ");
  writer.print(title);
  writer.println();
}
