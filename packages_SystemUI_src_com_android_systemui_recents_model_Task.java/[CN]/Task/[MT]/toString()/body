{
  String groupAffiliation="no group";
  if (group != null) {
    groupAffiliation=group.affiliation;
  }
  return "Task (" + groupAffiliation + "): "+ key.baseIntent.getComponent().getPackageName()+ " ["+ super.toString()+ "]";
}
