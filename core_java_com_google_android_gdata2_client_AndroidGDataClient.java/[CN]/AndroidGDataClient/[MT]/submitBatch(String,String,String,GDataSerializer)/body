{
  HttpEntity entity=createEntityForEntry(batch,GDataSerializer.FORMAT_BATCH);
  InputStream in=createAndExecuteMethod(new PostRequestCreator("POST",entity),batchUrl,authToken,null,protocolVersion);
  if (in != null) {
    return in;
  }
  throw new IOException("Unable to process batch request.");
}
