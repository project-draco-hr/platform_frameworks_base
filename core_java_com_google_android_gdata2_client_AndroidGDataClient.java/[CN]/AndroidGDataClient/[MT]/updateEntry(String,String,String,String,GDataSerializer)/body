{
  HttpEntity entity=createEntityForEntry(entry,GDataSerializer.FORMAT_UPDATE);
  InputStream in=createAndExecuteMethod(new PostRequestCreator("PUT",entity),editUri,authToken,eTag,protocolVersion);
  if (in != null) {
    return in;
  }
  throw new IOException("Unable to update entry.");
}
