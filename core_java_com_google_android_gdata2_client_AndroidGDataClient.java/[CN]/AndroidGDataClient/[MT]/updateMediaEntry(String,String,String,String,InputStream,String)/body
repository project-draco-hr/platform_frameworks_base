{
  InputStream in=createAndExecuteMethod(new MediaPutRequestCreator(mediaEntryInputStream,contentType),editUri,authToken,eTag,protocolVersion);
  if (in != null) {
    return in;
  }
  throw new IOException("Unable to write media entry.");
}
