{
  HttpPost post=new HttpPost(uri);
  post.addHeader(X_HTTP_METHOD_OVERRIDE,"PUT");
  InputStreamEntity entity=new InputStreamEntity(mMediaInputStream,-1);
  entity.setContentType(mContentType);
  post.setEntity(entity);
  return post;
}
