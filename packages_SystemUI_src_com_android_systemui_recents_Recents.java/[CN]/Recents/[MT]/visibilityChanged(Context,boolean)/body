{
  SystemUIApplication app=(SystemUIApplication)getInstanceAndStartIfNeeded(context.getApplicationContext()).mContext;
  PhoneStatusBar statusBar=app.getComponent(PhoneStatusBar.class);
  if (statusBar != null) {
    statusBar.updateRecentsVisibility(visible);
  }
}
