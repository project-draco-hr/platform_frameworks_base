{
  if (mFilename != null) {
    new File(mFilename).delete();
    mFilename=null;
    mBitmap.recycle();
    mBitmap=null;
  }
  if (mBitmapFileName != null) {
    new File(mBitmapFileName).delete();
    mBitmapFileName=null;
  }
}
