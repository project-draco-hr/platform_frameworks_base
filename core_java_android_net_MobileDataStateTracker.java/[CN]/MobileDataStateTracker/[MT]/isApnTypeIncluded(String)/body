{
  if (typeList == null)   return false;
  String[] list=typeList.split(",");
  for (int i=0; i < list.length; i++) {
    if (TextUtils.equals(list[i],mApnType) || TextUtils.equals(list[i],Phone.APN_TYPE_ALL)) {
      return true;
    }
  }
  return false;
}
