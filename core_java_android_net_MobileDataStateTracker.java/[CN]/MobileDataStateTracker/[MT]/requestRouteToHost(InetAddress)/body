{
  if (DBG) {
    Log.d(TAG,"Requested host route to " + hostAddress.getHostAddress() + " for "+ mApnType+ "("+ mInterfaceName+ ")");
  }
  if (mInterfaceName != null) {
    return NetworkUtils.addHostRoute(mInterfaceName,hostAddress,null);
  }
 else {
    return false;
  }
}
