{
  if (!mBouncerOn && mCurrentSession != null) {
    if (DEBUG) {
      Log.v(TAG,"onTouchEvent(ev.action=" + MotionEvent.actionToString(ev.getAction()) + ")");
    }
    mCurrentSession.addMotionEvent(ev);
    mCurrentSession.setTouchArea(width,height);
    enforceTimeout();
  }
}
