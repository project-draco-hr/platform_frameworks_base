{
  if (enableSystemAudio) {
    return buildCommand(src,avr,HdmiCec.MESSAGE_SYSTEM_AUDIO_MODE_REQUEST,physicalAddressToParam(avrPhysicalAddress));
  }
 else {
    return buildCommand(src,avr,HdmiCec.MESSAGE_SYSTEM_AUDIO_MODE_REQUEST);
  }
}
