{
  if (req.getRoute().getId().equals(TEST_ROUTE_ID)) {
    mStub=new OneStub();
    ConnectionInfo connection=new ConnectionInfo.Builder(req.getRoute()).setProtocolStub(MediaPlayerProtocol.class,mStub).build();
    callback.onConnected(connection);
    return true;
  }
  return false;
}
