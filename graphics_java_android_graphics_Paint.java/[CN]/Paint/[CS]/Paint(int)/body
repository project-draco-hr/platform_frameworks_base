{
  mNativePaint=native_init();
  setFlags(flags | DEFAULT_PAINT_FLAGS);
  setHinting(DisplayMetrics.DENSITY_DEVICE >= DisplayMetrics.DENSITY_TV ? HINTING_OFF : HINTING_ON);
  mCompatScaling=mInvCompatScaling=1;
}
