{
  if (mNativePaint == 0)   throw new NullPointerException("Already finalized!");
  long filterNative=0;
  if (filter != null)   filterNative=filter.native_instance;
  nSetColorFilter(mNativePaint,filterNative);
  mColorFilter=filter;
  return filter;
}
