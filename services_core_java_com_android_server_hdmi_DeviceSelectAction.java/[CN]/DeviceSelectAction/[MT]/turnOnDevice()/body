{
  sendUserControlPressedAndReleased(mTarget.getLogicalAddress(),HdmiConstants.UI_COMMAND_POWER);
  sendUserControlPressedAndReleased(mTarget.getLogicalAddress(),HdmiConstants.UI_COMMAND_POWER_ON_FUNCTION);
  mState=STATE_WAIT_FOR_DEVICE_POWER_ON;
  addTimer(mState,TIMEOUT_POWER_ON_MS);
}
