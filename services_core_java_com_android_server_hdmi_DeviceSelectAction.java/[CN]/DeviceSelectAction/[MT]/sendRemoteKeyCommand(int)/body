{
  sendCommand(HdmiCecMessageBuilder.buildUserControlPressed(mSourceAddress,mTarget.getLogicalAddress(),keyCode));
  sendCommand(HdmiCecMessageBuilder.buildUserControlReleased(mSourceAddress,mTarget.getLogicalAddress()));
}
