{
  sendCommand(HdmiCecMessageBuilder.buildUserControlPressed(getSourceAddress(),mTarget.getLogicalAddress(),keyCode));
  sendCommand(HdmiCecMessageBuilder.buildUserControlReleased(getSourceAddress(),mTarget.getLogicalAddress()));
}
