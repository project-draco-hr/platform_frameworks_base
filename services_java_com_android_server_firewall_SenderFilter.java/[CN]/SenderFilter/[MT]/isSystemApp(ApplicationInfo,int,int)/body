{
  if (callerUid == Process.SYSTEM_UID || callerPid == Process.myPid()) {
    return true;
  }
  if (callerApp == null) {
    return false;
  }
  return (callerApp.flags & ApplicationInfo.FLAG_SYSTEM) != 0;
}
