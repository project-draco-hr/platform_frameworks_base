{
  if (mBounds != null) {
    mCursorDeltaY=mPointer.y - mBounds.bottom;
  }
  mBounds=new Rect(Math.min(mOrigin.x,mPointer.x),Math.min(mOrigin.y,mPointer.y),Math.max(mOrigin.x,mPointer.x),Math.max(mOrigin.y,mPointer.y));
  mRegionSelectorDrawable.setBounds(mBounds);
}
