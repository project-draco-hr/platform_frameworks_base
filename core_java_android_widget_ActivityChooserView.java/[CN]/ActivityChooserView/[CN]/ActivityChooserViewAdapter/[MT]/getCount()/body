{
  int count=0;
  int activityCount=mDataModel.getActivityCount();
  if (activityCount > 0) {
    activityCount--;
  }
  count=Math.min(activityCount,mMaxActivityCount);
  if (mHeaderView != null) {
    count++;
  }
  if (mFooterView != null) {
    count++;
  }
  return count;
}
