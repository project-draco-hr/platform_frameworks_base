{
  if (mProperties != null) {
    GenerationalClassUtil.writeIntermediateFile(processingEnvironment,mProperties.getPackage(),createIntermediateFileName(mProperties.getPackage()),mProperties);
    generateBRClasses(!buildInfo.isLibrary(),mProperties.getPackage());
  }
}
