{
  if (!isGet) {
    if (!finalBitSet) {
      if (sendEmpty) {
        sendReply(OBEXConstants.OBEX_HTTP_CONTINUE);
        return true;
      }
 else {
        if ((responseSize > 3) || (privateOutput.size() > 0)) {
          sendReply(OBEXConstants.OBEX_HTTP_CONTINUE);
          return true;
        }
 else {
          return false;
        }
      }
    }
 else {
      return false;
    }
  }
 else {
    sendReply(OBEXConstants.OBEX_HTTP_CONTINUE);
    return true;
  }
}
