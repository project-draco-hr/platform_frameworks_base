{
  if (VDBG) {
    Log.v(TAG,"sendMessage(): clientId=" + clientId + ", sessionId="+ sessionId+ ", peerHandle="+ peerHandle+ ", messageId="+ messageId+ ", retryCount="+ retryCount);
  }
  try {
    mService.sendMessage(clientId,sessionId,(Integer)peerHandle,message,messageId,retryCount);
  }
 catch (  RemoteException e) {
    throw e.rethrowFromSystemServer();
  }
}
