{
  if (mClientId == -1) {
    Log.w(TAG,"disconnect(): called without calling connect() first - or called " + "multiple times.");
    return;
  }
  try {
    if (VDBG)     Log.v(TAG,"disconnect()");
    mService.disconnect(mClientId,mBinder);
    mBinder=null;
    mClientId=-1;
  }
 catch (  RemoteException e) {
    throw e.rethrowFromSystemServer();
  }
}
