{
  if (mControlSocketInUse) {
    mSocketResult=result;
    notifyAll();
  }
 else   if (result > 0) {
    throw new VpnConnectingError((result == AUTH_ERROR_CODE) ? VpnManager.VPN_ERROR_AUTH : VpnManager.VPN_ERROR_CONNECTION_FAILED);
  }
}
