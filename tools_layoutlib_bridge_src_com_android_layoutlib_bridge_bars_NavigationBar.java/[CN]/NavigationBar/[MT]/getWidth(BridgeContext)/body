{
  DisplayMetrics metrics=context.getMetrics();
  float sw=metrics.widthPixels > metrics.heightPixels ? metrics.heightPixels : metrics.widthPixels;
  sw/=metrics.density;
  if (sw >= 600) {
    return WIDTH_SW600;
  }
  if (sw >= 360) {
    return WIDTH_SW360;
  }
  return WIDTH_DEFAULT;
}
