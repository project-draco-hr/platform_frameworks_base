{
  if (name == null) {
    name="";
  }
  T obj=mMap.get(name);
  if (obj != null) {
    return obj;
  }
  if (mActiveOverflow != null) {
    MutableInt over=mActiveOverflow.get(name);
    if (over != null) {
      obj=mCurOverflow;
      if (obj != null) {
        over.value--;
        if (over.value <= 0) {
          mActiveOverflow.remove(name);
        }
        return obj;
      }
    }
  }
  Slog.wtf(TAG,"Unable to find object for " + name + " mapsize="+ mMap.size()+ " activeoverflow="+ mActiveOverflow+ " curoverflow="+ mCurOverflow);
  return null;
}
