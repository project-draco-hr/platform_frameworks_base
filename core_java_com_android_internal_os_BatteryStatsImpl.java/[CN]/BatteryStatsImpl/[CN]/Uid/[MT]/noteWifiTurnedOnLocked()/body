{
  if (!mWifiTurnedOn) {
    mWifiTurnedOn=true;
    if (mWifiTurnedOnTimer == null) {
      mWifiTurnedOnTimer=new StopwatchTimer(Uid.this,WIFI_TURNED_ON,null,mUnpluggables);
    }
    mWifiTurnedOnTimer.startRunningLocked(BatteryStatsImpl.this);
  }
}
