{
  if (mFlashlightTurnedOnTimer == null) {
    mFlashlightTurnedOnTimer=new StopwatchTimer(Uid.this,FLASHLIGHT_TURNED_ON,mFlashlightTurnedOnTimers,mOnBatteryTimeBase);
  }
  return mFlashlightTurnedOnTimer;
}
