{
switch (type) {
case WAKE_TYPE_FULL:
    return wakeTimeFull;
case WAKE_TYPE_PARTIAL:
  return wakeTimePartial;
case WAKE_TYPE_WINDOW:
return wakeTimeWindow;
default :
throw new IllegalArgumentException("type = " + type);
}
}
