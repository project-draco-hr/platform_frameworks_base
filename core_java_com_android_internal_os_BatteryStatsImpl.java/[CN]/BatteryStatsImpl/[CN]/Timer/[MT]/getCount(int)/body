{
  int val;
  if (which == STATS_LAST) {
    val=mLastCount;
  }
 else {
    val=mCount;
    if ((which == STATS_CURRENT) || (which == STATS_UNPLUGGED)) {
      val-=mLoadedCount;
    }
  }
  return val;
}
