{
  int val;
  if (which == STATS_LAST) {
    val=mLastCount;
  }
 else {
    val=computeCurrentCountLocked();
    if (which == STATS_UNPLUGGED) {
      val-=mUnpluggedCount;
    }
 else     if (which != STATS_TOTAL) {
      val-=mLoadedCount;
    }
  }
  return val;
}
