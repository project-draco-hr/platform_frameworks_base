{
  if (mPhoneSimStateRaw == TelephonyManager.SIM_STATE_ABSENT) {
    if (state == ServiceState.STATE_OUT_OF_SERVICE && signalBin > SIGNAL_STRENGTH_NONE_OR_UNKNOWN) {
      state=ServiceState.STATE_IN_SERVICE;
    }
  }
  return state;
}
