{
  if (obj instanceof ModelField) {
    ModelField that=(ModelField)obj;
    return mDeclaredClass.equals(that.mDeclaredClass) && ModelAnalyzer.instance.getTypeUtils().isSameType(mField.asType(),that.mField.asType());
  }
 else {
    return false;
  }
}
