{
  UserInfo userInfo=mUsers.get(id);
  if (userInfo != null) {
    mUsers.remove(id);
    File userFile=new File(mUsersDir,id + ".xml");
    userFile.delete();
    writeUserListLocked();
    updateUserIdsLocked();
    return true;
  }
  return false;
}
