{
  final int radius=mState.mMaxRadius;
  final Rect bounds=getBounds();
  if (radius != RADIUS_AUTO && radius <= bounds.width() / 2 && radius <= bounds.height() / 2) {
    return false;
  }
  return !isBounded();
}
