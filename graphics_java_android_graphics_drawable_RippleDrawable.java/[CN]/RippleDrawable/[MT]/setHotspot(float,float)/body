{
  if (mRipple == null || mBackground == null) {
    mPendingX=x;
    mPendingY=y;
    mHasPending=true;
  }
  if (mRipple != null) {
    mRipple.move(x,y);
  }
  if (mBackground != null) {
    mBackground.move(x,y);
  }
}
