{
  final int restoreToCount=canvas.saveLayer(bounds.left,bounds.top,bounds.right,bounds.bottom,getMaskingPaint(mode));
  canvas.drawColor(Color.TRANSPARENT);
  mMask.draw(canvas);
  return restoreToCount;
}
