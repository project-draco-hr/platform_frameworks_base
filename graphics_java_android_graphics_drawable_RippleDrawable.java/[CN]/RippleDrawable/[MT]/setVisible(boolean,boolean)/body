{
  final boolean changed=super.setVisible(visible,restart);
  if (!visible) {
    clearHotspots();
  }
 else   if (changed) {
    if (mRippleActive) {
      activateRipple();
    }
    if (mBackgroundActive) {
      activateBackground();
    }
  }
  return changed;
}
