{
  if (mIsPooled) {
    throw new IllegalStateException("Instance already recycled.");
  }
  clear();
  if (sPoolSize < MAX_POOL_SIZE) {
    mNext=sPool;
    mIsPooled=true;
    sPool=this;
    sPoolSize++;
  }
}
