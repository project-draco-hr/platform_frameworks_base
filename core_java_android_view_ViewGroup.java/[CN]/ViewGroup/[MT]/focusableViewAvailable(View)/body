{
  if (mParent != null && (getDescendantFocusability() != FOCUS_BLOCK_DESCENDANTS) && !shouldBlockFocusForTouchscreen() && !(isFocused() && getDescendantFocusability() != FOCUS_AFTER_DESCENDANTS)) {
    mParent.focusableViewAvailable(v);
  }
}
