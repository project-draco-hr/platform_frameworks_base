{
  if (mResourceData[index] == null) {
    return null;
  }
  String value=mResourceData[index].getValue();
  if (value != null) {
    return new CharSequence[]{value};
  }
  Bridge.getLog().warning(LayoutLog.TAG_RESOURCES_RESOLVE,String.format(String.format("Unknown value for getTextArray(%d) => %s",index,mResourceData[index].getName())));
  return null;
}
