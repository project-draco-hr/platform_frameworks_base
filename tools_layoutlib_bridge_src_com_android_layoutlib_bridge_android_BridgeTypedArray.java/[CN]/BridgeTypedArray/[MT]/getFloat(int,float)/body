{
  if (mResourceData[index] == null) {
    return defValue;
  }
  String s=mResourceData[index].getValue();
  if (s != null) {
    try {
      return Float.parseFloat(s);
    }
 catch (    NumberFormatException e) {
      Bridge.getLog().warning(BridgeConstants.TAG_RESOURCES_RESOLVE,String.format("Unable to convert \"%s\" into a float in attribute \"%2$s\"",s,mNames[index]));
    }
  }
  return defValue;
}
