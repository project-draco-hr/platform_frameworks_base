{
  int count=0;
  for (  ResourceValue data : mData) {
    if (data != null) {
      count++;
    }
  }
  mIndices=new int[count + 1];
  mIndices[0]=count;
  int index=1;
  for (int i=0; i < mData.length; i++) {
    if (mData[i] != null) {
      mIndices[index++]=i;
    }
  }
}
