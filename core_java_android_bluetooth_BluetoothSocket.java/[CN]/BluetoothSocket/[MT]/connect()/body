{
  mLock.readLock().lock();
  try {
    if (mClosed)     throw new IOException("socket closed");
    if (mSdp != null) {
      mPort=mSdp.doSdp();
    }
    connectNative();
  }
  finally {
    mLock.readLock().unlock();
  }
}
