{
  mLock.readLock().lock();
  try {
    if (mClosed)     return;
    if (mSdp != null) {
      mSdp.cancel();
    }
    abortNative();
  }
  finally {
    mLock.readLock().unlock();
  }
  mLock.writeLock().lock();
  try {
    mClosed=true;
    destroyNative();
  }
  finally {
    mLock.writeLock().unlock();
  }
}
