{
  if (mAndroidKeycode == androidKeycode) {
    if (mParam == NO_PARAM) {
      return new byte[]{(byte)(mCecKeycode & 0xFF)};
    }
 else {
      return new byte[]{(byte)(mCecKeycode & 0xFF),(byte)(mParam & 0xFF)};
    }
  }
 else {
    return null;
  }
}
