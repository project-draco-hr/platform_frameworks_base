{
  super(out);
  this.encode=encode;
  if (encode) {
    estate=new Base64.EncoderState(flags,null);
    dstate=null;
  }
 else {
    estate=null;
    dstate=new Base64.DecoderState(flags,null);
  }
}
