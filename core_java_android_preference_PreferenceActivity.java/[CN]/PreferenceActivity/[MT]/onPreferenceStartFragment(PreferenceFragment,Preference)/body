{
  Fragment f=Fragment.instantiate(this,pref.getFragment(),pref.getExtras());
  getFragmentManager().openTransaction().replace(com.android.internal.R.id.prefs,f).addToBackStack(BACK_STACK_PREFS).commit();
  return true;
}
