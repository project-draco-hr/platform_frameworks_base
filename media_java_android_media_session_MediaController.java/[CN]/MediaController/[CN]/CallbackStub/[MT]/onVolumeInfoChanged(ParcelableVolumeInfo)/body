{
  MediaController controller=mController.get();
  if (controller != null) {
    AudioInfo info=new AudioInfo(pvi.volumeType,pvi.audioAttrs,pvi.controlType,pvi.maxVolume,pvi.currentVolume);
    controller.postMessage(MSG_UPDATE_VOLUME,info,null);
  }
}
