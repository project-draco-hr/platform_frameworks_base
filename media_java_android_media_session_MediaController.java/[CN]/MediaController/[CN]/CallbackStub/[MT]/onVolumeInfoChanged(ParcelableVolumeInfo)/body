{
  MediaController controller=mController.get();
  if (controller != null) {
    VolumeInfo info=new VolumeInfo(pvi.volumeType,pvi.audioStream,pvi.controlType,pvi.maxVolume,pvi.currentVolume);
    controller.postMessage(MSG_UPDATE_VOLUME,info,null);
  }
}
