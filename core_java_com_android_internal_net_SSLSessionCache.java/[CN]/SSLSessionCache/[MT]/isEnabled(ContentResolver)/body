{
  String sslCache=Settings.Gservices.getString(resolver,Settings.Gservices.SSL_SESSION_CACHE);
  if (Log.isLoggable(TAG,Log.DEBUG)) {
    Log.d(TAG,"enabled " + sslCache);
  }
  return CACHE_TYPE_DB.equals(sslCache);
}
