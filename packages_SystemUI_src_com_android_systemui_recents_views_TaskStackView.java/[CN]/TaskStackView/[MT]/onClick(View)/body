{
  TaskView tv=(TaskView)v;
  Task task=tv.getTask();
  Console.log(Constants.Log.UI.ClickEvents,"[TaskStack|Clicked|Thumbnail]",task + " cb: " + mCb);
  if (closeOpenInfoPanes()) {
    return;
  }
  if (mCb != null) {
    mCb.onTaskLaunched(this,tv,mStack,task);
  }
}
