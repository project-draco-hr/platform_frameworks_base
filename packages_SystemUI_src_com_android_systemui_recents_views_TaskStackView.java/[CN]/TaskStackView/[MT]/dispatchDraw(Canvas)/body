{
  if (Console.Enabled) {
    Console.log(Constants.Log.UI.Draw,"[TaskStackView|dispatchDraw]","",Console.AnsiPurple);
  }
  synchronizeStackViewsWithModel();
  clipTaskViews();
  super.dispatchDraw(canvas);
}
