{
  if (Console.Enabled) {
    int refCount=mHwLayersTrigger.getCount();
    Console.log(Constants.Log.UI.HwLayers,"[TaskStackView|decHwLayersRefCount] refCount: " + refCount + "->"+ (refCount - 1)+ " "+ reason);
  }
  mHwLayersTrigger.decrement();
}
