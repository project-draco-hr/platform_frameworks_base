{
  tv.announceForAccessibility(getContext().getString(R.string.accessibility_recents_item_dismissed,task.title));
  mStack.removeTask(task,new TaskViewAnimation(DEFAULT_SYNC_STACK_DURATION,mFastOutSlowInInterpolator));
}
