{
  Task task=tv.getTask();
  Recents.getTaskLoader().unloadTaskData(task);
  detachViewFromParent(tv);
  updateTaskViewsList();
  tv.resetViewProperties();
  tv.setFocusedState(false,false,false);
  tv.setClipViewInStack(false);
}
