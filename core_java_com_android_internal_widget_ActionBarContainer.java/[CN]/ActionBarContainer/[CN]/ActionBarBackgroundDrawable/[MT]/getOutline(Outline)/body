{
  if (mIsSplit) {
    if (mSplitBackground != null) {
      mSplitBackground.getOutline(outline);
    }
  }
 else {
    if (mBackground != null) {
      mBackground.getOutline(outline);
    }
  }
}
