{
  ParcelFileDescriptor input=null;
  try {
    input=cr.openFileDescriptor(uri,"r");
    return makeBitmap(minSideLength,maxNumOfPixels,uri,cr,input,null);
  }
 catch (  IOException ex) {
    Log.e(TAG,"",ex);
    return null;
  }
 finally {
    closeSilently(input);
  }
}
