{
  if (activityExtras == null) {
    Slog.w(TAG,"getAssistContextExtras failed: timeout retrieving from activtity " + activity);
  }
  for (int i=0; i < services.size(); i++) {
    if (servicesExtras[i] == null) {
      Slog.w(TAG,"getAssistContextExtras failed: timeout retrieving from service " + i + " "+ services.get(i));
    }
  }
synchronized (this) {
    numPending=0;
    notifyAll();
  }
}
