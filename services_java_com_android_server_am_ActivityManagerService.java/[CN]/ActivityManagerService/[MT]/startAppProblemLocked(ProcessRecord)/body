{
  if (app.userId == mCurrentUserId) {
    app.errorReportReceiver=ApplicationErrorReport.getErrorReportReceiver(mContext,app.info.packageName,app.info.flags);
  }
 else {
    app.errorReportReceiver=null;
  }
  skipCurrentReceiverLocked(app);
}
