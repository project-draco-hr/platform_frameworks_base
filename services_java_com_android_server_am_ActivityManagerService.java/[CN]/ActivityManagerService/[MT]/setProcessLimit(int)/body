{
  enforceCallingPermission(android.Manifest.permission.SET_PROCESS_LIMIT,"setProcessLimit()");
synchronized (this) {
    mProcessLimit=max < 0 ? MAX_HIDDEN_APPS : max;
    mProcessLimitOverride=max;
  }
  trimApplications();
}
