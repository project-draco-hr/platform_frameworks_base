{
synchronized (this) {
    enforceCallingPermission(android.Manifest.permission.READ_FRAME_BUFFER,"getTaskTopThumbnail()");
    TaskRecord tr=recentTaskForIdLocked(id);
    if (tr != null) {
      return tr.stack.getTaskTopThumbnailLocked(tr);
    }
  }
  return null;
}
