{
  if (!sAnimations.contains(this) && (Thread.currentThread() == Looper.getMainLooper().getThread())) {
    long endTime=mDuration;
    if (mRepeatCount > 0) {
      endTime+=mRepeatCount * mDuration;
    }
    setCurrentPlayTime(endTime);
  }
  mPlayingState=ENDED;
}
