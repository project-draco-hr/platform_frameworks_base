{
  Path_Delegate pathDelegate=sManager.getDelegate(nPath);
  if (pathDelegate == null) {
    return;
  }
  pathDelegate.mPath.append(new Arc2D.Float(oval.left,oval.top,oval.width(),oval.height(),-startAngle,-sweepAngle,Arc2D.OPEN),false);
}
