{
  LocalePickerWithRegion localePicker=new LocalePickerWithRegion();
  boolean shouldShowTheList=localePicker.setListener(context,listener,parent,true,translatedOnly);
  return shouldShowTheList ? localePicker : null;
}
