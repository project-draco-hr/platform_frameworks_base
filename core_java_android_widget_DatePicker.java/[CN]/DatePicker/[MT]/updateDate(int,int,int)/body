{
  if (mYearPicker.getCurrent() != year || mDayPicker.getCurrent() != dayOfMonth || mMonthPicker.getCurrent() != month) {
    updatePickers(year,month,dayOfMonth);
    updateMiniMonth();
    notifyDateChanged();
  }
}
