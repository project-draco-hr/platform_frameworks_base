{
  ShortcutPackageInfo spi=mService.getPackageInfoForTest(packageName,userId);
  assertNotNull(spi);
  assertEquals(expectedVersion,spi.getVersionCode());
  assertTrue(spi.canRestoreTo(genPackage(packageName,0,9999999,packageName)));
}
