{
  if (inCallCall.getParentCallId() != null && !mCallByTelecommCallId.containsKey(inCallCall.getParentCallId())) {
    Log.wtf(this,"InCallCall %s has nonexistent parent %s",inCallCall.getId(),inCallCall.getParentCallId());
  }
  if (inCallCall.getChildCallIds() != null) {
    for (int i=0; i < inCallCall.getChildCallIds().size(); i++) {
      if (!mCallByTelecommCallId.containsKey(inCallCall.getChildCallIds().get(i))) {
        Log.wtf(this,"InCallCall %s has nonexistent child %s",inCallCall.getId(),inCallCall.getChildCallIds().get(i));
      }
    }
  }
}
