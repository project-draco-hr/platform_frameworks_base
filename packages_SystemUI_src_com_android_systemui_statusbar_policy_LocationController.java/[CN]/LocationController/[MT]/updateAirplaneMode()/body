{
  boolean wasAirplaneMode=mIsAirplaneMode;
  mIsAirplaneMode=(Settings.Global.getInt(mContext.getContentResolver(),Settings.Global.AIRPLANE_MODE_ON,0) == 1);
  if (mIsAirplaneMode != wasAirplaneMode) {
    refreshViews();
  }
}
