{
  ContentResolver contentResolver=mContext.getContentResolver();
  boolean isGpsEnabled=Settings.Secure.isLocationProviderEnabled(contentResolver,LocationManager.GPS_PROVIDER);
  boolean isNetworkEnabled=Settings.Secure.isLocationProviderEnabled(contentResolver,LocationManager.NETWORK_PROVIDER);
  return isGpsEnabled || isNetworkEnabled;
}
