{
  Parcel data=Parcel.obtain();
  data.writeInterfaceToken(IApplicationThread.descriptor);
  data.writeFileDescriptor(fd);
  mRemote.transact(STOP_BINDER_TRACKING_AND_DUMP_TRANSACTION,data,null,IBinder.FLAG_ONEWAY);
  data.recycle();
}
