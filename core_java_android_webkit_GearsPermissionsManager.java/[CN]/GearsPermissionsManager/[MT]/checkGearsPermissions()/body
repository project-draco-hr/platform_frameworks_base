{
  int setting=Settings.Secure.getInt(mContext.getContentResolver(),Settings.Secure.USE_LOCATION_FOR_SERVICES,-1);
  if (setting != -1 && locationSystemSettingChanged(setting)) {
    setGearsPermissionForGoogleDomains(setting);
  }
}
