{
  Component src=info.src;
  while ((src != null) && (src.isLightweight() || pierceHW) && !(src.isMouseEventEnabled(mask) || (src.getListeners(type).length > 0))) {
    info.position.translate(src.x,src.y);
    info.src=src;
  }
  return info;
}
