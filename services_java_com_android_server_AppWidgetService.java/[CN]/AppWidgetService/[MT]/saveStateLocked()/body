{
  File temp=savedStateTempFile();
  File real=savedStateRealFile();
  if (!real.exists()) {
    try {
      real.createNewFile();
    }
 catch (    IOException e) {
    }
  }
  if (temp.exists()) {
    temp.delete();
  }
  writeStateToFileLocked(temp);
  real.delete();
  temp.renameTo(real);
}
