{
  if (mExitIndex != 0) {
    mExitIndex=0;
    dispatchChanged();
  }
  final int mode=mCommittedMode == MODE_FULL ? MODE_FULL : MODE_LIMITED;
  if (mode != mMode) {
    mMode=mode;
    dispatchChanged();
  }
}
