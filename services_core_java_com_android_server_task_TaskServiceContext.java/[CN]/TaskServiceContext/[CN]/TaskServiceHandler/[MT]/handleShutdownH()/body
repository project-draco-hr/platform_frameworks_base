{
  for (int i=0; i < mPending.size(); i++) {
    ActiveTask at=mPending.valueAt(i);
    closeAndCleanupTaskH(at,true);
  }
  mWakeLock.release();
  mContext.unbindService(TaskServiceContext.this);
  service=null;
  mBound=false;
}
