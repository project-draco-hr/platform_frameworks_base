{
switch (executedTask.verb) {
case VERB_EXECUTING:
case VERB_STOPPING:
    closeAndCleanupTaskH(executedTask,reschedule);
  break;
default :
Log.e(TAG,"Got an execution complete message for a task that wasn't being" + "executed. " + executedTask);
}
}
