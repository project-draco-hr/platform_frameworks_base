{
switch (mVerb) {
case VERB_EXECUTING:
case VERB_STOPPING:
    closeAndCleanupTaskH(reschedule);
  break;
default :
Slog.e(TAG,"Got an execution complete message for a task that wasn't being" + "executed. Was " + VERB_STRINGS[mVerb] + ".");
}
}
