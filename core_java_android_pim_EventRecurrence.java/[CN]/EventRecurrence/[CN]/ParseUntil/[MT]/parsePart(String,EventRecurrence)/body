{
  if (VALIDATE_UNTIL) {
    try {
      Time until=new Time();
      until.parse(value);
    }
 catch (    TimeFormatException tfe) {
      throw new InvalidFormatException("Invalid UNTIL value: " + value);
    }
  }
  er.until=value;
  return PARSED_UNTIL;
}
