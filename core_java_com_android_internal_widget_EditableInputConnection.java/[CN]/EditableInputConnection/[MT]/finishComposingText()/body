{
  final boolean superResult=super.finishComposingText();
synchronized (this) {
    if (mBatchEditNesting < 0) {
      return false;
    }
    while (mBatchEditNesting > 0) {
      endBatchEdit();
    }
    mBatchEditNesting=-1;
  }
  return superResult;
}
