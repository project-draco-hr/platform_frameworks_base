{
  if (DBG)   Log.d(TAG,"startRecognition called by: " + listener.asBinder());
  if (mInternalService != null) {
    mInternalService.mHandler.sendMessage(Message.obtain(mInternalService.mHandler,MSG_START_RECOGNITION,listener));
  }
}
