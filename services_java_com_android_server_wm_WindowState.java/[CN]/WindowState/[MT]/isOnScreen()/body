{
  final AppWindowToken atoken=mAppToken;
  if (atoken != null) {
    return mWinAnimator.mSurface != null && mPolicyVisibility && !mDestroying && ((!mAttachedHidden && !atoken.hiddenRequested) || mWinAnimator.mAnimation != null || atoken.animation != null);
  }
 else {
    return mWinAnimator.mSurface != null && mPolicyVisibility && !mDestroying && (!mAttachedHidden || mWinAnimator.mAnimation != null);
  }
}
