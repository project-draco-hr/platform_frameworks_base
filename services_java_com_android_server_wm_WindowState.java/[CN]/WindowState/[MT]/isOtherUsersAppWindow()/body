{
  final int type=mAttrs.type;
  if ((mOwnerUserId != mService.mCurrentUserId) && (type >= TYPE_BASE_APPLICATION) && (type <= LAST_APPLICATION_WINDOW)&& (type != TYPE_APPLICATION_STARTING)) {
    return true;
  }
  return false;
}
