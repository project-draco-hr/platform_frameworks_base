{
  final AppWindowToken atoken=mAppToken;
  return ((mWinAnimator.mSurface != null && !mWinAnimator.mReportDestroySurface) || (!mRelayoutCalled && mViewVisibility == View.VISIBLE)) && mPolicyVisibility && !mAttachedHidden&& (atoken == null || !atoken.hiddenRequested)&& !mExiting&& !mDestroying;
}
