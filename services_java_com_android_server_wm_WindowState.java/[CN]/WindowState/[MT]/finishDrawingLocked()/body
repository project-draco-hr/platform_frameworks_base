{
  if (mDrawPending) {
    if (SHOW_TRANSACTIONS || WindowManagerService.DEBUG_ORIENTATION)     Slog.v(TAG,"finishDrawingLocked: " + this + " in "+ mWinAnimator.mSurface);
    mCommitDrawPending=true;
    mDrawPending=false;
    return true;
  }
  return false;
}
