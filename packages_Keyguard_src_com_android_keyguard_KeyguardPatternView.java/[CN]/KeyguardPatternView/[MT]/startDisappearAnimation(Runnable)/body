{
  mLockPatternView.clearPattern();
  animate().alpha(0f).translationY(-100).setInterpolator(AnimationUtils.loadInterpolator(mContext,android.R.interpolator.fast_out_linear_in)).setDuration(100).withEndAction(finishRunnable);
  return true;
}
