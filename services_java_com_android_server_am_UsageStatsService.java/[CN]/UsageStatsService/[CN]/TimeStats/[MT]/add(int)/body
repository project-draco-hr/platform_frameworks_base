{
  final int[] bins=LAUNCH_TIME_BINS;
  for (int i=0; i < NUM_LAUNCH_TIME_BINS - 1; i++) {
    if (val < bins[i]) {
      mTimes[i]++;
      return;
    }
  }
  mTimes[NUM_LAUNCH_TIME_BINS - 1]++;
}
