{
  if (DEBUG)   Log.v(LOGTAG,"creating relro files");
  if (new File(CHROMIUM_WEBVIEW_NATIVE_LIB_64).exists()) {
    createRelroFile(true);
  }
  if (new File(CHROMIUM_WEBVIEW_NATIVE_LIB_32).exists()) {
    createRelroFile(false);
  }
}
