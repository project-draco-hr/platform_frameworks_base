{
synchronized (mBinder) {
    if (mHeld) {
      try {
        mService.releaseWifiLock(mBinder);
      }
 catch (      RemoteException e) {
      }
      RuntimeInit.crash("WifiLock",new Exception("WifiLock finalized while still held: " + mTag));
    }
  }
}
