{
  if (config != null) {
    int netId=addOrUpdateNetworkNative(config);
    if (netId != -1) {
      selectNetwork(netId);
    }
 else {
      Log.e(TAG,"Failed to update network " + config);
    }
  }
}
