{
  if (mParent == null) {
    startActivityForResultInner(intent,requestCode,fillInIntent,flagsMask,flagsValues,this);
  }
 else {
    mParent.startActivityFromChild(this,intent,requestCode,fillInIntent,flagsMask,flagsValues);
  }
}
