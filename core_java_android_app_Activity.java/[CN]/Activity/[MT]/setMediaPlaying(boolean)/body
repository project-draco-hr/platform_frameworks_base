{
  if (!mResumed) {
    playing=false;
  }
  try {
    mMediaPlaying=ActivityManagerNative.getDefault().setMediaPlaying(mToken,playing) && playing;
  }
 catch (  RemoteException e) {
    mMediaPlaying=false;
  }
  return mMediaPlaying;
}
