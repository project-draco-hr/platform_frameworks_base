{
  if (mBar != null) {
    final boolean showX=(isShowing() && mNotificationScroller.getVisibility() == View.VISIBLE && mNotificationCount > 0);
    mBar.getClearButton().setVisibility(showX ? View.VISIBLE : View.INVISIBLE);
  }
}
