{
  final View view=LayoutInflater.from(getContext()).inflate(R.layout.color_matrix_settings,container,false);
  ((ViewGroup)view).addView(super.onCreateView(inflater,container,savedInstanceState));
  return view;
}
