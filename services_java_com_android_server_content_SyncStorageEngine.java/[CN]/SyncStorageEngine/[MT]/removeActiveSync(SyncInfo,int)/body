{
synchronized (mAuthorities) {
    if (DEBUG) {
      Log.v(TAG,"removeActiveSync: account=" + syncInfo.account + " user="+ userId+ " auth="+ syncInfo.authority+ " service="+ syncInfo.service);
    }
    getCurrentSyncs(userId).remove(syncInfo);
  }
  reportActiveChange();
}
