{
  if (isEncrypting()) {
    return new ZeroPaddingEncryptionStreamer(super.createMainDataStreamer(keyStore,operationToken),getModulusSizeBytes());
  }
 else {
    return super.createMainDataStreamer(keyStore,operationToken);
  }
}
