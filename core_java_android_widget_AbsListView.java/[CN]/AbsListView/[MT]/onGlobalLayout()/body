{
  if (isShown()) {
    if (mFiltered && mPopup != null && !mPopup.isShowing()) {
      showPopup();
    }
  }
 else {
    if (mPopup != null && mPopup.isShowing()) {
      dismissPopup();
    }
  }
}
