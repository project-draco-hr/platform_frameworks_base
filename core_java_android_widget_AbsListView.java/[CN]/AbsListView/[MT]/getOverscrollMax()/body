{
  final int childCount=getChildCount();
  if (childCount > 0) {
    return Math.min(mOverscrollMax,getChildAt(childCount - 1).getBottom() / OVERSCROLL_LIMIT_DIVISOR);
  }
 else {
    return mOverscrollMax;
  }
}
