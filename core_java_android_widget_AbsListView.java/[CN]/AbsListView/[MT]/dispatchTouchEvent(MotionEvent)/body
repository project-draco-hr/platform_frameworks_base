{
  if (mGestures != GESTURES_NONE) {
    if ((ev.getAction() != MotionEvent.ACTION_DOWN || mFastScroller == null || !mFastScroller.isPointInside(ev.getX(),ev.getY())) && mGesturesPopup.isShowing()) {
      mGesturesOverlay.dispatchTouchEvent(ev);
    }
  }
  return super.dispatchTouchEvent(ev);
}
