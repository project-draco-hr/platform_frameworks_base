{
  mHolder[0]=prediction.name.charAt(0);
  final KeyEvent[] events=mKeyMap.getEvents(mHolder);
  if (events != null) {
    for (    KeyEvent event : events) {
      sendToTextFilter(event.getKeyCode(),event.getRepeatCount(),event);
    }
  }
}
