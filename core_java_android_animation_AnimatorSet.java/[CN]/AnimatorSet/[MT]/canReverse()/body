{
  if (!mReversible) {
    return false;
  }
  for (  Node node : mNodes) {
    if (!node.animation.canReverse() || node.animation.getStartDelay() > 0) {
      return false;
    }
  }
  return true;
}
