{
  if (postAnimRunnable != null) {
    animate().withEndAction(postAnimRunnable);
  }
  animate().alpha(finalAlpha).setStartDelay(delay).setInterpolator(mConfig.fastOutSlowInInterpolator).setDuration(duration).withLayer().start();
}
