{
  if (isFreeformTask) {
    return Math.min(mLayoutRect.width() / mBitmapRect.width(),mLayoutRect.height() / mBitmapRect.height());
  }
 else {
    return Math.max(1f,mLayoutRect.width() / mBitmapRect.width());
  }
}
