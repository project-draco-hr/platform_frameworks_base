{
  mTask=t;
  if (t.thumbnail != null) {
    setThumbnail(t.thumbnail);
    if (t.colorBackground != 0) {
      mBgFillPaint.setColor(t.colorBackground);
    }
  }
 else {
    setThumbnail(null);
  }
}
