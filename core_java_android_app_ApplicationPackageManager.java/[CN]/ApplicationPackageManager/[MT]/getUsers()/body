{
  try {
    return mPM.getUsers();
  }
 catch (  RemoteException re) {
    ArrayList<UserInfo> users=new ArrayList<UserInfo>();
    UserInfo primary=new UserInfo(0,"Root!",null,UserInfo.FLAG_ADMIN | UserInfo.FLAG_PRIMARY);
    users.add(primary);
    return users;
  }
}
