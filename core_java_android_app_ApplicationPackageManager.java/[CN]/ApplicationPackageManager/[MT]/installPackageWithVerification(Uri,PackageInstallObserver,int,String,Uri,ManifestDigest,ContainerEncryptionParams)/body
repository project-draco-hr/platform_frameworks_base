{
  try {
    mPM.installPackageWithVerificationEtc(packageURI,null,observer.mObserver,flags,installerPackageName,verificationURI,manifestDigest,encryptionParams);
  }
 catch (  RemoteException e) {
  }
}
