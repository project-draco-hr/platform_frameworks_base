{
  if (isExpanded() || isSearching()) {
    mSearchView.setQuery("",false);
    if (mFullBar) {
      onClose();
    }
 else {
      mSearchView.setIconified(true);
    }
    return true;
  }
  return false;
}
