{
  if (MediaFrameworkPerfTestRunner.mGetNativeHeapDump)   MediaTestUtil.getNativeHeapDump(this.getName() + "_after");
  if (MediaFrameworkPerfTestRunner.mGetProcmem) {
    mMemWriter.write("\n");
    mProcMemWriter.close();
    mMemWriter.close();
  }
  super.tearDown();
}
