{
  mEndPid=getMediaserverPid();
  mEndMemory=getMediaserverVsize();
  if (startPid != mEndPid)   return false;
  if ((mEndMemory - startMemory) > MAX_ACCEPTED_MEMORY_LEAK_KB)   return false;
  return true;
}
