{
  if (DEBUG)   Slog.d(TAG,"onAnimationEnd: mTarget=" + mTarget + " mMoveToFullScreen="+ mMoveToFullScreen+ " mWillReplace="+ mWillReplace);
  if (mMoveToFullScreen && !mWillReplace) {
    mTarget.moveToFullscreen();
  }
  finishAnimation();
}
