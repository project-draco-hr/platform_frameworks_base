{
  if (isPopupShowing()) {
    showDropDown();
  }
 else   if (mAdapter != null) {
    post(new Runnable(){
      public void run(){
        updateDropDownForFilter(mAdapter.getCount());
      }
    }
);
  }
}
