{
  if (enoughToFilter()) {
    if (mFilter != null) {
      performFiltering(getText(),mLastKeyCode);
      buildImeCompletions();
    }
  }
 else {
    if (!mPopup.isDropDownAlwaysVisible())     dismissDropDown();
    if (mFilter != null) {
      mFilter.filter(null);
    }
  }
}
