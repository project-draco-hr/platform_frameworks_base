{
  super.onRtlPropertiesChanged(layoutDirection);
  if (!isLayoutRtl()) {
    mResolvedSwipeDirection=mSwipeDirection;
  }
 else {
    mResolvedSwipeDirection=mSwipeDirection == SWIPE_DIRECTION_START ? SWIPE_DIRECTION_RIGHT : SWIPE_DIRECTION_LEFT;
  }
}
