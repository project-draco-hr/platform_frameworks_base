{
  try {
    new AndroidKeyPairGeneratorSpec(getContext(),TEST_ALIAS_1,TEST_DN_1,SERIAL_1,NOW,null);
    fail("Should throw IllegalArgumentException when keystoreAlias is null");
  }
 catch (  IllegalArgumentException success) {
  }
}
