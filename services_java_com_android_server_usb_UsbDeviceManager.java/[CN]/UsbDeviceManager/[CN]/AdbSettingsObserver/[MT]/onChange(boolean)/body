{
  mAdbEnabled=(Settings.Secure.getInt(mContentResolver,Settings.Secure.ADB_ENABLED,0) > 0);
  SystemProperties.set("persist.service.adb.enable",mAdbEnabled ? "1" : "0");
  updateAdbNotification();
}
