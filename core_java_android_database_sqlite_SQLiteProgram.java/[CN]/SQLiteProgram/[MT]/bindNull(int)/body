{
  mDatabase.verifyDbIsOpen();
synchronized (this) {
    acquireReference();
    try {
      if (this.nStatement == 0) {
        addToBindArgs(index,null);
      }
 else {
        native_bind_null(index);
      }
    }
  finally {
      releaseReference();
    }
  }
}
