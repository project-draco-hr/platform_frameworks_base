{
  if (!mDatabase.isOpen()) {
    throw new IllegalStateException("database " + mDatabase.getPath() + " already closed");
  }
  mDatabase.lock();
  try {
    releaseReference();
  }
  finally {
    mDatabase.unlock();
  }
}
