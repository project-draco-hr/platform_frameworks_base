{
  resetPasswordText(false);
  long deadline=mLockPatternUtils.getLockoutAttemptDeadline(mLockPatternUtils.getCurrentUser());
  if (shouldLockout(deadline)) {
    handleAttemptLockout(deadline);
  }
 else {
    resetState();
  }
}
