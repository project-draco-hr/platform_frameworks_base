{
  float stretch=0;
  if (max - min > MIN_SCROLL_AMOUNT_TO_DISABLE_DRAG_TRACKER) {
    if (delta < min) {
      stretch=delta - min;
    }
 else     if (delta > max) {
      stretch=delta - max;
    }
  }
  return stretch;
}
