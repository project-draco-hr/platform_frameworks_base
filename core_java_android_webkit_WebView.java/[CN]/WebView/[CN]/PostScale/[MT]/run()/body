{
  if (mWebView.mWebViewCore != null) {
    mWebView.setNewZoomScale(mWebView.mActualScale,mUpdateTextWrap,true);
    if (mWebView.getSettings().getBuiltInZoomControls()) {
      mWebView.updateZoomButtonsEnabled();
    }
  }
}
