{
  if (mDrawHistory) {
    return mHistoryHeight;
  }
 else   if (mVerticalScrollBarMode == SCROLLBAR_ALWAYSOFF && !mZoomManager.canZoomOut()) {
    return computeVerticalScrollExtent();
  }
 else {
    return (int)Math.floor(mContentHeight * mZoomManager.getScale());
  }
}
