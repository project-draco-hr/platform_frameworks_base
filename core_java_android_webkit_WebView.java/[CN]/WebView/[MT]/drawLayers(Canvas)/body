{
  if (mRootLayer != 0) {
    int scrollY=computeVerticalScrollOffset();
    int viewHeight=getHeight() - getVisibleTitleHeight();
    nativeDrawLayers(mRootLayer,mScrollX,scrollY,getWidth(),viewHeight,mActualScale,canvas);
  }
}
