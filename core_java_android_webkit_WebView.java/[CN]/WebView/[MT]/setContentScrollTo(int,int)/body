{
  if (mDrawHistory) {
    return false;
  }
  int vx=contentToViewX(cx);
  int vy=contentToViewY(cy);
  pinScrollTo(vx,vy,false,0);
  if (mScrollX != vx || mScrollY != vy) {
    return true;
  }
 else {
    return false;
  }
}
