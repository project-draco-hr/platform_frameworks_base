{
  if (mDrawHistory) {
    return false;
  }
  int vx=contentToView(cx);
  int vy=contentToView(cy);
  pinScrollTo(vx,vy,false);
  if (mScrollX != vx || mScrollY != vy) {
    return true;
  }
 else {
    return false;
  }
}
