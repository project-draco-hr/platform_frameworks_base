{
  return mDatabase.query(TABLE_DOCUMENTS,columnNames,COLUMN_ROW_STATE + " IN (?, ?) AND " + COLUMN_PARENT_DOCUMENT_ID+ " = ?",strings(ROW_STATE_MAPPED,ROW_STATE_UNMAPPED,parentDocumentId),null,null,null);
}
