{
  mTestCase=testCase;
  mPropertyNodesVerifierList=new ArrayList<PropertyNodesVerifier>();
  mIsV30=(version == V30);
  mExpectedLineList=new ArrayList<String>();
  mContactStructVerifier=contactStructVerifier;
  mVCardType=(version == V30 ? VCardConfig.VCARD_TYPE_V30_GENERIC_UTF8 : VCardConfig.VCARD_TYPE_V21_GENERIC_UTF8);
  mCount=1;
}
