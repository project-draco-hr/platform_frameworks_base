{
  ExportTestResolver resolver=new ExportTestResolver();
  resolver.buildInput(Email.CONTENT_ITEM_TYPE).put(Email.DATA,"sample@example.com");
  VCardVerificationHandler handler=new VCardVerificationHandler(this,version);
  handler.addPropertyVerifierWithEmptyName().addNodeWithoutOrder("EMAIL","sample@example.com");
  verifyOneComposition(resolver,handler,version);
}
