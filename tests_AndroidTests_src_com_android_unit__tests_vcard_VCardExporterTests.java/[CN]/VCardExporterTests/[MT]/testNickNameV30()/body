{
  VCardVerifier verifier=new VCardVerifier(V30);
  ContactEntry entry=verifier.addInputEntry();
  entry.buildData(Nickname.CONTENT_ITEM_TYPE).put(Nickname.NAME,"Nicky");
  verifier.addPropertyNodesVerifierElemWithEmptyName().addNodeWithOrder("NICKNAME","Nicky");
  verifier.verify();
}
