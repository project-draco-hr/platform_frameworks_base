{
  ExportTestResolver resolver=new ExportTestResolver();
  resolver.buildInput(Nickname.CONTENT_ITEM_TYPE).put(Nickname.NAME,"Nicky");
  VCardVerificationHandler handler=new VCardVerificationHandler(this,V30);
  handler.addPropertyVerifierWithEmptyName().addNodeWithOrder("NICKNAME","Nicky");
  verifyOneComposition(resolver,handler,V30);
}
