{
  Context realTestContext=getContext();
  Context mockContext=new MyMockContext(realTestContext);
  setContext(mockContext);
  mAms=new MyAccountManagerService(getContext(),new MyMockPackageManager(),new MockAccountAuthenticatorCache(),realTestContext);
}
