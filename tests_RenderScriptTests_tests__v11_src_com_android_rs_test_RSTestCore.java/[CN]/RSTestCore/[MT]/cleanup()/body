{
  stopTesting=true;
  UnitTest t=activeTest;
  if (mTimer != null) {
    mTimer.cancel();
    mTimer.purge();
    mTimer=null;
  }
  if (t != null) {
    try {
      t.join();
    }
 catch (    InterruptedException e) {
    }
    t=null;
  }
}
