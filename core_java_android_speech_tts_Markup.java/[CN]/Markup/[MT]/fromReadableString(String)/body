{
  while (!isWhitespace(str)) {
    String newStr=matchValue(str);
    if (newStr == null) {
      newStr=matchMarkup(str);
      if (newStr == null) {
        return str;
      }
    }
    str=newStr;
  }
  return "";
}
