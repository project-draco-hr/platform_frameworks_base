{
  if (type != null) {
    Matcher matcher=legalIdentifierPattern.matcher(type);
    if (!matcher.matches()) {
      throw new IllegalArgumentException("Type cannot be empty and may only contain " + "0-9, a-z and underscores.");
    }
  }
  mType=type;
}
