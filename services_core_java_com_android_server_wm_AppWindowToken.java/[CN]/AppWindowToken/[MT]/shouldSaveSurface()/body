{
  mHasSavedSurface=allDrawn || mAnimatingWithSavedSurface;
  if (mHasSavedSurface) {
    if (DEBUG_APP_TRANSITIONS || DEBUG_ANIM)     Slog.v(TAG,"Saving surface: " + this);
    return true;
  }
  return false;
}
