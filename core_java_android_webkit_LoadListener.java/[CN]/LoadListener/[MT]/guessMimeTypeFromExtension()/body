{
  if (WebView.LOGV_ENABLED) {
    Log.v(LOGTAG,"guessMimeTypeFromExtension: mURL = " + mUrl);
  }
  return MimeTypeMap.getSingleton().getMimeTypeFromExtension(MimeTypeMap.getFileExtensionFromUrl(mUrl));
}
