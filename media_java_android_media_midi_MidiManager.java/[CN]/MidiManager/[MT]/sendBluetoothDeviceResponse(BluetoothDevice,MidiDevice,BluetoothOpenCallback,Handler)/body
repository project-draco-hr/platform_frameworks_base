{
  if (handler != null) {
    handler.post(new Runnable(){
      @Override public void run(){
        callback.onDeviceOpened(bluetoothDevice,device);
      }
    }
);
  }
 else {
    callback.onDeviceOpened(bluetoothDevice,device);
  }
}
