{
synchronized (EmbeddedRecognizerImpl.class) {
    if (nativeObject == 0)     throw new IllegalStateException("Object was destroyed.");
  }
  Vector<Grammar> grammars=new Vector<Grammar>();
  grammars.add(grammar);
  recognize(audio,grammars);
}
