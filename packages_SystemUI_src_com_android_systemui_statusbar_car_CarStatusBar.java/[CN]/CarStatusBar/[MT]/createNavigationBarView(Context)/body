{
  if (mNavigationBarView != null) {
    return;
  }
  CarNavigationBarView carNavBar=(CarNavigationBarView)View.inflate(context,R.layout.car_navigation_bar,null);
  carNavBar.setActivityStarter(this);
  mNavigationBarView=carNavBar;
}
