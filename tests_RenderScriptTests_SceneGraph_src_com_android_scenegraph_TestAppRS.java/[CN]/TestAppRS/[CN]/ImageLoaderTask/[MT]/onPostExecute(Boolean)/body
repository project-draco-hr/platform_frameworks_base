{
  if (tempEnv != null) {
    mEnvCube=tempEnv;
    mPaintF.mProgram.bindTexture(mEnvCube,1);
  }
  if (tempDiff != null) {
    mDiffCube=tempDiff;
    mAluminumF.mProgram.bindTexture(mDiffCube,1);
  }
}
