{
  if (mTerminated) {
    mCallback.onMessageSendFail(messageId,WifiNanSessionCallback.FAIL_REASON_SESSION_TERMINATED);
    return;
  }
  mManager.sendMessage(mSessionId,peerId,message,messageLength,messageId);
}
