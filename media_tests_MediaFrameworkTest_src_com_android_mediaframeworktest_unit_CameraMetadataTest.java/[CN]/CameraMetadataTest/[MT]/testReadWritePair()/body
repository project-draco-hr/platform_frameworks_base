{
  checkKeyMarshal("android.lens.focusRange",new TypeReference<Pair<Float,Float>>(){
{
    }
  }
,Pair.create(1.0f / 2.0f,1.0f / 3.0f),toByteArray(1.0f / 2.0f,1.0f / 3.0f));
  checkKeyMarshal("android.flash.mode",new TypeReference<Pair<Byte,Integer>>(){
{
    }
  }
,Pair.create((byte)123,22),toByteArray((byte)123,(byte)22));
}
