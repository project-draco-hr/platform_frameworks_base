{
  @SuppressWarnings("unchecked") Class<T> expectedClass=(Class<T>)expected.getClass();
  return checkKeyMarshal(keyName,TypeReference.createSpecializedTypeReference(expectedClass),expected,expectedNative);
}
