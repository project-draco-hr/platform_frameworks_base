{
  if (mRanking == null) {
    for (    Entry entry : mEntries) {
      if (key.equals(entry.key)) {
        return entry.notification.getNotification().priority == Notification.PRIORITY_MIN;
      }
    }
  }
 else {
    Ranking ranking=mRanking.getRanking(key);
    return ranking != null && ranking.isAmbient();
  }
  return false;
}
