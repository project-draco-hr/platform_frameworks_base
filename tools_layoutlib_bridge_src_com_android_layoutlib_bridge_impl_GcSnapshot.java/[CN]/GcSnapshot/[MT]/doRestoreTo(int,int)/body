{
  if (size <= saveCount) {
    return this;
  }
  GcSnapshot previous=doRestore();
  if (size == saveCount + 1) {
    return previous;
  }
 else {
    return previous.doRestoreTo(size - 1,saveCount);
  }
}
