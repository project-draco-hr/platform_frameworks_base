{
  mGraphics2D=graphics2D;
  if (mTransform != null) {
    mGraphics2D.setTransform(mTransform);
    mTransform=null;
  }
  if (mClip != null) {
    mGraphics2D.setClip(mClip);
    mClip=null;
  }
}
