{
  Graphics2D originalGraphics=layer.getGraphics();
  Graphics2D configuredGraphics2D=paint != null ? createCustomGraphics(originalGraphics,paint,compositeOnly) : (Graphics2D)originalGraphics.create();
  try {
    drawable.draw(configuredGraphics2D,paint);
  }
  finally {
    configuredGraphics2D.dispose();
  }
}
