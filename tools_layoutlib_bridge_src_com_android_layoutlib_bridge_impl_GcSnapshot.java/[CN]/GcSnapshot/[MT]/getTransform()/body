{
  if (mLayers.size() > 0) {
    return mLayers.get(0).getGraphics().getTransform();
  }
 else {
    if (mTransform == null) {
      mTransform=new AffineTransform();
    }
    return mTransform;
  }
}
