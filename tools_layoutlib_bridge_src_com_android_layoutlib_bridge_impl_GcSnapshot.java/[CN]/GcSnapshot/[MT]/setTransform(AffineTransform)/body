{
  if (mGraphics2D != null) {
    mGraphics2D.setTransform(transform);
  }
 else {
    if (mTransform == null) {
      mTransform=new AffineTransform();
    }
    mTransform.setTransform(transform);
  }
}
