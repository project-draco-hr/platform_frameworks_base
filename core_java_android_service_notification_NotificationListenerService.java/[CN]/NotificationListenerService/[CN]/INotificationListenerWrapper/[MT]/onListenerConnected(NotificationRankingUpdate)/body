{
synchronized (mWrapper) {
    applyUpdate(update);
    try {
      NotificationListenerService.this.onListenerConnected(mRanking.getOrderedKeys());
    }
 catch (    Throwable t) {
      Log.w(TAG,"Error running onListenerConnected",t);
    }
  }
}
