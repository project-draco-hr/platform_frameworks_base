{
  if (i == null) {
    return 0;
  }
  int num=0;
  while (i.hasNext()) {
    String name=i.next();
    num++;
    if (localLOGV)     Log.v(TAG,prefix + name);
    if (!remove_all_objects(dest.get(name),filter)) {
      dest.remove(name);
    }
  }
  return num;
}
