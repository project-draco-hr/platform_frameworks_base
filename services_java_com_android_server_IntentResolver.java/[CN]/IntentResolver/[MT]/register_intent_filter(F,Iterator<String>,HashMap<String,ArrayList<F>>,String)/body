{
  if (i == null) {
    return 0;
  }
  int num=0;
  while (i.hasNext()) {
    String name=i.next();
    num++;
    if (localLOGV)     Log.v(TAG,prefix + name);
    ArrayList<F> array=dest.get(name);
    if (array == null) {
      array=new ArrayList<F>();
      dest.put(name,array);
    }
    array.add(filter);
  }
  return num;
}
