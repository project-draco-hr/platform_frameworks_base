{
  checkToolbarInitialized();
  mOriginatingView.getLocationInWindow(mViewPosition);
  mViewRect.set(mViewPosition[0],mViewPosition[1],mViewPosition[0] + mOriginatingView.getWidth(),mViewPosition[1] + mOriginatingView.getHeight());
  repositionToolbar();
}
