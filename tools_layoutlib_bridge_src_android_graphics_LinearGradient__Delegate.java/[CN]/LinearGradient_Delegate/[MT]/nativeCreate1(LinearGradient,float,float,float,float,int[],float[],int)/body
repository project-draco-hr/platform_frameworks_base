{
  TileMode tile=null;
  for (  TileMode tm : TileMode.values()) {
    if (tm.nativeInt == tileMode) {
      tile=tm;
      break;
    }
  }
  LinearGradient_Delegate newDelegate=new LinearGradient_Delegate(x0,y0,x1,y1,colors,positions,tile);
  return sManager.addDelegate(newDelegate);
}
