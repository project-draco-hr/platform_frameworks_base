{
  if (mDrawState == DRAW_PENDING) {
    if (SHOW_TRANSACTIONS || DEBUG_ORIENTATION)     Slog.v(TAG,"finishDrawingLocked: " + this + " in "+ mSurface);
    mDrawState=COMMIT_DRAW_PENDING;
    return true;
  }
  return false;
}
