{
  if (viewName.indexOf('.') == -1) {
    if (Objects.equals(viewName,"View") || Objects.equals(viewName,"ViewGroup") || Objects.equals(viewName,"ViewStub")) {
      return "android.view." + viewName;
    }
    return "android.widget." + viewName;
  }
  return viewName;
}
