{
  if (!inEditingMode()) {
    return false;
  }
  mDragFromTextInput=true;
  event.offsetLocation((mWebTextView.getLeft() - getScrollX()),(mWebTextView.getTop() - getScrollY()));
  boolean result=onTouchEvent(event);
  mDragFromTextInput=false;
  return result;
}
