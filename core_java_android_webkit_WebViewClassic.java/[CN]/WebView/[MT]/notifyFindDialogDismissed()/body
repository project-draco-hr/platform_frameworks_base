{
  mFindCallback=null;
  mCachedOverlappingActionModeHeight=-1;
  if (mWebViewCore == null) {
    return;
  }
  clearMatches();
  setFindIsUp(false);
  pinScrollTo(mScrollX,mScrollY,false,0);
  invalidate();
}
