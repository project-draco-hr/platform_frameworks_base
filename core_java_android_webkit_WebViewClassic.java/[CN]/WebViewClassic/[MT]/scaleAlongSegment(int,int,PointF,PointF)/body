{
  float abX=b.x - a.x;
  float abY=b.y - a.y;
  float ab2=(abX * abX) + (abY * abY);
  float apX=x - a.x;
  float apY=y - a.y;
  float abDotAP=(apX * abX) + (apY * abY);
  float scale=abDotAP / ab2;
  return scale;
}
