{
  if (mHandleAlpha.getAlpha() == 0) {
    return;
  }
  ensureSelectionHandles();
  if (mSelectingText) {
    if (mIsCaretSelection) {
      int x=contentToViewDimension(mSelectCursorBase.x) - (mSelectHandleCenter.getIntrinsicWidth() / 2);
      int y=contentToViewDimension(mSelectCursorBase.y);
      mSelectHandleBaseBounds.set(x,y,x + mSelectHandleCenter.getIntrinsicWidth(),y + mSelectHandleCenter.getIntrinsicHeight());
      mSelectHandleCenter.setBounds(mSelectHandleBaseBounds);
      mSelectHandleCenter.draw(canvas);
    }
 else {
      drawHandle(mSelectCursorBase,HANDLE_ID_BASE,mSelectHandleBaseBounds,canvas);
      drawHandle(mSelectCursorExtent,HANDLE_ID_EXTENT,mSelectHandleExtentBounds,canvas);
    }
  }
}
