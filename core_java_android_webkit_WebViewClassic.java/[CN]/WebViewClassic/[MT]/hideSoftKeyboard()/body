{
  InputMethodManager imm=InputMethodManager.peekInstance();
  if (imm != null && (imm.isActive(mWebView) || (inEditingMode() && imm.isActive(mWebTextView)))) {
    imm.hideSoftInputFromWindow(mWebView.getWindowToken(),0);
  }
}
