{
  mFindCallback=null;
  mCachedOverlappingActionModeHeight=-1;
  if (mWebViewCore == null) {
    return;
  }
  clearMatches();
  setFindIsUp(false);
  pinScrollTo(getScrollX(),getScrollY(),false,0);
  invalidate();
}
