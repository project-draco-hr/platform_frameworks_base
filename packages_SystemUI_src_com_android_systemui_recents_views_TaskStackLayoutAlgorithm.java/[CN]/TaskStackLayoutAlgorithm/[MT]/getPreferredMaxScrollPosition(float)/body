{
  float maxStackScrollBounds=mStackEndScrollP + SNAP_TO_MAX_STACK_SCROLL_FACTOR * (mMaxScrollP - mStackEndScrollP);
  if (scroll < maxStackScrollBounds) {
    return mPreferredStackEndScrollP;
  }
  return mMaxScrollP;
}
