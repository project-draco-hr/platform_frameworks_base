{
  if (propName == null || propName.equals("") || values == null || values.isEmpty())   return 1;
  if (mSpecialValueSetMap.containsKey(propName)) {
    for (    String value : values) {
      if (!mSpecialValueSetMap.get(propName).contains(value)) {
        if (!value.startsWith("X-"))         return PARSE_ERROR;
      }
    }
  }
  return 1;
}
