{
  String countryCode=mContext.getResources().getString(R.string.config_wifi_default_country_code);
  if (countryCode != null && !countryCode.isEmpty()) {
    setCountryCode(countryCode,false);
  }
 else {
  }
}
