{
  char c0;
  char c1=0;
  boolean success;
  c0=command.charAt(6);
  if (command.length() >= 8) {
    c1=command.charAt(7);
  }
  success=simulatedCallState.onChld(c0,c1);
  if (!success) {
    throw new InterpreterEx("ERROR");
  }
}
