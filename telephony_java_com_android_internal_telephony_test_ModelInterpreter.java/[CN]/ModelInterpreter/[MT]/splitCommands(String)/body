{
  if (!line.startsWith("AT")) {
    throw new InterpreterEx("ERROR");
  }
  if (line.length() == 2) {
    return new String[0];
  }
  String ret[]=new String[1];
  ret[0]=line.substring(2);
  return ret;
}
