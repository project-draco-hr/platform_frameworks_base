{
synchronized (this) {
    try {
      byte[] bytes=s.getBytes("US-ASCII");
      out.write(bytes);
      out.write('\r');
    }
 catch (    IOException ex) {
      ex.printStackTrace();
    }
  }
}
