{
  super(c);
  mPaint=new Paint();
  mPaint.setAntiAlias(true);
  mPaint.setTextSize(50.0f);
  mPaint.setTextAlign(Paint.Align.CENTER);
  setRotationY(45.0f);
  setScaleX(2.0f);
  setScaleY(2.0f);
}
