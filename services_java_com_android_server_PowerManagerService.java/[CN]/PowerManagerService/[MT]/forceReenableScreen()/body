{
  if (!mPreventScreenOn) {
    Log.w(TAG,"forceReenableScreen: mPreventScreenOn is false, nothing to do");
    return;
  }
  Log.w(TAG,"App called preventScreenOn(true) but didn't promptly reenable the screen! " + "Forcing the screen back on...");
  preventScreenOn(false);
}
