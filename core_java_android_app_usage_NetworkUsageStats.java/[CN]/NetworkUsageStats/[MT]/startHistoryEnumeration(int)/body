{
  mHistory=null;
  try {
    mHistory=mSession.getHistoryForUid(mTemplate,uid,NetworkStats.SET_ALL,NetworkStats.TAG_NONE,NetworkStatsHistory.FIELD_ALL);
    setSingleUid(uid);
  }
 catch (  RemoteException e) {
    Log.w(TAG,e);
  }
  mEnumerationIndex=0;
}
