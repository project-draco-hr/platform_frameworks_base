{
  if (hasNextUid()) {
    stepUid();
    mHistory=null;
    try {
      mHistory=mSession.getHistoryForUid(mTemplate,getUid(),NetworkStats.SET_ALL,NetworkStats.TAG_NONE,NetworkStatsHistory.FIELD_ALL);
    }
 catch (    RemoteException e) {
      Log.w(TAG,e);
    }
    mEnumerationIndex=0;
  }
}
