{
  super.onSizeChanged(w,h,oldw,oldh);
  mLeftHandleX=(int)(EDGE_PADDING_DIP * mDensity) + mDimple.getIntrinsicWidth() / 2;
  mRightHandleX=getWidth() - (int)(EDGE_PADDING_DIP * mDensity) - mDimple.getIntrinsicWidth() / 2;
}
