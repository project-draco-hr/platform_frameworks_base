{
  super.onSizeChanged(w,h,oldw,oldh);
  mLeftHandleX=(int)(EDGE_PADDING_DIP * mDensity) + mDimpleWidth / 2;
  mRightHandleX=getWidth() - (int)(EDGE_PADDING_DIP * mDensity) - mDimpleWidth / 2;
  mDimpleSpacing=(getWidth() / 2) - mLeftHandleX;
}
