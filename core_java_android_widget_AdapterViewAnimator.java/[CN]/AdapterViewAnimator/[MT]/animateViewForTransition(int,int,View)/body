{
  PropertyAnimator pa;
  if (fromIndex == -1) {
    pa=new PropertyAnimator(400,view,"alpha",0.0f,1.0f);
    pa.start();
  }
 else   if (toIndex == -1) {
    pa=new PropertyAnimator(400,view,"alpha",1.0f,0.0f);
    pa.start();
  }
}
