{
  ObjectAnimator pa;
  if (fromIndex == -1) {
    view.setAlpha(0.0f);
    pa=new ObjectAnimator(400,view,"alpha",0.0f,1.0f);
    pa.start();
  }
 else   if (toIndex == -1) {
    pa=new ObjectAnimator(400,view,"alpha",1.0f,0.0f);
    pa.start();
  }
}
