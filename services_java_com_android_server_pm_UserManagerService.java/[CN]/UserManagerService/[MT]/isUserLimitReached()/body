{
synchronized (mInstallLock) {
    int nUsers=mUsers.size();
    int userLimit=mContext.getResources().getInteger(com.android.internal.R.integer.config_multiuserMaximumUsers);
    return nUsers >= userLimit;
  }
}
