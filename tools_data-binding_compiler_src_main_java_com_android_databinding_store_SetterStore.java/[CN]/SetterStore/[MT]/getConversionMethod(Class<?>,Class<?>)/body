{
  if (from != null && to != null) {
    for (    ConversionMethod conversion : mConversionMethods) {
      if (canUseForConversion(from,conversion.fromType) && canUseForConversion(conversion.toType,to)) {
        return conversion;
      }
    }
  }
  return null;
}
