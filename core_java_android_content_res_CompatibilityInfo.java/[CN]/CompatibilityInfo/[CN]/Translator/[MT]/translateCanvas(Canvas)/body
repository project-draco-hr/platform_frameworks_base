{
  if (mShiftMode == X_SHIFT_CONTENT || mShiftMode == X_SHIFT_AND_CLIP_CONTENT) {
    canvas.translate(mXOffset,0);
  }
  if (scalingRequired) {
    canvas.scale(applicationScale,applicationScale);
  }
}
