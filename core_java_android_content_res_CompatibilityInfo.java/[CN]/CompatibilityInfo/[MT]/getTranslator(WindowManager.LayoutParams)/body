{
  if ((mCompatibilityFlags & SCALING_EXPANDABLE_MASK) == (EXPANDABLE | LARGE_SCREENS)) {
    if (DBG)     Log.d(TAG,"no translation required");
    return null;
  }
  if (!isScalingRequired()) {
    return null;
  }
  return new Translator();
}
