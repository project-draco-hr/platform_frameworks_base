{
  int numInts;
  int response[];
  numInts=p.readInt();
  response=new int[numInts];
  response[0]=numInts;
  for (int i=1; i < numInts; i++) {
    response[i]=p.readInt();
  }
  return response;
}
