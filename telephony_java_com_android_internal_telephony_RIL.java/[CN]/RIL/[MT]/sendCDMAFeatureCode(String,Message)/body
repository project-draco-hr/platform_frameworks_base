{
  RILRequest rr=RILRequest.obtain(RIL_REQUEST_CDMA_FLASH,response);
  rr.mp.writeInt(1);
  rr.mp.writeString(FeatureCode);
  if (RILJ_LOGD)   riljLog(rr.serialString() + "> " + requestToString(rr.mRequest)+ " : "+ FeatureCode);
  send(rr);
}
