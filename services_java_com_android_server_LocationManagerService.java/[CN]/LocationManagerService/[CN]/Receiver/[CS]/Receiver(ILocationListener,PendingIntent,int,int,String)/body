{
  mListener=listener;
  mPendingIntent=intent;
  if (listener != null) {
    mKey=listener.asBinder();
  }
 else {
    mKey=intent;
  }
  mPermission=checkPermission();
  mUid=uid;
  mPid=pid;
  mPackageName=packageName;
}
