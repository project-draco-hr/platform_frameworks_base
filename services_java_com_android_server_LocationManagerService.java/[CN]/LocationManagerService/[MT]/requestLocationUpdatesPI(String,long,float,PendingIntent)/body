{
  try {
    _requestLocationUpdates(provider,minTime,minDistance,new Receiver(intent));
  }
 catch (  SecurityException se) {
    throw se;
  }
catch (  Exception e) {
    Log.e(TAG,"requestUpdates got exception:",e);
  }
}
