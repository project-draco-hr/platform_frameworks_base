{
synchronized (mLocationListeners) {
    if (mNetworkLocationProvider != null) {
      return mNetworkLocationProvider.getFromLocation(latitude,longitude,maxResults,language,country,variant,appName,addrs);
    }
 else {
      return null;
    }
  }
}
