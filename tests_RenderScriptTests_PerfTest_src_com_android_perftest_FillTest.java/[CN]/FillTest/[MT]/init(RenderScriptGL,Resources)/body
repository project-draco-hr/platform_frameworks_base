{
  mRS=rs;
  mRes=res;
  initCustomShaders();
  initFillScript();
  mTests=new ScriptField_TestScripts_s.Item[mNames.length];
  int index=0;
  addTest(index++,1,0,10);
  addTest(index++,0,0,10);
  addTest(index++,1,1,10);
  addTest(index++,0,1,10);
  return true;
}
