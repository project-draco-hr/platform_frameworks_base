{
  MetaDataService service=(MetaDataService)((WeakReference)s_ref).get();
  if (service == null) {
    return;
  }
switch (type) {
case TYPEID_BEAT:
    service.processBeat(item_len,buffer);
  break;
default :
Log.w(TAG,"unknown type metadata type " + type);
break;
}
}
