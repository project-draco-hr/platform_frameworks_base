{
  ArrayList<Task> tasksToLoad=new ArrayList<Task>();
  getTaskStack(mSystemServicesProxy,context.getResources(),-1,-1,numTasksToPreload,true,true,null,tasksToLoad);
  mLoadQueue.addTasks(tasksToLoad);
  mLoader.start(context);
}
