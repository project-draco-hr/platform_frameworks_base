{
  ArrayList<Task.TaskKey> taskKeys=new ArrayList<Task.TaskKey>();
  ArrayList<Task> tasksToLoad=new ArrayList<Task>();
  TaskStack stack=getTaskStack(mSystemServicesProxy,context.getResources(),-1,preloadCount,true,isTopTaskHome,taskKeys,tasksToLoad);
  SpaceNode root=new SpaceNode();
  root.setStack(stack);
  mLoader.start(context);
  mLoadQueue.addTasks(tasksToLoad);
  mPackageMonitor.setTasks(taskKeys);
  return root;
}
