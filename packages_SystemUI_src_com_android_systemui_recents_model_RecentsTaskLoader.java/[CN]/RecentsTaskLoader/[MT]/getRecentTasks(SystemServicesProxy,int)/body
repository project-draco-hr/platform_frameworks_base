{
  if (numTasks < 0) {
    RecentsConfiguration config=RecentsConfiguration.getInstance();
    numTasks=config.maxNumTasksToLoad;
  }
  List<ActivityManager.RecentTaskInfo> tasks=ssp.getRecentTasks(numTasks,UserHandle.CURRENT.getIdentifier());
  Collections.reverse(tasks);
  return tasks;
}
