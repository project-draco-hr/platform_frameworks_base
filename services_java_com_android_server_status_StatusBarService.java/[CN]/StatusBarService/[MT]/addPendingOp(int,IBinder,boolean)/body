{
synchronized (mQueue) {
    PendingOp op=new PendingOp();
    op.key=key;
    op.code=code;
    op.visible=visible;
    mQueue.add(op);
    if (mQueue.size() == 1) {
      mHandler.sendEmptyMessage(1);
    }
  }
}
