{
  Markup m1=new Markup("atype");
  m1.addNestedMarkup(new Markup("nested_type"));
  String str=m1.toString();
  assertEquals("type: \"atype\" " + "markup { type: \"nested_type\" }",str);
  Markup m2=Markup.markupFromString(str);
  assertEquals(m1,m2);
}
