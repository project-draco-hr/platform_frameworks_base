{
  if (orientation.equals(mGradientDrawable.getOrientation()))   return;
  if (DEBUG)   Log.d(mTag,"setOrientation " + orientation);
  mGradientDrawable.mutate();
  mGradientDrawable.setOrientation(orientation);
}
