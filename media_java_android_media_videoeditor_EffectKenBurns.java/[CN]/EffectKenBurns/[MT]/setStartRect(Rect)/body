{
  if ((startRect.left == 0) && (startRect.right == 0) && (startRect.bottom == 0)&& (startRect.top == 0)) {
    throw new IllegalArgumentException("Invalid Rectangle");
  }
  mStartRect=startRect;
}
