{
  if (obj instanceof ContactMethod) {
    return false;
  }
  ContactMethod contactMethod=(ContactMethod)obj;
  return (kind == contactMethod.kind && type == contactMethod.type && data.equals(contactMethod.data) && label.equals(contactMethod.label) && isPrimary == contactMethod.isPrimary);
}
