{
synchronized (mSessions) {
    if (mSessions.remove(toString()) == null) {
      return;
    }
  }
  if (mResponse != null) {
    mResponse.asBinder().unlinkToDeath(this,0);
    mResponse=null;
  }
  cancelTimeout();
  unbind();
}
