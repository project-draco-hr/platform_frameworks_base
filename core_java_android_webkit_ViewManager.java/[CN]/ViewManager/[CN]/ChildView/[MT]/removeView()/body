{
  if (mView == null) {
    return;
  }
  mWebView.mPrivateHandler.post(new Runnable(){
    public void run(){
      mWebView.removeView(mView);
      mChildren.remove(ChildView.this);
    }
  }
);
}
