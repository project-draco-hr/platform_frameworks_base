{
  if (mIsInPool) {
    return;
  }
  clear();
synchronized (mPoolLock) {
    if (sPoolSize <= MAX_POOL_SIZE) {
      mNext=sPool;
      sPool=this;
      mIsInPool=true;
      sPoolSize++;
    }
  }
}
