{
  enforceSealed();
  if (mSourceWindowId == View.NO_ID || mSourceViewId == View.NO_ID || mConnection == null) {
    return null;
  }
  try {
    return mConnection.findAccessibilityNodeInfoByAccessibilityId(mSourceWindowId,mSourceViewId);
  }
 catch (  RemoteException e) {
  }
  return null;
}
