{
synchronized (mLock) {
    try {
      action.run(mService);
      return true;
    }
 catch (    Exception ex) {
      Log.e(TAG,action.getName() + " failed",ex);
      disconnect();
      return false;
    }
  }
}
