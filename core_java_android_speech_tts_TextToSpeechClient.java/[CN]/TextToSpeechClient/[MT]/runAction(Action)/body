{
synchronized (mLock) {
    if (mServiceConnection == null) {
      return false;
    }
    if (!mServiceConnection.isEstablished()) {
      return false;
    }
    mServiceConnection.runAction(action);
    return true;
  }
}
