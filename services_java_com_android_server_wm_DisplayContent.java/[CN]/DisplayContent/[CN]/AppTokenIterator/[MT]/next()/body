{
  if (hasNext()) {
    AppWindowToken wtoken=mTaskList.mAppTokens.get(mCur);
    mCur+=mReverse ? -1 : 1;
    if (!hasNext()) {
      getNextTaskList();
    }
    return wtoken;
  }
  throw new NoSuchElementException();
}
