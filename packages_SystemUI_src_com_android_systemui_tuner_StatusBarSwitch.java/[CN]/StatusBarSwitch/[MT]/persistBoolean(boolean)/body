{
  if (!value) {
    if (!mBlacklist.contains(getKey())) {
      mBlacklist.add(getKey());
      setList(mBlacklist);
    }
  }
 else {
    if (mBlacklist.remove(getKey())) {
      setList(mBlacklist);
    }
  }
  return true;
}
