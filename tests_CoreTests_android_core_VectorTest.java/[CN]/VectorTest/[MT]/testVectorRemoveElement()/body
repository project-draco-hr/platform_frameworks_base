{
  Vector<Integer> vector=new Vector(mVector);
  for (int i=10; i > 0; i--) {
    vector.removeElement(i);
    vector.removeElement(i);
    vector.removeElement(i);
    vector.removeElement(i);
    vector.removeElement(i);
    vector.removeElement(i);
    vector.removeElement(i);
    vector.removeElement(i);
    vector.removeElement(i);
    vector.removeElement(i);
  }
}
