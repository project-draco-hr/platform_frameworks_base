{
  if ("+".equals(mOp)) {
    if (String.class.equals(getLeft().getResolvedType()) || String.class.equals(getRight().getResolvedType())) {
      return String.class;
    }
  }
  return classAnalyzer.findCommonParentOf(getLeft().getResolvedType(),getRight().getResolvedType());
}
