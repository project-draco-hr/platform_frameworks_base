{
  if ("+".equals(mOp)) {
    if (getLeft().getResolvedType().isString() || getRight().getResolvedType().isString()) {
      return reflectionAnalyzer.findClass(String.class);
    }
  }
  return reflectionAnalyzer.findCommonParentOf(getLeft().getResolvedType(),getRight().getResolvedType());
}
