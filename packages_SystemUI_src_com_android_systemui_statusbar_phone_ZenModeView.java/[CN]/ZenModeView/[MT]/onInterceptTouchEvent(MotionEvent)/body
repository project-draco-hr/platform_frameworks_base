{
  final boolean rt=super.onInterceptTouchEvent(ev);
  if (DEBUG)   logTouchEvent("onInterceptTouchEvent",rt,ev);
  if (ev.getAction() == MotionEvent.ACTION_DOWN && ev.getY() > mCloseButton.getBottom() && mPeekable) {
    return true;
  }
  return rt;
}
