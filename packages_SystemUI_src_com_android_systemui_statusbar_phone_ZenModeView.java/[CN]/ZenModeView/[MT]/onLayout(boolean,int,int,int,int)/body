{
  mLayoutRect.set(left,top,right,bottom);
  if (DEBUG)   log("onLayout %s %s %dx%d",changed,mLayoutRect.toShortString(),mLayoutRect.width(),mLayoutRect.height());
  super.onLayout(changed,left,top,right,bottom);
}
