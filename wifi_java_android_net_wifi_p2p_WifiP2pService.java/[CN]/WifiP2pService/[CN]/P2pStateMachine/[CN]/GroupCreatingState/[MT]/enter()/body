{
  if (DBG)   logd(getName());
  sendMessageDelayed(obtainMessage(GROUP_CREATING_TIMED_OUT,++mGroupCreatingTimeoutIndex,0),GROUP_CREATING_WAIT_TIME_MS);
  if (!mAutonomousGroup) {
    mWifiNative.setP2pGroupIdle(GROUP_IDLE_TIME_S);
  }
}
