{
  if (DBG)   Slog.d(TAG,getName() + message.toString());
switch (message.what) {
case WifiMonitor.SUP_CONNECTION_EVENT:
    mReplyChannel.replyToMessage(mSavedEnableRequestMessage,WifiP2pManager.ENABLE_P2P_SUCCEEDED);
  transitionTo(mInactiveState);
break;
default :
return NOT_HANDLED;
}
return HANDLED;
}
