{
  if (!isEmptyFind) {
    mNumberOfMatches=matchCount;
    mActiveMatchIndex=matchIndex;
    updateMatchesString();
  }
 else {
    mMatches.setVisibility(View.INVISIBLE);
    mNumberOfMatches=0;
  }
}
