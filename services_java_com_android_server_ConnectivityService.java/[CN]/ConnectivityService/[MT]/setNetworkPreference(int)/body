{
  enforceChangePermission();
  if (ConnectivityManager.isNetworkTypeValid(preference)) {
    int oldPreference=mNetworkPreference;
    persistNetworkPreference(preference);
    if (mNetworkPreference != oldPreference)     enforcePreference();
  }
}
