{
  float currentRadius=startValue.mRadius + ((endValue.mRadius - startValue.mRadius) * fraction);
  if (mRevealCircle == null) {
    return new RevealCircle(currentRadius);
  }
 else {
    mRevealCircle.setRadius(currentRadius);
    return mRevealCircle;
  }
}
