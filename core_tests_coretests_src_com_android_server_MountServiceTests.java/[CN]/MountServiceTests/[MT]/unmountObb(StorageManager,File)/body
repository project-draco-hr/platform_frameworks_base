{
  ObbObserver observer=new ObbObserver();
  assertTrue("unmountObb call on test1.obb should succeed",sm.unmountObb(outFile.getPath(),false,observer));
  assertTrue("Unmount should have completed",waitForCompletion(observer));
}
