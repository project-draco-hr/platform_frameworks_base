{
  mIntent=intent;
  mResponse=response;
  mUserHandle=userHandle;
  if (mResponse != null) {
    try {
      mResponse.linkToDeath(this,0);
    }
 catch (    RemoteException re) {
      close();
    }
  }
}
