{
  int ret=0;
  nextTok();
  for (int i=tokStart; i < tokEnd; i++) {
    char c=line.charAt(i);
    if (c < '0' || c > '9') {
      throw new ATParseEx();
    }
    ret*=10;
    ret+=c - '0';
  }
  return ret;
}
