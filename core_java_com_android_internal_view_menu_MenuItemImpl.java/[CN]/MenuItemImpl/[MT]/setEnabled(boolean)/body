{
  if (enabled) {
    mFlags|=ENABLED;
  }
 else {
    mFlags&=~ENABLED;
  }
  for (int i=MenuBuilder.NUM_TYPES - 1; i >= 0; i--) {
    if (hasItemView(i)) {
      mItemViews[i].get().setEnabled(enabled);
    }
  }
  return this;
}
