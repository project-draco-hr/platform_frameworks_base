{
  if (windowToken != null) {
    return windowGainedFocus(startInputReason,client,windowToken,controlFlags,softInputMode,windowFlags,attribute,inputContext);
  }
 else {
    return startInput(startInputReason,client,inputContext,attribute,controlFlags);
  }
}
