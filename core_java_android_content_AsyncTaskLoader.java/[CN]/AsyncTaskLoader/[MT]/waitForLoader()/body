{
  LoadTask task=mTask;
  if (task != null) {
    try {
      task.get();
    }
 catch (    InterruptedException e) {
      Log.w(TAG,e);
    }
catch (    ExecutionException e) {
      throw new RuntimeException("An error occured while executing waitForLoader()",e.getCause());
    }
  }
}
