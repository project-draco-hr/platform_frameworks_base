{
  if (mTranslateAnim != null) {
    mTranslateAnim.cancel();
  }
  mTranslateAnim=(AnimatorSet)getTranslateViewAnimator(leftTarget,null);
  if (mTranslateAnim != null) {
    mTranslateAnim.start();
  }
}
