{
  if (otherObj == null) {
    return false;
  }
  try {
    return mTarget.asBinder().equals(((PendingIntent)otherObj).mTarget.asBinder());
  }
 catch (  ClassCastException e) {
  }
  return false;
}
