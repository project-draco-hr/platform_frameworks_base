{
  if (oval == null) {
    throw new NullPointerException("need oval parameter");
  }
  Ellipse2D ovalShape=new Ellipse2D.Float(oval.left,oval.top,oval.width(),oval.height());
  mPath.append(ovalShape,false);
}
