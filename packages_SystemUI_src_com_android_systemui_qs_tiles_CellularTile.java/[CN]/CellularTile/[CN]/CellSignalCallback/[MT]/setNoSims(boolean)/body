{
  mInfo.noSim=show;
  if (mInfo.noSim) {
    mInfo.mobileSignalIconId=0;
    mInfo.dataTypeIconId=0;
    mInfo.enabled=true;
    mInfo.enabledDesc=mContext.getString(R.string.keyguard_missing_sim_message_short);
    mInfo.signalContentDescription=mInfo.enabledDesc;
  }
  refreshState(mInfo);
}
