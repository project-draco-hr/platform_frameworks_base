{
  if ((preset < MediaRecorder.AudioSource.DEFAULT) || (preset > MediaRecorder.getAudioSourceMax())) {
    throw new IllegalArgumentException("Invalid audio source " + preset);
  }
  mAttributes=new AudioAttributes.Builder().setInternalCapturePreset(preset).build();
  return this;
}
