{
  if (!isOpen()) {
    return;
  }
  lock();
  try {
    closeClosable();
    closePendingStatements();
    dbclose();
    if (mConnectionPool != null) {
      mConnectionPool.close();
    }
  }
  finally {
    unlock();
  }
}
