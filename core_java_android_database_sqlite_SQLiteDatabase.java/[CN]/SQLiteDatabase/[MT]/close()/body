{
  if (!isOpen()) {
    return;
  }
  lock();
  try {
    closeClosable();
    closePendingStatements();
    onAllReferencesReleased();
    if (mConnectionPool != null) {
      mConnectionPool.close();
    }
  }
  finally {
    unlock();
  }
}
