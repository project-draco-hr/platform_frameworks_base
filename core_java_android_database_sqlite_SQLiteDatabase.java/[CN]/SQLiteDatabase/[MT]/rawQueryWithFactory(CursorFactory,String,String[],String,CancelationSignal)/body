{
  throwIfNotOpen();
  SQLiteCursorDriver driver=new SQLiteDirectCursorDriver(this,sql,editTable,cancelationSignal);
  return driver.query(cursorFactory != null ? cursorFactory : mCursorFactory,selectionArgs);
}
