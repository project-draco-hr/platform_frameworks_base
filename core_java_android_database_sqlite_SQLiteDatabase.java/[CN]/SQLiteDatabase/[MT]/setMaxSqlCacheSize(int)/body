{
synchronized (mCompiledQueries) {
    if (mMaxSqlCacheSize > 0) {
      resetCompiledSqlCache();
    }
    mMaxSqlCacheSize=(cacheSize > MAX_SQL_CACHE_SIZE) ? MAX_SQL_CACHE_SIZE : (cacheSize < 0) ? 0 : cacheSize;
  }
}
