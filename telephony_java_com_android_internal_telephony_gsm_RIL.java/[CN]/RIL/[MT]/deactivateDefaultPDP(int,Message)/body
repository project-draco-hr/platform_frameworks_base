{
  RILRequest rr=RILRequest.obtain(RIL_REQUEST_DEACTIVATE_DEFAULT_PDP,result);
  rr.mp.writeInt(1);
  rr.mp.writeString(Integer.toString(cid));
  if (RILJ_LOG)   riljLog(rr.serialString() + "> " + requestToString(rr.mRequest)+ " "+ cid);
  send(rr);
}
