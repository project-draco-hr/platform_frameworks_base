{
  StringBuilder sb=new StringBuilder(8);
  String sn;
  sn=Integer.toString(mSerial);
  sb.append('[');
  for (int i=0, s=sn.length(); i < 4 - s; i++) {
    sb.append('0');
  }
  sb.append(sn);
  sb.append(']');
  return sb.toString();
}
