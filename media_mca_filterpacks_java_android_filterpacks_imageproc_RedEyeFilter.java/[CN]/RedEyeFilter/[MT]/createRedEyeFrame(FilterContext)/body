{
  FrameFormat format=ImageFormat.create(mRedEyeBitmap.getWidth(),mRedEyeBitmap.getHeight(),ImageFormat.COLORSPACE_RGBA,FrameFormat.TARGET_GPU);
  mRedEyeFrame=context.getFrameManager().newFrame(format);
  mRedEyeFrame.setBitmap(mRedEyeBitmap);
}
