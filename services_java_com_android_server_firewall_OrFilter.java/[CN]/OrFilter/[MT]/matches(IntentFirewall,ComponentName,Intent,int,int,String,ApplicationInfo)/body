{
  for (int i=0; i < children.size(); i++) {
    if (children.get(i).matches(ifw,resolvedComponent,intent,callerUid,callerPid,resolvedType,resolvedApp)) {
      return true;
    }
  }
  return false;
}
