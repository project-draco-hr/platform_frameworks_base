{
  mTitle=title;
  setContentDescription(title);
  if (mShowTextAllCaps && title != null) {
    mTextButton.setText(title.toString().toUpperCase(getContext().getResources().getConfiguration().locale));
  }
 else {
    mTextButton.setText(mTitle);
  }
  updateTextButtonVisibility();
}
