{
  if (v == mCancelButton) {
    return;
  }
  if (v == mOkButton) {
    verifyPasswordAndUnlock();
  }
  final int digit=checkDigit(v);
  if (digit >= 0) {
    mCallback.pokeWakelock(DIGIT_PRESS_WAKE_MILLIS);
    reportDigit(digit);
  }
}
