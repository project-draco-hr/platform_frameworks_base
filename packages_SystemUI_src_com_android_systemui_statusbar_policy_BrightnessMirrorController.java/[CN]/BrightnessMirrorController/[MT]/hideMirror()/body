{
  inAnimation(mScrimBehind.animate());
  inAnimation(mPanelHolder.animate()).withLayer().withEndAction(new Runnable(){
    @Override public void run(){
      mBrightnessMirror.setVisibility(View.GONE);
    }
  }
);
}
