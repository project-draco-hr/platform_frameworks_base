{
  int len=nativeGetContentLength();
  if (JNI_DRM_FAILURE == len)   throw new IOException();
  if (JNI_DRM_UNKNOWN_DATA_LEN == len)   return 0;
  int availableLen=len - offset;
  if (availableLen < 0)   throw new IOException();
  return availableLen;
}
