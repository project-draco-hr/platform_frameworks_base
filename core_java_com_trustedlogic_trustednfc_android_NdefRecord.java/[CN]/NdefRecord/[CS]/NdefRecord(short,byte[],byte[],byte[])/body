{
  mFlags=FLAG_MB | FLAG_ME;
  if (data.length < 0xFF) {
    mFlags|=FLAG_SR;
  }
  if (id.length != 0) {
    mFlags|=FLAG_IL;
  }
  mTnf=tnf;
  mType=(byte[])type.clone();
  mId=(byte[])id.clone();
  mPayload=(byte[])data.clone();
}
