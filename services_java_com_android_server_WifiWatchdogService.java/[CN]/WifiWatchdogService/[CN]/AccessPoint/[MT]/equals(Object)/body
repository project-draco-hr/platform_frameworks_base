{
  if (!(o instanceof AccessPoint))   return false;
  AccessPoint otherAp=(AccessPoint)o;
  boolean iHaveNull=hasNull();
  return (iHaveNull && otherAp.hasNull()) || (otherAp.bssid != null && ssid.equals(otherAp.ssid) && bssid.equals(otherAp.bssid));
}
