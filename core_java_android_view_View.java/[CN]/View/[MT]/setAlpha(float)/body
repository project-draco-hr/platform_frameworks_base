{
  mAlpha=alpha;
  if (onSetAlpha((int)(alpha * 255))) {
    mPrivateFlags|=ALPHA_SET;
    invalidate();
  }
 else {
    mPrivateFlags&=~ALPHA_SET;
    invalidate(false);
  }
  invalidateParentIfAccelerated();
}
