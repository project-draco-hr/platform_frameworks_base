{
  if (!gainFocus) {
    if (isPressed()) {
      setPressed(false);
    }
  }
  invalidate();
  if (mOnFocusChangeListener != null) {
    mOnFocusChangeListener.onFocusChange(this,gainFocus);
  }
}
