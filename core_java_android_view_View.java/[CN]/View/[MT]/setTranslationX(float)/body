{
  if (mTranslationX != translationX) {
    invalidate(false);
    mTranslationX=translationX;
    mMatrixDirty=true;
    mPrivateFlags|=DRAWN;
    invalidate(false);
    invalidateParentIfAccelerated();
  }
}
