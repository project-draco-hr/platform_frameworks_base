{
  if (mTranslationX != translationX) {
    invalidateParentCaches();
    invalidate(false);
    mTranslationX=translationX;
    mMatrixDirty=true;
    mPrivateFlags|=DRAWN;
    invalidate(false);
  }
}
