{
  mPrivateFlags|=PIVOT_EXPLICITLY_SET;
  if (mPivotX != pivotX) {
    invalidateParentCaches();
    invalidate(false);
    mPivotX=pivotX;
    mMatrixDirty=true;
    mPrivateFlags|=DRAWN;
    invalidate(false);
  }
}
