{
  mPrivateFlags|=PIVOT_EXPLICITLY_SET;
  if (mPivotX != pivotX) {
    invalidate(false);
    mPivotX=pivotX;
    mMatrixDirty=true;
    mPrivateFlags|=DRAWN;
    invalidate(false);
    invalidateParentIfAccelerated();
  }
}
