{
  mPrivateFlags|=PIVOT_EXPLICITLY_SET;
  if (mPivotY != pivotY) {
    invalidateParentCaches();
    invalidate(false);
    mPivotY=pivotY;
    mMatrixDirty=true;
    mPrivateFlags|=DRAWN;
    invalidate(false);
  }
}
