{
  mPrivateFlags|=PIVOT_EXPLICITLY_SET;
  if (mPivotY != pivotY) {
    invalidate(false);
    mPivotY=pivotY;
    mMatrixDirty=true;
    mPrivateFlags|=DRAWN;
    invalidate(false);
    invalidateParentIfAccelerated();
  }
}
