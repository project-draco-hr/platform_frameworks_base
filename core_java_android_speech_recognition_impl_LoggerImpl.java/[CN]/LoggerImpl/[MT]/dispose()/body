{
synchronized (LoggerImpl.class) {
    if (nativeObject != 0) {
      deleteNativeObject(nativeObject);
      System.getInstance().unregister(this);
    }
    nativeObject=0;
    instance=null;
  }
}
