{
  Bitmap b=loadBitmap(name,res);
  if (b == null) {
    return null;
  }
  return Allocation.createFromBitmap(rs,b,Allocation.MipmapControl.MIPMAP_ON_SYNC_TO_TEXTURE,Allocation.USAGE_GRAPHICS_TEXTURE);
}
