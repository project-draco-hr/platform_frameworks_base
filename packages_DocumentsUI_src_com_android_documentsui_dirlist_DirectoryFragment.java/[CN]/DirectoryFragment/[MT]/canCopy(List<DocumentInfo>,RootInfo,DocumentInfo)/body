{
  if (dest == null || !dest.isDirectory() || !dest.isCreateSupported()) {
    return false;
  }
  if (root.isDownloads()) {
    for (    DocumentInfo docs : files) {
      if (docs.isDirectory()) {
        return false;
      }
    }
  }
  return true;
}
