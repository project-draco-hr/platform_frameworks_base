{
  assertRunOnServiceThread();
  if (mService.isControlEnabled() && !isInPresetInstallationMode() && mService.isPowerOnOrTransient()) {
    mService.standby();
    return true;
  }
  return false;
}
