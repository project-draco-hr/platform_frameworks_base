{
  if (!mService.invokeVendorCommandListeners(mDeviceType,message.getSource(),message.getParams(),false)) {
    mService.maySendFeatureAbortCommand(message,Constants.ABORT_NOT_IN_CORRECT_MODE);
  }
  return true;
}
