{
  checkWritePermission(userId);
  mStorage.removeUser(userId);
  notifyObservers(null,userId);
  final KeyStore ks=KeyStore.getInstance();
  final int userUid=UserHandle.getUid(userId,Process.SYSTEM_UID);
  ks.resetUid(userUid);
}
