{
  if (installOnSd(flags)) {
    return new SdInstallArgs(packageURI,observer,flags,installerPackageName);
  }
 else {
    return new FileInstallArgs(packageURI,observer,flags,installerPackageName);
  }
}
