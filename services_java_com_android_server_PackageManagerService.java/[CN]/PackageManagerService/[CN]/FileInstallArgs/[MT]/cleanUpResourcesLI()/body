{
  String sourceDir=getCodePath();
  if (cleanUp() && mInstaller != null) {
    int retCode=mInstaller.rmdex(sourceDir);
    if (retCode < 0) {
      Log.w(TAG,"Couldn't remove dex file for package: " + " at location " + sourceDir + ", retcode="+ retCode);
    }
  }
}
