{
  try {
    cachePath=imcs.copyResourceToContainer(packageURI,cid,getEncryptKey(),RES_FILE_NAME);
  }
 catch (  RemoteException e) {
  }
  return (cachePath == null) ? PackageManager.INSTALL_FAILED_CONTAINER_ERROR : PackageManager.INSTALL_SUCCEEDED;
}
