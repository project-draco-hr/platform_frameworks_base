{
  mTaskList.setFilter(new TaskFilter(){
    @Override public boolean acceptTask(    Task t,    int i){
      if (i % 2 == 0) {
        return true;
      }
      return false;
    }
  }
);
  if (mCb != null) {
    mCb.onStackFiltered(this);
  }
}
