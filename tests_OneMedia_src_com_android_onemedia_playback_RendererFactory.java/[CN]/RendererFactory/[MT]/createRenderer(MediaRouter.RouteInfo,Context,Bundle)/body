{
  if (route.getPlaybackType() == MediaRouter.RouteInfo.PLAYBACK_TYPE_LOCAL) {
    return new LocalRenderer(context,params);
  }
  Log.e(TAG,"Unable to create renderer for route of playback type " + route.getPlaybackType());
  return null;
}
