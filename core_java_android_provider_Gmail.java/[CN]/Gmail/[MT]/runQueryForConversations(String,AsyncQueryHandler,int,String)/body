{
  if (TextUtils.isEmpty(account)) {
    throw new IllegalArgumentException("account is empty");
  }
  handler.startQuery(token,null,Uri.withAppendedPath(CONVERSATIONS_URI,account),CONVERSATION_PROJECTION,query,null,null);
}
