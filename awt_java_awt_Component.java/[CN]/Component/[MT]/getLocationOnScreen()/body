{
  toolkit.lockAWT();
  try {
    Point p=new Point();
    if (isShowing()) {
      return p;
    }
    throw new IllegalComponentStateException(Messages.getString("awt.151"));
  }
  finally {
    toolkit.unlockAWT();
  }
}
