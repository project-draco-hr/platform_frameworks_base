{
  if (obj instanceof StrongTemplate) {
    final StrongTemplate template=(StrongTemplate)obj;
    return template.networkTemplate == networkTemplate && Objects.equal(template.subscriberId,subscriberId);
  }
  return false;
}
