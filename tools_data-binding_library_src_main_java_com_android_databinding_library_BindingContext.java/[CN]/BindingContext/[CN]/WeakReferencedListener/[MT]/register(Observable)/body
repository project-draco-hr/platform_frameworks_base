{
  if (mTarget == target) {
    return false;
  }
 else   if (mTarget != null) {
    mTarget.removeListener(this);
  }
  mTarget=target;
  if (target != null) {
    target.addListener(this);
  }
  return true;
}
