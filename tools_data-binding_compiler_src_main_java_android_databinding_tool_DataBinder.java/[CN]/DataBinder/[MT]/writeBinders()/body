{
  for (  LayoutBinder layoutBinder : mLayoutBinders) {
    L.d("writing data binder %s",layoutBinder.getClassName());
    mFileWriter.writeToFile(layoutBinder.getPackage() + "." + layoutBinder.getClassName(),layoutBinder.writeViewBinder());
  }
}
