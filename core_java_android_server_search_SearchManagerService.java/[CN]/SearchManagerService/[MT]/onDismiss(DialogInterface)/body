{
  if (DBG)   debug("onDismiss()");
  if (mCallback != null) {
    try {
      mCallback.onDismiss();
    }
 catch (    RemoteException ex) {
      Log.e(TAG,"onDismiss() failed: " + ex);
    }
  }
}
