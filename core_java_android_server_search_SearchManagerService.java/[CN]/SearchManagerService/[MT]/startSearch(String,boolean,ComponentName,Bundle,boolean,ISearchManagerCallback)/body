{
  if (DBG)   debug("startSearch()");
  Runnable task=new Runnable(){
    public void run(){
      performStartSearch(initialQuery,selectInitialQuery,launchActivity,appSearchData,globalSearch,searchManagerCallback);
    }
  }
;
  mHandler.post(task);
}
