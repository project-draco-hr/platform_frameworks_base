{
  EventLog.writeEvent(2720,syncOperation.authority,Sync.History.EVENT_STOP,syncOperation.syncSource);
  mSyncStorageEngine.stopSyncEvent(rowId,elapsedTime,resultMessage,downstreamActivity,upstreamActivity);
}
