{
  Intent intent=new Intent("android.provider.telephony.SMS_CB_RECEIVED");
  intent.putExtra("pdus",pdus);
  if (false)   Log.d(TAG,"Dispatching " + pdus.length + " SMS CB pdus");
  dispatch(intent,"android.permission.RECEIVE_SMS");
}
