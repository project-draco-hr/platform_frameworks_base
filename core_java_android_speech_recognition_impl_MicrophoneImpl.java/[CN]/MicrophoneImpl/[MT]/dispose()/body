{
synchronized (MicrophoneImpl.class) {
    if (nativeObject != 0) {
      deleteNativeObject(nativeObject);
      nativeObject=0;
      instance=null;
      System.getInstance().unregister(this);
    }
  }
}
