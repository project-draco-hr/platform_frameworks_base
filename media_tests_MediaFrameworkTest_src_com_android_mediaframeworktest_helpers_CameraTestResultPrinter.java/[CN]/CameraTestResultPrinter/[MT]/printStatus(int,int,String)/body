{
  Log.v(TAG,String.format("Print status: numAttempts=%d iteration=%d cameraId=%s",numAttempts,iteration,cameraId));
  sendInstrumentationStatus(numAttempts,iteration,cameraId);
  if (mWriteToFile) {
    writeToFile(numAttempts,iteration,cameraId);
  }
}
