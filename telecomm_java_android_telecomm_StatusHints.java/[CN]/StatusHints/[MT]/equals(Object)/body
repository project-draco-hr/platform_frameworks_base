{
  if (other != null && other instanceof StatusHints) {
    StatusHints otherHints=(StatusHints)other;
    return Objects.equals(otherHints.getComponentName(),getComponentName()) && Objects.equals(otherHints.getLabel(),getLabel()) && otherHints.getIconId() == getIconId() && Objects.equals(otherHints.getExtras(),getExtras());
  }
  return false;
}
