{
  if (stack == null || stack.isFullscreen()) {
    setupSurface(false);
  }
 else {
    stack.getBounds(mBounds);
    setupSurface(true);
    if (!mBounds.equals(mLastBounds)) {
      draw();
    }
  }
}
