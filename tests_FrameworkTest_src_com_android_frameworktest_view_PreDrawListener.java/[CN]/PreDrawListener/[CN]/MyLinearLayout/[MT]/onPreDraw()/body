{
  if (mCancelNextDraw) {
    Button b=new Button(this.getContext());
    b.setText("Hello");
    addView(b,new LinearLayout.LayoutParams(LayoutParams.FILL_PARENT,LayoutParams.WRAP_CONTENT));
    mCancelNextDraw=false;
    return false;
  }
  return true;
}
