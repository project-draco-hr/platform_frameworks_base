{
  if (mUnassociatedKeyboardLayouts.isEmpty()) {
    return (String[])ArrayUtils.emptyArray(String.class);
  }
  return mUnassociatedKeyboardLayouts.toArray(new String[mUnassociatedKeyboardLayouts.size()]);
}
