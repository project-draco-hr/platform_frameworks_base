{
  if (params.result > 0) {
    Toast.makeText(params.context,R.string.screenshot_failed_toast,Toast.LENGTH_SHORT).show();
  }
 else {
    Toast.makeText(params.context,R.string.screenshot_saving_toast,Toast.LENGTH_SHORT).show();
  }
}
