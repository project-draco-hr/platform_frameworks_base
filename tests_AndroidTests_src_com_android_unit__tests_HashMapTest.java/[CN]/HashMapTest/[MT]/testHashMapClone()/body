{
  HashMap cMap;
  for (int i=ITERATIONS - 1; i >= 0; i--) {
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
    cMap=(HashMap)mMap.clone();
  }
}
