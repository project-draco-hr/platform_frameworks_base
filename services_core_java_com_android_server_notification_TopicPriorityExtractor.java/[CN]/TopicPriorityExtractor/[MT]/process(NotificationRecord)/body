{
  if (record == null || record.getNotification() == null) {
    if (DBG)     Slog.d(TAG,"skipping empty notification");
    return null;
  }
  if (mConfig == null) {
    if (DBG)     Slog.d(TAG,"missing config");
    return null;
  }
  final int packagePriority=mConfig.getTopicPriority(record.sbn.getPackageName(),record.sbn.getUid(),record.sbn.getNotification().getTopic());
  record.setPackagePriority(packagePriority);
  return null;
}
