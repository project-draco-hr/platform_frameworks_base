{
  new Thread(new Runnable(){
    public void run(){
      try {
        processCommand(command);
      }
 catch (      SipException e) {
        Log.w(TAG,"command error: " + command,e);
        onError(e);
      }
    }
  }
).start();
}
