{
synchronized (mLock) {
    if (mOriginalValues != null) {
      for (      T item : items) {
        mOriginalValues.add(item);
      }
    }
 else {
      for (      T item : items) {
        mObjects.add(item);
      }
    }
  }
  if (mNotifyOnChange)   notifyDataSetChanged();
}
