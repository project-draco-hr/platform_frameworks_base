{
synchronized (mActiveRequests) {
    Request req=mActiveRequests.get(request.asBinder());
    if (req == null) {
      throw new IllegalStateException("Received callback without active request: " + request);
    }
    return req;
  }
}
