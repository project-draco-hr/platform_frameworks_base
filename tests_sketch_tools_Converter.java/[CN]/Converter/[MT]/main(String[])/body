{
  String fileName=args[0];
  if (fileName != null) {
    File file=new File(fileName);
    if (!file.exists()) {
      printHelp(fileName);
    }
 else {
      new Converter(file).convert();
    }
  }
 else {
    printHelp(null);
  }
}
