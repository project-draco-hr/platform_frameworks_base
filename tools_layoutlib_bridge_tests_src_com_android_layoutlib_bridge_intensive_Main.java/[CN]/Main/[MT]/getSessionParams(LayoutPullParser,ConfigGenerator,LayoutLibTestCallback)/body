{
  FolderConfiguration config=configGenerator.getFolderConfig();
  ResourceResolver resourceResolver=ResourceResolver.create(mProjectResources.getConfiguredResources(config),mFrameworkRepo.getConfiguredResources(config),"AppTheme",true);
  return new SessionParams(layoutParser,RenderingMode.NORMAL,null,configGenerator.getHardwareConfig(),resourceResolver,layoutLibCallback,0,19,getLayoutLog());
}
