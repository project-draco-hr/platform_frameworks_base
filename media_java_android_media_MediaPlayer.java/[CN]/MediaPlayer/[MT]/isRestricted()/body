{
  try {
    final int mode=mAppOps.checkAudioOperation(AppOpsManager.OP_PLAY_AUDIO,getAudioStreamType(),Process.myUid(),ActivityThread.currentPackageName());
    return mode != AppOpsManager.MODE_ALLOWED;
  }
 catch (  RemoteException e) {
    return false;
  }
}
