{
  if (!finishing) {
    if (this == task.stack.getVisibleBehindActivity()) {
      mStackSupervisor.requestVisibleBehindLocked(this,false);
    }
    finishing=true;
    if (stopped) {
      clearOptionsLocked();
    }
  }
}
