{
  if (mIcon != null) {
    return mIcon;
  }
  if (mIconFilename != null) {
    try {
      return ActivityManagerNative.getDefault().getTaskDescriptionIcon(mIconFilename);
    }
 catch (    RemoteException e) {
    }
  }
  return null;
}
