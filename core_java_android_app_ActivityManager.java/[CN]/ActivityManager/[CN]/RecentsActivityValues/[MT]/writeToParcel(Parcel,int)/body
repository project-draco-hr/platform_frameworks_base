{
  TextUtils.writeToParcel(label,dest,Parcelable.PARCELABLE_WRITE_RETURN_VALUE);
  if (icon == null) {
    dest.writeInt(0);
  }
 else {
    dest.writeInt(1);
    icon.writeToParcel(dest,0);
  }
  dest.writeInt(colorPrimary);
}
