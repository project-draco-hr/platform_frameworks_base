{
  if (DBG) {
    Log.d(LOG_TAG,"--- handlePaste");
  }
  if (!mEditFlag) {
    return;
  }
  if (mTextSelectBuffer != null && mTextCopyBufer.length() > 0) {
    mTextSelectBuffer.insert(mEST.getSelectionStart(),mTextCopyBufer);
  }
 else {
    mEST.setHintMessage(HINT_MSG_COPY_BUF_BLANK);
  }
}
