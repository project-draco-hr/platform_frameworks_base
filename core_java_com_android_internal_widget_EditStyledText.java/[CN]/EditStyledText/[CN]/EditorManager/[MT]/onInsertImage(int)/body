{
  if (DBG) {
    Log.d(LOG_TAG,"--- onInsertImage by resID");
  }
  mEST.getText().append("b");
  mEST.getText().setSpan(new ImageSpan(mEST.getContext(),resID),mEST.getText().length() - 1,mEST.getText().length(),Spannable.SPAN_EXCLUSIVE_EXCLUSIVE);
}
