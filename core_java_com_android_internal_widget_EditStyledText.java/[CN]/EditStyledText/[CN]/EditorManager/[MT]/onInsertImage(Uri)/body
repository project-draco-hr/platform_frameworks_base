{
  if (DBG) {
    Log.d(LOG_TAG,"--- onInsertImage by URI: " + uri.getPath() + ","+ uri.toString());
  }
  mEST.getText().append("a");
  mEST.getText().setSpan(new ImageSpan(mEST.getContext(),uri),mEST.getText().length() - 1,mEST.getText().length(),Spannable.SPAN_EXCLUSIVE_EXCLUSIVE);
}
