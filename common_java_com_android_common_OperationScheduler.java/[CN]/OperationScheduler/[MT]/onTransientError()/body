{
  long now=System.currentTimeMillis();
  mStorage.edit().putLong(PREFIX + "lastErrorTimeMillis",now).commit();
  mStorage.edit().putInt(PREFIX + "errorCount",mStorage.getInt(PREFIX + "errorCount",0) + 1).commit();
}
