{
  if (DBG)   Log.d(TAG,"removeDefaultRoute for " + this + "("+ mInterfaceName+ ")");
  if (mInterfaceName != null && mDefaultRouteSet == true) {
    NetworkUtils.removeDefaultRoute(mInterfaceName);
    mDefaultRouteSet=false;
  }
}
