{
  final int length=mTransformed.length();
  for (int i=0; i < length; i++) {
    final char c=mTransformed.charAt(i);
    final int type=Character.getType(c);
    if (type != Character.SPACE_SEPARATOR)     return false;
  }
  return true;
}
