{
  if ((event.getFlags() & KeyEvent.FLAG_SOFT_KEYBOARD) != 0 && mInputContentType != null && (mInputContentType.imeOptions & EditorInfo.IME_FLAG_NO_ENTER_ACTION) != 0) {
    return false;
  }
  return true;
}
