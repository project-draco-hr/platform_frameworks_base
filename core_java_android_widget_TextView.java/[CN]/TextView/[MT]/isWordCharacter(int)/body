{
  final char c=mTransformed.charAt(position);
  final int type=Character.getType(c);
  return (c == '\'' || c == '"' || type == Character.UPPERCASE_LETTER || type == Character.LOWERCASE_LETTER || type == Character.TITLECASE_LETTER || type == Character.MODIFIER_LETTER || type == Character.DECIMAL_DIGIT_NUMBER);
}
