{
  if (mLayoutAlignment != null) {
    int resolvedTextAlignment=getResolvedTextAlignment();
    if (resolvedTextAlignment == TEXT_ALIGNMENT_VIEW_START || resolvedTextAlignment == TEXT_ALIGNMENT_VIEW_END) {
      mLayoutAlignment=null;
    }
  }
}
