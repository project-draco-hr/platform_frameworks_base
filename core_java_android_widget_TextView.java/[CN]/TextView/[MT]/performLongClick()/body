{
  if (canSelectText()) {
    if (startSelectionMode()) {
      mEatTouchRelease=true;
      return true;
    }
  }
  if (super.performLongClick()) {
    mEatTouchRelease=true;
    return true;
  }
  return false;
}
