{
  if (getEditor().mSelectHandleCenter == null) {
    getEditor().mSelectHandleCenter=mContext.getResources().getDrawable(mTextSelectHandleRes);
  }
  if (mHandle == null) {
    mHandle=new InsertionHandleView(getEditor().mSelectHandleCenter);
  }
  return mHandle;
}
