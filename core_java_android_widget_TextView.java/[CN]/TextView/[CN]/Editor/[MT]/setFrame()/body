{
  if (mErrorPopup != null) {
    TextView tv=(TextView)mErrorPopup.getContentView();
    chooseSize(mErrorPopup,mError,tv);
    mErrorPopup.update(TextView.this,getErrorX(),getErrorY(),mErrorPopup.getWidth(),mErrorPopup.getHeight());
  }
}
