{
  if (false)   Log.v(TAG,"<<< Releasing cursor " + this);
  mCursorState=0;
  if (mWindow != null) {
    mWindow.close();
    mWindow=null;
  }
  if (false)   Log.v("DatabaseWindow","closing window in release()");
}
