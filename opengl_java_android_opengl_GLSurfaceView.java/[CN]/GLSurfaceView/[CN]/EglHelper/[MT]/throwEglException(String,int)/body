{
  String message=formatEglError(function,error);
  if (LOG_THREADS) {
    Log.e(TAG,"throwEglException tid=" + Thread.currentThread().getId() + " "+ message);
  }
  throw new RuntimeException(message);
}
