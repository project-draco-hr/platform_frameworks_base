{
  if (mRenderMode == RENDERMODE_CONTINUOUSLY) {
    mChoreographer.postCallback(Choreographer.CALLBACK_ANIMATION,mExecuteDrawAction,null);
  }
 else {
    mGLHandler.post(mExecuteDrawAction);
  }
}
