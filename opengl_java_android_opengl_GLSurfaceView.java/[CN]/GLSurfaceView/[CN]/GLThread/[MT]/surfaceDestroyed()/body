{
synchronized (this) {
    mHasSurface=false;
    notify();
    while (!mWaitingForSurface) {
      try {
        wait();
      }
 catch (      InterruptedException e) {
        Thread.currentThread().interrupt();
      }
    }
  }
}
