{
  if (platformVersion == 0 || platformVersion >= KITKAT || platformVersion > FROYO && platformVersion < HONEYCOMB) {
    return WHITE;
  }
  if (platformVersion < GINGERBREAD) {
    return BLACK;
  }
 else   if (platformVersion < KITKAT) {
    return 0xff33b5e5;
  }
  return WHITE;
}
