{
  ModelField[] fields=getDeclaredFields();
  for (  ModelField field : fields) {
    if (name.equals(stripFieldName(field.getName())) && field.isStatic() == staticAccess && (allowPrivate || !field.isPublic())) {
      return field;
    }
  }
  return null;
}
