{
  Intent intent=getLaunchIntentForPackageCategory(this,packageName,Intent.CATEGORY_INFO);
  if (intent != null) {
    return intent;
  }
  return getLaunchIntentForPackageCategory(this,packageName,Intent.CATEGORY_LAUNCHER);
}
