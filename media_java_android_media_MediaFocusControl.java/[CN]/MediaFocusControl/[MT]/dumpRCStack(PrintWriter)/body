{
  pw.println("\nRemote Control stack entries (last is top of stack):");
synchronized (mMCStack) {
    Iterator<MediaController> stackIterator=mMCStack.iterator();
    while (stackIterator.hasNext()) {
      MediaController mcse=stackIterator.next();
      pw.println("  pi: " + mcse.mMediaIntent + " -- pack: "+ mcse.mCallingPackageName+ "  -- ercvr: "+ mcse.mReceiverComponent+ "  -- client: "+ mcse.mRcClient+ "  -- uid: "+ mcse.mCallingUid+ "  -- type: "+ mcse.mPlaybackType+ "  state: "+ mcse.mPlaybackState);
    }
  }
}
