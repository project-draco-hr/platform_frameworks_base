{
  final int SW_LID=0x00;
  int sw=mInputManager.getSwitchState(-1,InputDevice.SOURCE_ANY,SW_LID);
  if (sw > 0) {
    return LID_OPEN;
  }
 else   if (sw == 0) {
    return LID_CLOSED;
  }
 else {
    return LID_ABSENT;
  }
}
