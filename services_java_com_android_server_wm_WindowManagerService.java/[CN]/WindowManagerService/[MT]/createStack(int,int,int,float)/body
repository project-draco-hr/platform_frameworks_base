{
synchronized (mWindowMap) {
    TaskStack stack=getDefaultDisplayContentLocked().createStack(stackId,relativeStackId,position,weight);
    mStackIdToStack.put(stackId,stack);
    performLayoutAndPlaceSurfacesLocked();
  }
}
