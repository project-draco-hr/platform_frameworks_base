{
synchronized (mWindowMap) {
    if (mMagnificationMediator != null) {
      WindowState window=mWindowMap.get(token);
      if (window != null) {
        mMagnificationMediator.onRectangleOnScreenRequestedLw(window,rectangle,immediate);
      }
    }
  }
}
