{
  final TaskRecord task=r.task;
  if (task.removeActivity(r)) {
    if (DEBUG_ADD_REMOVE)     Slog.i(TAG,"removeActivity: Removing from history, task=" + task);
    mTaskHistory.remove(task);
  }
}
