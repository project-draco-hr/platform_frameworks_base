{
  int i=mHistory.size() - 1;
  while (i >= 0) {
    ActivityRecord r=mHistory.get(i);
    if (!r.finishing && (token != r.appToken) && (taskId != r.task.taskId)&& r.userId == mCurrentUser) {
      return r;
    }
    i--;
  }
  return null;
}
