{
synchronized (mLock) {
    if (mDestroyed) {
      return;
    }
    if (isConnected()) {
      disconnectLocked(MediaSession.DISCONNECT_REASON_SESSION_DESTROYED);
    }
    mRoute=null;
    mRequest=null;
    mDestroyed=true;
  }
}
