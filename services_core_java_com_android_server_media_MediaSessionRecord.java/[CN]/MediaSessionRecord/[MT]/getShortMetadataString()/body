{
  int fields=mMetadata == null ? 0 : mMetadata.size();
  String title=mMetadata == null ? null : mMetadata.getString(MediaMetadata.METADATA_KEY_TITLE);
  return "size=" + fields + ", title="+ title;
}
