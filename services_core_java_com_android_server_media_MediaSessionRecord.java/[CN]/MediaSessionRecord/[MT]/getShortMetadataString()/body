{
  int fields=mMetadata == null ? 0 : mMetadata.size();
  MediaMetadata.Description description=mMetadata == null ? null : mMetadata.getDescription();
  return "size=" + fields + ", description="+ description;
}
