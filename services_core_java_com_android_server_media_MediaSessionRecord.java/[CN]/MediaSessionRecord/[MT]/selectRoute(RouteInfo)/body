{
synchronized (mLock) {
    if (route != mRoute) {
      if (mConnection != null) {
        mConnection.disconnect();
        mConnection=null;
      }
    }
    mRoute=route;
  }
  mSessionCb.sendRouteChange(route);
}
