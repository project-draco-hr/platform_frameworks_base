{
  int id=v.getId();
  if (id == R.id.enable_trust) {
    SampleTrustAgent.sendEnableTrust(this,"SampleTrustAgent",TRUST_DURATION_MS,null);
  }
 else   if (id == R.id.revoke_trust) {
    SampleTrustAgent.sendRevokeTrust(this);
  }
}
