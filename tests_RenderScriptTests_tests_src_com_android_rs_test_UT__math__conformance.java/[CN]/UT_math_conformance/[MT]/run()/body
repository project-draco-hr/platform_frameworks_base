{
  RenderScript pRS=RenderScript.create(mCtx);
  ScriptC_math_conformance s=new ScriptC_math_conformance(pRS,mRes,R.raw.math_conformance);
  pRS.setMessageHandler(mRsMessage);
  s.invoke_math_conformance_test();
  pRS.finish();
  waitForMessage();
  pRS.destroy();
  passTest();
}
