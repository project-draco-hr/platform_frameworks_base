{
  if (mChallengeView == null)   return false;
  return x >= mDragHandleEdgeSlop && y >= mChallengeView.getTop() && x < getWidth() - mDragHandleEdgeSlop && y < mChallengeView.getTop() + mDragHandleSize;
}
