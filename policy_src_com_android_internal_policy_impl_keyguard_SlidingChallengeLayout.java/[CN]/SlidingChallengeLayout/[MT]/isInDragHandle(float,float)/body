{
  if (mChallengeView == null)   return false;
  return x >= mDragHandleEdgeSlop && y >= mChallengeView.getTop() - getDragHandleSizeAbove() && x < getWidth() - mDragHandleEdgeSlop && y < mChallengeView.getTop() + getDragHandleSizeBelow();
}
