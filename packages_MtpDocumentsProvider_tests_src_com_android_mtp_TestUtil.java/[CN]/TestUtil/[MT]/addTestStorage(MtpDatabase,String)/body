{
  database.getMapper().startAddingDocuments(parentId);
  database.getMapper().putStorageDocuments(parentId,OPERATIONS_SUPPORTED,new MtpRoot[]{new MtpRoot(0,100,"Storage",1024,1024,"")});
  database.getMapper().stopAddingDocuments(parentId);
}
