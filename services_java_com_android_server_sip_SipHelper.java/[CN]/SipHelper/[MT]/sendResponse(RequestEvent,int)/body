{
  try {
    getServerTransaction(event).sendResponse(mMessageFactory.createResponse(responseCode,event.getRequest()));
  }
 catch (  ParseException e) {
    throw new SipException("sendResponse()",e);
  }
}
