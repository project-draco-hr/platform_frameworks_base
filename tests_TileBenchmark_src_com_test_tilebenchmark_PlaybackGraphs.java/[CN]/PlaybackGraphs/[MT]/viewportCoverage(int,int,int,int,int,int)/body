{
  if (tileIndexX * PlaybackView.TILEX < r && (tileIndexX + 1) * PlaybackView.TILEX >= l && tileIndexY * PlaybackView.TILEY < t && (tileIndexY + 1) * PlaybackView.TILEY >= b) {
    return 1.0f;
  }
  return 0.0f;
}
