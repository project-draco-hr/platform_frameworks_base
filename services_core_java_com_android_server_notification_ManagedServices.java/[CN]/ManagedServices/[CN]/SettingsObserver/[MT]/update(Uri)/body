{
  if (uri == null || mSecureSettingsUri.equals(uri)) {
    if (DEBUG)     Slog.d(TAG,"Setting changed: mSecureSettingsUri=" + mSecureSettingsUri + " / uri="+ uri);
    rebindServices();
    rebuildRestoredPackages();
  }
}
