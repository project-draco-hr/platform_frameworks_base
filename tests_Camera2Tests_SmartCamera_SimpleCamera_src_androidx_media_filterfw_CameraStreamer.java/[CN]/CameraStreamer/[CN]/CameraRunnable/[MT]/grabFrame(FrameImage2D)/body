{
synchronized (mState) {
    if (mState.current() != State.STATE_RUNNING) {
      return false;
    }
    if (mCamera == null) {
      return false;
    }
    mCamFrameHandler.grabFrame(targetFrame);
    return true;
  }
}
