{
  if (event == null) {
    throw new IllegalArgumentException("event must not be null");
  }
synchronized (mInputFilterLock) {
    if (!mDisconnected) {
      nativeInjectInputEvent(event,0,0,INPUT_EVENT_INJECTION_SYNC_NONE,0,policyFlags | WindowManagerPolicy.FLAG_FILTERED);
    }
  }
}
