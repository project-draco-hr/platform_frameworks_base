{
  Log.d(TAG,"Clicked item " + position);
  int gadgetId=mGadgetManager.allocateGadgetId(getCallingPackage());
  mGadgetManager.bindGadgetId(gadgetId,new ComponentName("com.android.gadgethost","com.android.gadgethost.TestGadgetProvider"));
  Intent result=new Intent();
  result.putExtra(GadgetManager.EXTRA_GADGET_ID,gadgetId);
  setResult(RESULT_OK,result);
  finish();
}
