{
  if (mService != null) {
    try {
      mService.registerPrivateKeyAccessListener(admin,listener);
    }
 catch (    RemoteException e) {
      Log.w(TAG,"Failed to register private key access listener with device policy " + "service",e);
    }
  }
}
