{
  Log.d(TAG,"User input status: " + status);
synchronized (mLock) {
    mUserStatus=status;
    mLock.notifyAll();
  }
  String message=(status == OSUOperationStatus.UserInputAborted) ? "Browser closed" : null;
  return mOSUManager.notifyUser(status,message,mSpName);
}
