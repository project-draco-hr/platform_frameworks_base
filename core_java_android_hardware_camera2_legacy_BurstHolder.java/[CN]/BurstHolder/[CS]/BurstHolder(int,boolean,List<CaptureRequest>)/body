{
  mRequestBuilders=new ArrayList<RequestHolder.Builder>();
  int i=0;
  for (  CaptureRequest r : requests) {
    mRequestBuilders.add(new RequestHolder.Builder(requestId,i,r,repeating));
    ++i;
  }
  mRepeating=repeating;
  mRequestId=requestId;
}
