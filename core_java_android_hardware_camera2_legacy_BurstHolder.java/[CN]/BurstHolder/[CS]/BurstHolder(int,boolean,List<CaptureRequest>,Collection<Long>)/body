{
  mRequestBuilders=new ArrayList<>();
  int i=0;
  for (  CaptureRequest r : requests) {
    mRequestBuilders.add(new RequestHolder.Builder(requestId,i,r,repeating,jpegSurfaceIds));
    ++i;
  }
  mRepeating=repeating;
  mRequestId=requestId;
}
