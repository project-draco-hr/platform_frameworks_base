{
  drawBackgroundCircle(canvas);
  drawArrow(canvas);
  canvas.save();
  updateIconColor();
  canvas.scale(mImageScale,mImageScale,getWidth() / 2,getHeight() / 2);
  super.onDraw(canvas);
  canvas.restore();
}
