{
  String name=getClass().getName();
  if (name.indexOf("$") != -1) {
    return null;
  }
  int number=0;
  name=name.substring(name.lastIndexOf(".") + 1) + Integer.toString(number);
  return name;
}
