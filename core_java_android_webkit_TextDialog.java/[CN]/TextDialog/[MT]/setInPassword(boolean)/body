{
  PasswordTransformationMethod method;
  if (inPassword) {
    method=PasswordTransformationMethod.getInstance();
  }
 else {
    method=null;
  }
  setTransformationMethod(method);
  setInputType(inPassword ? EditorInfo.TYPE_TEXT_VARIATION_PASSWORD : EditorInfo.TYPE_CLASS_TEXT);
}
