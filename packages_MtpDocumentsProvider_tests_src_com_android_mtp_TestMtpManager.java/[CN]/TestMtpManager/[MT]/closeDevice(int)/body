{
  final MtpDeviceRecord device=mDevices.get(deviceId);
  if (device == null || !device.opened) {
    throw new IOException();
  }
  mDevices.put(deviceId,new MtpDeviceRecord(device.deviceId,device.name,device.deviceKey,false,device.roots,null,null));
}
