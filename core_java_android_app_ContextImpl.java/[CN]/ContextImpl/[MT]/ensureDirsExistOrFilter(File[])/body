{
  File[] result=new File[dirs.length];
  for (int i=0; i < dirs.length; i++) {
    File dir=dirs[i];
    if (!dir.exists()) {
      if (!dir.mkdirs()) {
        Log.w(TAG,"Failed to ensure directory: " + dir);
        dir=null;
      }
    }
    result[i]=dir;
  }
  return result;
}
