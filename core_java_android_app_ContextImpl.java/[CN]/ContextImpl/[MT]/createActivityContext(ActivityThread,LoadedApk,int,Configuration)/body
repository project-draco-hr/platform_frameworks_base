{
  if (packageInfo == null)   throw new IllegalArgumentException("packageInfo");
  final Display display=ResourcesManager.getInstance().getAdjustedDisplay(displayId,overrideConfiguration);
  return new ContextImpl(null,mainThread,packageInfo,null,null,false,display,overrideConfiguration);
}
