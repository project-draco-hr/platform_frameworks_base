{
  mContext.enforceCallingPermission("android.permission.BACKUP","getAvailableRestoreSets");
synchronized (this) {
    if (mRestoreSets == null) {
      mRestoreSets=mRestoreTransport.getAvailableRestoreSets();
    }
    return mRestoreSets;
  }
}
