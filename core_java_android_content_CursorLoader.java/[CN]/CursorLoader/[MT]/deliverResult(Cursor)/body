{
  if (mStopped) {
    if (cursor != null) {
      cursor.close();
    }
    return;
  }
  Cursor oldCursor=mCursor;
  mCursor=cursor;
  super.deliverResult(cursor);
  if (oldCursor != null && !oldCursor.isClosed()) {
    oldCursor.close();
  }
}
