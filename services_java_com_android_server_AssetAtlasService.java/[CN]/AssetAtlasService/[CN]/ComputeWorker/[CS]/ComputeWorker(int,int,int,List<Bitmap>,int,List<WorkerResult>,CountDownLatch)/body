{
  mStart=start;
  mEnd=end;
  mStep=step;
  mBitmaps=bitmaps;
  mResults=results;
  mSignal=signal;
  int threshold=(int)(pixelCount * PACKING_THRESHOLD);
  while (threshold > MAX_SIZE * MAX_SIZE) {
    threshold>>=1;
  }
  mThreshold=threshold;
}
