{
  TileMode tile=null;
  for (  TileMode tm : TileMode.values()) {
    if (tm.nativeInt == tileMode) {
      tile=tm;
      break;
    }
  }
  RadialGradient_Delegate newDelegate=new RadialGradient_Delegate(x,y,radius,colors,positions,tile);
  return sManager.addDelegate(newDelegate);
}
