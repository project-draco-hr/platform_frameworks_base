{
  if (connection == null) {
    throw new IllegalArgumentException("connection may not be null");
  }
  RouteInterfaceHandler iface=connection.addRouteInterface(RoutePlaybackControls.NAME);
  return new RoutePlaybackControlsHandler(iface);
}
