{
  mSignalClusters.add(cluster);
  cluster.setWifiIndicators(mWifiEnabled,mWifiIconId,mWifiActivityIconId);
  cluster.setMobileDataIndicators(hasMobileDataFeature(),mPhoneSignalIconId,mMobileActivityIconId,mDataTypeIconId);
}
