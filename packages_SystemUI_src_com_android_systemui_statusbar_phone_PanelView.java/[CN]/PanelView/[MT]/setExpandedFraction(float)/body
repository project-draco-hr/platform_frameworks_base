{
  if (Float.isNaN(frac)) {
    if (DEBUG_NAN) {
      Slog.v(TAG,"setExpandedFraction: frac=NaN, using 0 instead",new Throwable());
    }
    frac=0;
  }
  setExpandedHeight(getFullHeight() * frac);
}
