{
  float currentMaxPanelHeight=getMaxPanelHeight();
  if ((!mTracking || isTrackingBlocked()) && mHeightAnimator == null && mExpandedHeight > 0 && currentMaxPanelHeight != mExpandedHeight && !mPeekPending && mPeekAnimator == null && !mPeekTouching) {
    setExpandedHeight(currentMaxPanelHeight);
  }
}
