{
  float currentMaxPanelHeight=getMaxPanelHeight();
  if ((!mTracking || isTrackingBlocked()) && mHeightAnimator == null && !isShadeCollapsed() && currentMaxPanelHeight != mExpandedHeight && !mPeekPending && mPeekAnimator == null && !mPeekTouching) {
    setExpandedHeight(currentMaxPanelHeight);
  }
}
