{
  float currentMaxPanelHeight=getMaxPanelHeight();
  if (!mTracking && mHeightAnimator == null && mExpandedHeight > 0 && currentMaxPanelHeight != mExpandedHeight && !mPeekPending && mPeekAnimator == null) {
    setExpandedHeight(currentMaxPanelHeight);
  }
}
