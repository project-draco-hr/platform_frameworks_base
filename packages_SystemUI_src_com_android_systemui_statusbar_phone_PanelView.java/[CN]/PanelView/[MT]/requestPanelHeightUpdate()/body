{
  float currentMaxPanelHeight=getMaxPanelHeight();
  if (!mTracking && mHeightAnimator == null && mExpandedHeight > 0 && currentMaxPanelHeight != mExpandedHeight) {
    setExpandedHeightInternal(currentMaxPanelHeight);
  }
}
