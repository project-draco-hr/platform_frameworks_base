{
  float fh=getFullHeight();
  if (fh == 0) {
  }
  LOG("setExpansion: height=%.1f fh=%.1f",h,fh);
  if (h < 0)   h=0;
 else   if (h > fh)   h=fh;
  mExpandedHeight=h;
  requestLayout();
  mExpandedFraction=Math.min(1f,h / fh);
}
