{
  if (mShowHours == hours) {
    return;
  }
  mShowHours=hours;
  if (animate) {
    animatePicker(hours,ANIM_DURATION_NORMAL);
  }
  initData();
  invalidate();
  mTouchHelper.invalidateRoot();
}
