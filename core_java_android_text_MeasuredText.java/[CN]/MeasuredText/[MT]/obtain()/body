{
  MeasuredText mt;
synchronized (cached) {
    for (int i=cached.length; --i >= 0; ) {
      if (cached[i] != null) {
        mt=cached[i];
        cached[i]=null;
        return mt;
      }
    }
  }
  mt=new MeasuredText();
  if (localLOGV) {
    Log.v("MEAS","new: " + mt);
  }
  return mt;
}
