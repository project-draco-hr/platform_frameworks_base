{
  if (mStackFromBottom) {
    final int childCount=getChildCount();
    if (childCount > 0) {
      return Math.min(mOverscrollMax,(getHeight() - getChildAt(0).getTop()) / OVERSCROLL_LIMIT_DIVISOR);
    }
 else {
      return mOverscrollMax;
    }
  }
  return super.getOverscrollMax();
}
