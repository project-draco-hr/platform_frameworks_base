{
  canvas.save();
  mClipPath.reset();
  mClipPath.addCircle(mClipPosition * getWidth(),getHeight() / 2.0f,getWidth() / 4.0f,Path.Direction.CW);
  canvas.clipPath(mClipPath);
  super.dispatchDraw(canvas);
  canvas.restore();
}
