{
  if (serviceInfo.permission == null || !serviceInfo.permission.equals(Manifest.permission.BIND_ROUTE_PROVIDER)) {
    Slog.w(TAG,"Ignoring route provider service because it did not " + "require the BIND_ROUTE_PROVIDER permission in its manifest: " + serviceInfo.packageName + "/"+ serviceInfo.name);
    return false;
  }
  return true;
}
