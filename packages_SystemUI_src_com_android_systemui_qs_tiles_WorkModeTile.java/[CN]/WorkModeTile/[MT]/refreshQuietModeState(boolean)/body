{
  if (backgroundRefresh) {
    refreshState(isWorkModeEnabled() ? UserBoolean.BACKGROUND_TRUE : UserBoolean.BACKGROUND_FALSE);
  }
 else {
    refreshState(isWorkModeEnabled() ? UserBoolean.USER_TRUE : UserBoolean.USER_FALSE);
  }
}
