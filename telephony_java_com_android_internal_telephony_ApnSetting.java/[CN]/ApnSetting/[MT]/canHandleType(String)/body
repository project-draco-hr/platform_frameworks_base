{
  for (  String t : types) {
    if (t.equalsIgnoreCase(type) || t.equalsIgnoreCase(Phone.APN_TYPE_ALL) || (t.equalsIgnoreCase(Phone.APN_TYPE_DEFAULT) && type.equalsIgnoreCase(Phone.APN_TYPE_HIPRI))) {
      return true;
    }
  }
  return false;
}
