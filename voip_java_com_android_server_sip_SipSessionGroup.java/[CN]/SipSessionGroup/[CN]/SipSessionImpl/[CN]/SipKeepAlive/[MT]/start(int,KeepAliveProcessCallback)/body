{
  if (mRunning)   return;
  mRunning=true;
  mInterval=interval;
  mCallback=new KeepAliveProcessCallbackProxy(callback);
  mWakeupTimer.set(interval * 1000,this);
  if (SKA_DBG) {
    log("start keepalive:" + mLocalProfile.getUriString());
  }
  run();
}
