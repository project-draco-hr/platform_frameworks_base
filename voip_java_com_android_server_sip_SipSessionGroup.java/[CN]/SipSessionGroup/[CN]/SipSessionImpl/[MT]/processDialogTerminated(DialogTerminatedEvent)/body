{
  if (mDialog == event.getDialog()) {
    onError(new SipException("dialog terminated"));
  }
 else {
    if (SSI_DBG)     log("not the current dialog; current=" + mDialog + ", terminated="+ event.getDialog());
  }
}
