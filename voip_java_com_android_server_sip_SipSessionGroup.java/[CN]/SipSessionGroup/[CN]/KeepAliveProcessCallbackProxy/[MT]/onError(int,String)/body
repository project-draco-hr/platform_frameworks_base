{
  if (mCallback == null)   return;
  proxy(new Runnable(){
    @Override public void run(){
      try {
        mCallback.onError(errorCode,description);
      }
 catch (      Throwable t) {
        loge("onError",t);
      }
    }
  }
);
}
