{
  if (isRequestEvent(Request.INVITE,event)) {
    if (DBG)     log("processRequest: mWakeLock.acquire got INVITE, thread:" + Thread.currentThread());
    mWakeLock.acquire(WAKE_LOCK_HOLDING_TIME);
  }
  process(event);
}
