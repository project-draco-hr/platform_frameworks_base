{
  if (pairable && discoverable)   return BluetoothDevice.SCAN_MODE_CONNECTABLE_DISCOVERABLE;
 else   if (pairable && !discoverable)   return BluetoothDevice.SCAN_MODE_CONNECTABLE;
 else   return BluetoothDevice.SCAN_MODE_NONE;
}
