{
  if (setScreenAutoBrightnessAdjustment(request.screenAutoBrightnessAdjustment) || setLightSensorEnabled(request.useAutoBrightness && DisplayPowerRequest.wantScreenOn(request.screenState))) {
    updateAutoBrightness(false);
  }
}
