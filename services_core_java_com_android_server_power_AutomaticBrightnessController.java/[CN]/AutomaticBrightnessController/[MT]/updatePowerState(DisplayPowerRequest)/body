{
  if (setScreenAutoBrightnessAdjustment(request.screenAutoBrightnessAdjustment) || setLightSensorEnabled(request.useAutoBrightness && request.wantScreenOnNormal())) {
    updateAutoBrightness(false);
  }
}
