{
  try {
    startActivityAsUser(mLaunchIntent,mLaunchOpts.toBundle(),UserHandle.CURRENT);
  }
 catch (  Exception e) {
    Console.logError(RecentsActivity.this,getString(R.string.recents_launch_error_message,"Home"));
  }
}
