{
  super.onNewIntent(intent);
  mTaskLaunched=false;
  Console.logDivider(Constants.Log.App.SystemUIHandshake);
  Console.log(Constants.Log.App.SystemUIHandshake,"[RecentsActivity|onNewIntent]",intent.getAction() + " visible: " + mVisible,Console.AnsiRed);
  Console.logTraceTime(Constants.Log.App.TimeRecentsStartup,Constants.Log.App.TimeRecentsStartupKey,"onNewIntent");
  RecentsTaskLoader.initialize(this);
  RecentsConfiguration.reinitialize(this);
  updateRecentsTasks(intent);
  addSearchBarAppWidgetView();
}
