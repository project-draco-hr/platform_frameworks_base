{
  super.onConfigurationChanged(newConfig);
  updateNavBarScrim(false,AnimationProps.IMMEDIATE);
  int newOrientation=getResources().getConfiguration().orientation;
  EventBus.getDefault().send(new ConfigurationChangedEvent(false,(mLastOrientation != newOrientation)));
  mLastOrientation=newOrientation;
}
