{
  if (mVisible) {
    if (mFullScreenOverlayView != null && mFullScreenOverlayView.cancelAnimateOnEnterRecents(mFinishRunnable))     return true;
    dismissRecentsToHomeRaw(animated);
    return true;
  }
  return false;
}
