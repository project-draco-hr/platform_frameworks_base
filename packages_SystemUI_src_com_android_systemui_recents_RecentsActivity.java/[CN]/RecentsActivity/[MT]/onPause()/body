{
  Console.log(Constants.DebugFlags.App.SystemUIHandshake,"[RecentsActivity|onPause]","",Console.AnsiRed);
  super.onPause();
  RecentsTaskLoader loader=RecentsTaskLoader.getInstance();
  loader.stopLoader();
}
