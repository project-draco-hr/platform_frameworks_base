{
  super.onPause();
  if (Constants.DebugFlags.App.EnableFastToggleRecents) {
    mIterateTrigger.stopDozing();
  }
  if (Constants.DebugFlags.App.EnableFastToggleRecents) {
    mIterateTrigger.stopDozing();
  }
}
