{
  if (Console.Enabled) {
    Console.log(Constants.Log.App.SystemUIHandshake,"[RecentsActivity|onDestroy]","",Console.AnsiRed);
  }
  super.onDestroy();
  unregisterReceiver(mSystemBroadcastReceiver);
  RecentsTaskLoader.getInstance().unregisterReceivers();
}
