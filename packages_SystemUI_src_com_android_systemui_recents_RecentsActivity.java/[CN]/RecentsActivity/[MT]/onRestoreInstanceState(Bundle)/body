{
  super.onRestoreInstanceState(savedInstanceState);
  if (RecentsDebugFlags.Static.EnableHistory && savedInstanceState.getBoolean(KEY_SAVED_STATE_HISTORY_VISIBLE,false)) {
    EventBus.getDefault().send(new ShowHistoryEvent());
  }
}
