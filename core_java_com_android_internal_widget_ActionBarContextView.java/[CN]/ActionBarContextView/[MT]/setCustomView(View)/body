{
  if (mCustomView != null) {
    removeView(mCustomView);
  }
  mCustomView=view;
  if (mTitleView != null) {
    removeView(mTitleView);
    mTitleView=null;
  }
  if (view != null) {
    addView(view);
  }
  requestLayout();
}
