{
  View child;
  if (!mDataChanged) {
    child=mRecycler.get(position);
    if (child != null) {
      setUpChild(child,addChild);
      return child;
    }
  }
  child=mAdapter.getView(position,null,this);
  setUpChild(child,addChild);
  return child;
}
