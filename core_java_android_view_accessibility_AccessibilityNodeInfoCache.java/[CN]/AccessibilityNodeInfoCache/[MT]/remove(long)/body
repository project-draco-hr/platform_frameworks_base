{
  if (ENABLED) {
synchronized (mLock) {
      if (DEBUG) {
        Log.i(LOG_TAG,"Process: " + Process.myPid() + " remove("+ accessibilityNodeId+ ")");
      }
      mCacheImpl.remove(accessibilityNodeId);
    }
  }
}
