{
  String testName="testGetPackageSizeInfo";
  PackageStats stats=invokePMGetPackageSizeInfo();
  assertTrue(stats != null);
  if (localLOGV)   Log.i(TAG,"code=" + stats.codeSize + ", data="+ stats.dataSize+ ", cache="+ stats.cacheSize);
}
