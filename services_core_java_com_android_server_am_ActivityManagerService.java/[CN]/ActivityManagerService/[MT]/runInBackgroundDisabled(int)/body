{
synchronized (this) {
    UidRecord uidRec=mActiveUids.get(uid);
    if (uidRec != null) {
      if (uidRec.idle) {
        doStopUidLocked(uidRec.uid,uidRec);
      }
    }
 else {
      doStopUidLocked(uid,null);
    }
  }
}
