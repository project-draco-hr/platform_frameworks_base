{
  if (info == null)   return null;
  ApplicationInfo newInfo=new ApplicationInfo(info);
  newInfo.uid=applyUserId(info.uid,userId);
  newInfo.dataDir=PackageManager.getDataDirForUser(info.volumeUuid,info.packageName,userId).getAbsolutePath();
  return newInfo;
}
