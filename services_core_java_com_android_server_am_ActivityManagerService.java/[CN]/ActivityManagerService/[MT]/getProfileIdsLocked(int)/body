{
  Set userIds=new HashSet<Integer>();
  final List<UserInfo> profiles=getUserManagerLocked().getProfiles(userId,false);
  for (  UserInfo user : profiles) {
    userIds.add(Integer.valueOf(user.id));
  }
  return userIds;
}
