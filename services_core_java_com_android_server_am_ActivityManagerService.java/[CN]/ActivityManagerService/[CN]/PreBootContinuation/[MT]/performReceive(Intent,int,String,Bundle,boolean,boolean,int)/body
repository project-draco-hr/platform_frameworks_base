{
  curUser++;
  if (curUser >= users.length) {
    curUser=0;
    curRi++;
    if (curRi >= ris.size()) {
      if (onFinishCallback != null) {
        mHandler.post(onFinishCallback);
      }
      return;
    }
  }
  go();
}
