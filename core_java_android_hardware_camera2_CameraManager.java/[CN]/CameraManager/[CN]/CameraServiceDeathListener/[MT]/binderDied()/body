{
synchronized (mLock) {
    mCameraService=null;
    for (    String cameraId : mDeviceIdList) {
      mServiceListener.onStatusChangedLocked(CameraServiceListener.STATUS_PRESENT,cameraId);
    }
  }
}
