{
  if (!mDetectionInProgress && mGravitySensor != null) {
    if (mSensorManager.registerListener(mListener,mGravitySensor,SensorManager.SENSOR_DELAY_UI)) {
      mSuspendBlocker.acquire();
      mDetectionInProgress=true;
      mTotalSamples=0;
      mMovingSamples=0;
    }
  }
}
