{
  if (!mDestroyed) {
    if (mID != 0 && mRS.isAlive()) {
      mRS.nObjDestroyOOB(mID);
    }
    mRS=null;
    mID=0;
    mDestroyed=true;
    Log.v(RenderScript.LOG_TAG,getClass() + " auto finalizing object without having released the RS reference.");
  }
  super.finalize();
}
