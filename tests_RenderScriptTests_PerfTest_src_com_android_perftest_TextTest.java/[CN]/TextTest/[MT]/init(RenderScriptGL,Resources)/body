{
  mRS=rs;
  mRes=res;
  initTextScript();
  mTests=new ScriptField_TestScripts_s.Item[mNames.length];
  int index=0;
  addTest(index++,1);
  addTest(index++,3);
  addTest(index++,5);
  return true;
}
