{
  if (callback == null) {
    throw new IllegalArgumentException("isCorrect: Callback is null.");
  }
  putMessage(Message.obtain(mHandler,MSG_IS_CORRECT,new SpellCheckerParams(text,start,end,locale,callback)));
}
