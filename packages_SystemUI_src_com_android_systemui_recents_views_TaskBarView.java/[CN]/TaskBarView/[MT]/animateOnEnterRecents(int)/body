{
  setVisibility(View.VISIBLE);
  setTranslationY(-getMeasuredHeight());
  animate().translationY(0).setStartDelay(delay > -1 ? delay : mConfig.taskBarEnterAnimDelay).setInterpolator(mConfig.fastOutSlowInInterpolator).setDuration(mConfig.taskBarEnterAnimDuration).withLayer().start();
}
