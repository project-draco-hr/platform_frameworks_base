{
  if (mDataPaths == null) {
    return false;
  }
  Iterator<PatternMatcher> i=mDataPaths.iterator();
  while (i.hasNext()) {
    final PatternMatcher pe=i.next();
    if (pe.match(data)) {
      return true;
    }
  }
  return false;
}
