{
  if (DBG)   log(getName() + message.toString());
switch (message.what) {
case CMD_USER_WANTS_SIGN_IN:
    if (message.arg1 != mUserPromptedToken)     break;
  transitionTo(mInteractiveAppsPromptedState);
break;
default :
return NOT_HANDLED;
}
return HANDLED;
}
