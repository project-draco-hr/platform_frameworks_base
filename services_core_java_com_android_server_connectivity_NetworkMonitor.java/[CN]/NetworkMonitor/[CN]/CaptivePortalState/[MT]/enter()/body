{
  if (mEvaluationTimer.isRunning()) {
    NetworkEvent.logCaptivePortalFound(mNetId,mEvaluationTimer.stop());
    mEvaluationTimer.reset();
  }
  if (mDontDisplaySigninNotification)   return;
  if (mLaunchCaptivePortalAppBroadcastReceiver == null) {
    mLaunchCaptivePortalAppBroadcastReceiver=new CustomIntentReceiver(ACTION_LAUNCH_CAPTIVE_PORTAL_APP,new Random().nextInt(),CMD_LAUNCH_CAPTIVE_PORTAL_APP);
  }
  Message message=obtainMessage(EVENT_PROVISIONING_NOTIFICATION,1,mNetworkAgentInfo.network.netId,mLaunchCaptivePortalAppBroadcastReceiver.getPendingIntent());
  mConnectivityServiceHandler.sendMessage(message);
  sendMessageDelayed(CMD_CAPTIVE_PORTAL_RECHECK,0,CAPTIVE_PORTAL_REEVALUATE_DELAY_MS);
}
