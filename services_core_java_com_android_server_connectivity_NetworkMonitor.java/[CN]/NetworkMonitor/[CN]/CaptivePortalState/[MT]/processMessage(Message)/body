{
  if (DBG)   log(getName() + message.toString());
switch (message.what) {
case CMD_CAPTIVE_PORTAL_REEVALUATE:
    if (message.arg1 != mCaptivePortalReevaluateToken)     break;
  transitionTo(mEvaluatingState);
break;
default :
return NOT_HANDLED;
}
return HANDLED;
}
