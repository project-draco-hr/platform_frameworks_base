{
  if (mActionMode != this) {
    return;
  }
  mCallback.onDestroyActionMode(this);
  mCallback=null;
  animateToMode(false);
  mContextView.closeMode();
  mActionMode=null;
  if (mWasHiddenBeforeMode) {
    hide();
  }
}
