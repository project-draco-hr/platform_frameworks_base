{
  if (mCurrentAnim != null) {
    mCurrentAnim.end();
  }
  if (mContainerView.getVisibility() == View.VISIBLE) {
    return;
  }
  mContainerView.setVisibility(View.VISIBLE);
  mContainerView.setAlpha(0);
  mContainerView.setTranslationY(-mContainerView.getHeight());
  AnimatorSet anim=new AnimatorSet();
  AnimatorSet.Builder b=anim.play(ObjectAnimator.ofFloat(mContainerView,"translationY",0)).with(ObjectAnimator.ofFloat(mContainerView,"alpha",1));
  if (mContentView != null) {
    b.with(ObjectAnimator.ofFloat(mContentView,"translationY",-mContainerView.getHeight(),0));
  }
  anim.addListener(mShowListener);
  mCurrentAnim=anim;
  anim.start();
}
