{
  if (DEBUG_DIM_LAYER)   Slog.v(TAG,"getDimBehindState, dimLayerUser=" + dimLayerUser.toShortString());
  DimBehindState state=mState.get(dimLayerUser);
  if (state == null) {
    state=new DimBehindState();
    mState.put(dimLayerUser,state);
  }
  return state;
}
