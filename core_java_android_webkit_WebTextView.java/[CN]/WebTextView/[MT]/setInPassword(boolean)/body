{
  if (inPassword) {
    setInputType(InputType.TYPE_CLASS_TEXT | EditorInfo.TYPE_TEXT_VARIATION_WEB_PASSWORD);
    createBackground();
  }
  if (!DebugFlags.DRAW_WEBTEXTVIEW) {
    setWillNotDraw(!inPassword);
  }
  setBackgroundDrawable(inPassword ? mBackground : null);
}
