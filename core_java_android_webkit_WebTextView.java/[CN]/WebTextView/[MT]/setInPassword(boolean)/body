{
  if (inPassword) {
    setInputType(EditorInfo.TYPE_CLASS_TEXT | EditorInfo.TYPE_TEXT_VARIATION_WEB_PASSWORD);
    createBackground();
  }
  setWillNotDraw(!inPassword);
  setBackgroundDrawable(inPassword ? mBackground : null);
}
