{
  try {
    Slog.d(TAG,"lights " + (shown ? "on" : "out"));
    mWindowManager.statusBarVisibilityChanged(shown ? View.STATUS_BAR_VISIBLE : View.STATUS_BAR_HIDDEN);
  }
 catch (  RemoteException ex) {
  }
}
