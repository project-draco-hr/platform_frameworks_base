{
  if (mPermissionOwner == null) {
    return;
  }
  for (int i=0; i < mUris.size(); ++i) {
    ActivityManagerNative.getDefault().revokeUriPermissionFromOwner(mPermissionOwner,mUris.get(i),mMode,mSourceUserId);
  }
  mPermissionOwner=null;
}
