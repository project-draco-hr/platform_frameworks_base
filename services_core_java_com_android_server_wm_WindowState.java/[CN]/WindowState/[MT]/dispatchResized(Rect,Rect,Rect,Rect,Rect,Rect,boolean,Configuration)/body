{
  mClient.resized(frame,overscanInsets,contentInsets,visibleInsets,stableInsets,outsets,reportDraw,newConfig,getBackdropFrame(frame),isDragResizeChanged(),mPolicy.isNavBarForcedShownLw(this));
  mDragResizingChangeReported=true;
}
