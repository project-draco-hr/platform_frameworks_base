{
  final Task task=getTask();
  if (task == null) {
    return false;
  }
  if (task.isDragResizing()) {
    return true;
  }
  return mDisplayContent.mDividerControllerLocked.isResizing() && !task.inFreeformWorkspace() && !task.isFullscreen();
}
