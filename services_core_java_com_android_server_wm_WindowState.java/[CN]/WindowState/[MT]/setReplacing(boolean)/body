{
  if ((mAttrs.privateFlags & PRIVATE_FLAG_WILL_NOT_REPLACE_ON_RELAUNCH) != 0 || mAttrs.type == TYPE_APPLICATION_STARTING) {
    return;
  }
  mWillReplaceWindow=true;
  mReplacingWindow=null;
  mAnimateReplacingWindow=animate;
}
