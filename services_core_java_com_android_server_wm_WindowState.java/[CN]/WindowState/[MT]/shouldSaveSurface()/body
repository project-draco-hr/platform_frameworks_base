{
  if (ActivityManager.isLowRamDeviceStatic()) {
    return false;
  }
  Task task=getTask();
  if (task == null || task.inHomeStack() || task.getTopAppWindowToken() != mAppToken) {
    return false;
  }
  return mAppToken.shouldSaveSurface();
}
