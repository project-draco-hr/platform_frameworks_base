{
  PackageUserState state=userState.get(userId);
  if (state != null) {
    return state;
  }
  return ((pkgFlags & ApplicationInfo.FLAG_SYSTEM) != 0) ? DEFAULT_SYSTEM_USER_STATE : DEFAULT_USER_STATE;
}
