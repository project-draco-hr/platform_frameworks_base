{
  int num=0;
  for (  int user : users) {
    if (getInstalled(user)) {
      num++;
    }
  }
  int[] res=new int[num];
  num=0;
  for (  int user : users) {
    if (getInstalled(user)) {
      res[num]=user;
      num++;
    }
  }
  return res;
}
