{
synchronized (VoicetagItem.class) {
    if (nativeObject == 0)     throw new IllegalStateException("Object was destroyed.");
    if (!needToBeLoaded)     throw new IllegalStateException("This Voicetag was not created from a file, does not need to be loaded.");
    loadVoicetagProxy(nativeObject);
  }
}
