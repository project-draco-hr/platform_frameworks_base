{
synchronized (VoicetagItem.class) {
    if (nativeObject == 0)     throw new IllegalStateException("Object was destroyed.");
    if ((path == null) || (path.length() == 0))     throw new IllegalArgumentException("Path may not be null or empty string.");
    saveVoicetagProxy(nativeObject,path);
  }
}
