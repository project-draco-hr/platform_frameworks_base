{
synchronized (VoicetagItem.class) {
    if (nativeObject == 0)     throw new IllegalStateException("Object was destroyed.");
    if ((waveform == null) || (waveform.length == 0))     throw new IllegalArgumentException("Waveform may not be null or empty.");
    setAudioProxy(nativeObject,waveform);
  }
}
