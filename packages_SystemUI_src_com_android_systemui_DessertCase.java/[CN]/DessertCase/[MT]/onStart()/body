{
  super.onStart();
  Slog.v("DessertCase","ACHIEVEMENT UNLOCKED");
  PackageManager pm=getPackageManager();
  pm.setComponentEnabledSetting(new ComponentName(this,DessertCaseDream.class),PackageManager.COMPONENT_ENABLED_STATE_ENABLED,0);
  finish();
}
