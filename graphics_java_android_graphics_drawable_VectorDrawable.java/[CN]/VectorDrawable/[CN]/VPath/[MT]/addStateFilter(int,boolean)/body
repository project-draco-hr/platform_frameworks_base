{
  int k=0;
  while (k < mNumberOfStates) {
    if (mCheckState[mNumberOfStates] == state)     break;
    k++;
  }
  mCheckState[k]=state;
  mCheckValue[k]=condition;
  if (k == mNumberOfStates) {
    mNumberOfStates++;
  }
  if (condition) {
    mNumberOfTrue++;
  }
}
