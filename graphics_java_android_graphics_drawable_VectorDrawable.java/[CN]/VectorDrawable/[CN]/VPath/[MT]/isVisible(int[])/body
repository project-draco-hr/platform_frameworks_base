{
  int match=0;
  for (int i=0; i < state.length; i++) {
    int v=getState(state[i]);
    if (v != STATE_UNDEFINED) {
      if (v == STATE_TRUE) {
        match++;
      }
 else {
        return false;
      }
    }
  }
  return match == mNumberOfTrue;
}
