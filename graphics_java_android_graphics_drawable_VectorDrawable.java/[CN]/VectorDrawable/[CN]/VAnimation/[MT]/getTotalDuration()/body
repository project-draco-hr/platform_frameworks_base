{
  long total=mStartOffset;
  if (getRepeat() == -1) {
    return -1;
  }
  for (int i=0; i < mDuration.length; i++) {
    if (mRepeat > 1) {
      total+=mDuration[i] * mRepeat;
    }
 else {
      total+=mDuration[i];
    }
  }
  return total;
}
