{
  if (mTrigger == 0)   return 0;
  for (int i=0; i < state.length; i++) {
    if (state[i] == mTrigger) {
      if (mTriggerState)       return 0;
      mTriggerState=true;
      return 1;
    }
  }
  if (mTriggerState) {
    mTriggerState=false;
    return -1;
  }
  return 0;
}
