{
  if (SystemProperties.getBoolean(PROPERTY_TEST_CSIM,false)) {
    return true;
  }
  if (mParentCard == null) {
    return false;
  }
  if (mParentCard.isApplicationOnIcc(AppType.APPTYPE_CSIM) && ((mMdn == null) || (mMin == null))) {
    return false;
  }
  return true;
}
