{
  if (mCurrentEncoding.equalsIgnoreCase("QUOTED-PRINTABLE")) {
    propertyValue=getQuotedPrintable(propertyValue);
  }
  mInterpreter.propertyValues(VCardUtils.constructListFromValue(propertyValue,getVersion()));
}
