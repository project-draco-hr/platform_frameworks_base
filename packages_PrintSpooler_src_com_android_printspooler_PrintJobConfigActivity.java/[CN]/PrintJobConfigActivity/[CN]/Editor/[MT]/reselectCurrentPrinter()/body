{
  if (mCurrentPrinter != null) {
    mDestinationSpinnerAdapter.notifyDataSetChanged();
    final int position=mDestinationSpinnerAdapter.getPrinterIndex(mCurrentPrinter.getId());
    mDestinationSpinner.setSelection(position);
  }
}
