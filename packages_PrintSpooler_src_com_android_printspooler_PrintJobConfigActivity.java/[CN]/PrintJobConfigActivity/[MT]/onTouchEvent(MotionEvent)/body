{
  if (!mEditor.isPrintConfirmed() && !mEditor.isPreviewConfirmed() && getWindow().shouldCloseOnTouch(this,event)) {
    if (!mController.isWorking()) {
      PrintJobConfigActivity.this.finish();
    }
    mEditor.cancel();
    return true;
  }
  return super.onTouchEvent(event);
}
