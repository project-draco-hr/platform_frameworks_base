{
  checkNotClosed();
  checkLegalToCapture();
  handler=checkHandler(handler);
  return addPendingSequence(mDeviceImpl.setRepeatingRequest(request,createCaptureListenerProxy(handler,listener),mDeviceHandler));
}
