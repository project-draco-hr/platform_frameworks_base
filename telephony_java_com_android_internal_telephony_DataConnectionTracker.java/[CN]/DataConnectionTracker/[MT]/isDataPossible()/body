{
  boolean possible=(isDataAllowed() && !(getDataEnabled() && (state == State.FAILED || state == State.IDLE)));
  if (!possible && DBG && isDataAllowed()) {
    log("Data not possible.  No coverage: dataState = " + state);
  }
  return possible;
}
