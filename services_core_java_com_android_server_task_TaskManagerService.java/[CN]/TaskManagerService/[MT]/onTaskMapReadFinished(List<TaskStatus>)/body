{
synchronized (mTasks) {
    for (    TaskStatus ts : tasks) {
      if (mTasks.containsTaskIdForUid(ts.getTaskId(),ts.getUid())) {
        continue;
      }
      startTrackingTask(ts);
    }
  }
}
