{
  if (!mPreventScreenOn) {
    Slog.w(TAG,"forceReenableScreen: mPreventScreenOn is false, nothing to do");
    return;
  }
  Slog.w(TAG,"App called preventScreenOn(true) but didn't promptly reenable the screen! " + "Forcing the screen back on...");
  preventScreenOn(false);
}
