{
  if (!e.mIsPredefined) {
    throw new IllegalArgumentException("add requires a predefined Element.");
  }
  Entry en=new Entry();
  en.mElement=e;
  addEntry(en);
  mSizeBits+=e.mSize * 8;
  return this;
}
