{
  SystemServicesProxy ssp=Recents.getSystemServices();
  ssp.startActivityFromRecents(v.getContext(),task.key.id,task.title,ActivityOptions.makeBasic());
  MetricsLogger.action(v.getContext(),MetricsEvent.OVERVIEW_SELECT,task.key.getComponent().toString());
}
