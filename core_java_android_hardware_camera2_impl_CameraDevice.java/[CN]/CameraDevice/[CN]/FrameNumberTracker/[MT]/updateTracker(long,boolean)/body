{
  if (isError) {
    mFutureErrorSet.add(frameNumber);
  }
 else {
    if (frameNumber != mCompletedFrameNumber + 1) {
      throw new AssertionError(String.format("result frame number %d comes out of order",frameNumber));
    }
    mCompletedFrameNumber++;
  }
  update();
}
