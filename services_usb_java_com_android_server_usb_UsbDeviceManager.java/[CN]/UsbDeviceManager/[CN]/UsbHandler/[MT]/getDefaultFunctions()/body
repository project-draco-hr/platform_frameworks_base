{
  String func=SystemProperties.get(USB_PERSISTENT_CONFIG_PROPERTY,UsbManager.USB_FUNCTION_NONE);
  if (UsbManager.USB_FUNCTION_MTP.equals(func)) {
    func=UsbManager.USB_FUNCTION_NONE;
  }
  return func;
}
