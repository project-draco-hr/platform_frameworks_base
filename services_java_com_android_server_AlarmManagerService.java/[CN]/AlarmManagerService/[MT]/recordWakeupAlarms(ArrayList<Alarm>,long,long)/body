{
  for (  Alarm a : alarms) {
    if (a.when > now) {
      break;
    }
    WakeupEvent e=new WakeupEvent(now + skewToRTC,a.operation.getCreatorUid(),a.operation.getIntent().getAction());
    mRecentWakeups.add(e);
  }
}
