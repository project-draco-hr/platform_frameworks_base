{
  if (alarmList.size() <= 0) {
    return;
  }
  for (int i=0; i < alarmList.size(); i++) {
    Alarm alarm=alarmList.get(i);
    if (alarm.operation.equals(operation)) {
      alarmList.remove(i);
      i--;
    }
  }
}
