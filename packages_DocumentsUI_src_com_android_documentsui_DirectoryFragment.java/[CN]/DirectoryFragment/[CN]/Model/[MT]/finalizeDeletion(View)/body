{
  final ContentResolver resolver=mContext.getContentResolver();
  DeleteFilesTask task=new DeleteFilesTask(resolver,new Runnable(){
    @Override public void run(){
      Snackbar.make(view,R.string.toast_failed_delete,Snackbar.LENGTH_LONG).show();
    }
  }
);
  task.execute();
}
