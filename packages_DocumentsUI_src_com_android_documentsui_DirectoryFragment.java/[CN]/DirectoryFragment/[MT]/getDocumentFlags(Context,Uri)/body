{
  final Cursor cursor=context.getContentResolver().query(uri,new String[]{DocumentColumns.FLAGS},null,null,null);
  try {
    if (cursor.moveToFirst()) {
      return getCursorInt(cursor,DocumentColumns.FLAGS);
    }
 else {
      return 0;
    }
  }
  finally {
    cursor.close();
  }
}
