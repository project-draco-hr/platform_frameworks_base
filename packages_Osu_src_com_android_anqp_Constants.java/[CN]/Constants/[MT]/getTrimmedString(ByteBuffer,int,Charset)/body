{
  String s=getString(payload,length,charset,false);
  int zero=length - 1;
  while (zero >= 0) {
    if (s.charAt(zero) != 0) {
      break;
    }
    zero--;
  }
  return zero < length - 1 ? s.substring(0,zero + 1) : s;
}
