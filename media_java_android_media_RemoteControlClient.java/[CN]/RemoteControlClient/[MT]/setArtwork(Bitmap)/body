{
synchronized (mCacheLock) {
    if (mArtworkExpectedWidth > 0) {
      mArtwork=scaleBitmapIfTooBig(artwork,mArtworkExpectedWidth,mArtworkExpectedHeight);
    }
 else {
      mArtwork=artwork;
    }
    sendArtwork_syncCacheLock();
  }
}
