{
  mService=service;
  mMainLooper=looper;
  mH=new H(looper);
  mInputConnectionWrapper=new MutableInputConnectionWrapper(mNoOpInputConnection);
  mIInputContext=new IInputConnectionWrapper(looper,mInputConnectionWrapper);
  setCurrentInputConnection(mDummyInputConnection);
  if (mInstance == null) {
    mInstance=this;
  }
}
