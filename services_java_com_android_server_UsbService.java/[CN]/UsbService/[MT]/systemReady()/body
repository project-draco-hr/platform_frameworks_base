{
synchronized (this) {
    if (mContext.getResources().getBoolean(com.android.internal.R.bool.config_hasUsbHostSupport)) {
      initHostSupport();
    }
    update(false);
    mSystemReady=true;
  }
}
