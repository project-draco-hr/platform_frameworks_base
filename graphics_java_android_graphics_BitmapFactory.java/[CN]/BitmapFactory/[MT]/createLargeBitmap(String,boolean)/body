{
  LargeBitmap bm=null;
  InputStream stream=null;
  try {
    stream=new FileInputStream(pathName);
    bm=createLargeBitmap(stream,isShareable);
  }
  finally {
    if (stream != null) {
      try {
        stream.close();
      }
 catch (      IOException e) {
      }
    }
  }
  return bm;
}
