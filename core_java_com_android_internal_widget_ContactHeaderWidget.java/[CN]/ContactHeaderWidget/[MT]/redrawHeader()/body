{
  if (mContactDataUri != null) {
    mQueryHandler.startQuery(TOKEN_CONTACT_INFO,null,mContactDataUri,HEADER_PROJECTION,null,null,null);
  }
  if (mStatusUri != null) {
    mQueryHandler.startQuery(TOKEN_SOCIAL,null,mStatusUri,SOCIAL_PROJECTION,null,null,null);
  }
}
