{
synchronized (mJobs) {
    for (int i=0; i < jobs.size(); i++) {
      JobStatus js=jobs.get(i);
      if (mJobs.containsJobIdForUid(js.getJobId(),js.getUid())) {
        continue;
      }
      startTrackingJob(js);
    }
  }
}
