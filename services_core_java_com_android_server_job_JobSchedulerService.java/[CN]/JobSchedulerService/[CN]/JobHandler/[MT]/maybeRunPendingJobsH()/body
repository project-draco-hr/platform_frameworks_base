{
synchronized (mJobs) {
    if (mDeviceIdleMode) {
      return;
    }
    if (DEBUG) {
      Slog.d(TAG,"pending queue: " + mPendingJobs.size() + " jobs.");
    }
    assignJobsToContextsH();
    reportActive();
  }
}
