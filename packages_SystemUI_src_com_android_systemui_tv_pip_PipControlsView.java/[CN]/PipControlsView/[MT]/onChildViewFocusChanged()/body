{
  boolean hasFocus=(mFullButtonView != null && mFullButtonView.isFocused()) || (mPlayPauseButtonImageView != null && mPlayPauseButtonImageView.isFocused()) || (mCloseButtonView != null && mCloseButtonView.isFocused());
  if (mHasFocus != hasFocus) {
    mHasFocus=hasFocus;
    if (mOnChildFocusChangeListener != null) {
      mOnChildFocusChangeListener.onFocusChange(getFocusedChild(),mHasFocus);
    }
  }
}
