{
  if (mCachedButtonReceiver != null) {
    return mCachedButtonReceiver;
  }
  ArrayList<MediaSessionRecord> records=getPriorityListLocked(true,Session.FLAG_HANDLES_MEDIA_BUTTONS);
  if (records.size() > 0) {
    mCachedButtonReceiver=records.get(0);
  }
  return mCachedButtonReceiver;
}
