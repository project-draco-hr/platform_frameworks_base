{
  if (mEvaluator == null) {
    mEvaluator=(mValueType == int.class || mValueType == Integer.class) ? sIntEvaluator : (mValueType == double.class || mValueType == Double.class) ? sDoubleEvaluator : sFloatEvaluator;
  }
}
