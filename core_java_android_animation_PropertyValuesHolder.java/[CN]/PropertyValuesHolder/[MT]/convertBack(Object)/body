{
  if (mConverter != null) {
    value=mConverter.convertBack(value);
    if (value == null) {
      throw new IllegalArgumentException("Converter " + mConverter.getClass().getName() + " must implement convertBack and not return null.");
    }
  }
  return value;
}
