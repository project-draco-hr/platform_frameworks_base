{
  boolean needsContext=!isEnabled() || checkRenderContext() == SURFACE_STATE_ERROR;
  if (needsContext) {
    if (!usePBufferSurface()) {
      return false;
    }
  }
  action.run();
  return true;
}
