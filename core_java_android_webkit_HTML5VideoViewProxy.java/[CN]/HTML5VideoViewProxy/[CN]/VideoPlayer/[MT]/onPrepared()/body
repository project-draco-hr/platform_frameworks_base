{
  if (!mHTML5VideoView.isFullScreenMode() || mHTML5VideoView.isFullScreenMode() && mHTML5VideoView.getAutostart())   mHTML5VideoView.start();
  if (mBaseLayer != 0) {
    setBaseLayer(mBaseLayer);
  }
}
