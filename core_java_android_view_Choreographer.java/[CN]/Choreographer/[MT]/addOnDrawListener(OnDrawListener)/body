{
  if (listener == null) {
    throw new IllegalArgumentException("listener must not be null");
  }
  if (DEBUG) {
    Log.d(TAG,"Adding onDraw listener: " + listener);
  }
  mOnDrawListeners=ArrayUtils.appendElement(OnDrawListener.class,mOnDrawListeners,listener);
}
