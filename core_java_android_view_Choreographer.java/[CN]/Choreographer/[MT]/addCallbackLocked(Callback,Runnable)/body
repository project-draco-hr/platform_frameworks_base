{
  Callback callback=obtainCallbackLocked(runnable);
  if (head == null) {
    return callback;
  }
  Callback tail=head;
  while (tail.next != null) {
    tail=tail.next;
  }
  tail.next=callback;
  return head;
}
