{
  if (DEBUG) {
    Log.d(TAG,"Draw occurred.");
  }
  if (USE_PIPELINING) {
synchronized (mLock) {
      if (!mDrewLastFrame) {
        mDrewLastFrame=true;
        scheduleFrameLocked(SystemClock.uptimeMillis());
      }
    }
  }
}
