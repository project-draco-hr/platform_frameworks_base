{
  VerificationParams params1=new VerificationParams(VERIFICATION_URI,ORIGINATING_URI,REFERRER,MANIFEST_DIGEST);
  VerificationParams params2=new VerificationParams(Uri.parse(VERIFICATION_URI_STRING),Uri.parse(ORIGINATING_URI_STRING),Uri.parse(REFERRER_STRING),new ManifestDigest(DIGEST_BYTES));
  params2.setInstallerUid(INSTALLER_UID);
  assertFalse(params1.equals(params2));
}
