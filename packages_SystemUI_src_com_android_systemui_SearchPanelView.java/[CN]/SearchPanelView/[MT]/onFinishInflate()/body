{
  super.onFinishInflate();
  mContext.getSystemService(Context.LAYOUT_INFLATER_SERVICE);
  mCircle=(SearchPanelCircleView)findViewById(R.id.search_panel_circle);
  mLogo=(ImageView)findViewById(R.id.search_logo);
  mScrim=findViewById(R.id.search_panel_scrim);
  mVoiceInteractionManagerService=(IVoiceInteractionManagerService)IVoiceInteractionManagerService.Stub.asInterface(ServiceManager.getService(Context.VOICE_INTERACTION_MANAGER_SERVICE));
}
