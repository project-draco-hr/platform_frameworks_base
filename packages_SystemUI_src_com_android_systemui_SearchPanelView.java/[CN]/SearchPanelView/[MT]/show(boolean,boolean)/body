{
  if (animate) {
    if (mShowing != show) {
      mShowing=show;
    }
  }
 else {
    mShowing=show;
    onAnimationEnd(null);
  }
  setVisibility(show ? View.VISIBLE : View.GONE);
  if (show) {
    setFocusable(true);
    setFocusableInTouchMode(true);
    requestFocus();
  }
}
