{
  if (!mDatabase.isOpen()) {
    throw new IllegalStateException("database " + mDatabase.getPath() + " already closed");
  }
  if (forceCompilation) {
    mDatabase.lock();
    try {
      native_compile(sql);
    }
  finally {
      mDatabase.unlock();
    }
  }
}
