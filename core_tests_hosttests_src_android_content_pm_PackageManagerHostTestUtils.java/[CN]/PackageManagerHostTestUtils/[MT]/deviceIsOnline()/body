{
  AndroidDebugBridge bridge=AndroidDebugBridge.getBridge();
  IDevice[] devices=bridge.getDevices();
  for (  IDevice device : devices) {
    if ((mDevice != null) && mDevice.getSerialNumber().equals(device.getSerialNumber()) && device.isOnline()) {
      return true;
    }
  }
  return false;
}
