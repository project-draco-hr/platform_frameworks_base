{
  Rect oldClipRect=tr.getClipRect();
  tr.setClipRect(mFromRect.left + (int)((mToRect.left - mFromRect.left) * it),oldClipRect.top,mFromRect.right + (int)((mToRect.right - mFromRect.right) * it),oldClipRect.bottom);
}
