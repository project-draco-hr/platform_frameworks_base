{
  try {
    mGroup=new SipSessionGroupExt(group.getLocalProfile(),null,null);
    mSession=(SipSessionGroup.SipSessionImpl)mGroup.createSession(this);
  }
 catch (  Exception e) {
    Log.w(TAG,"start interval measurement error: " + e);
  }
}
