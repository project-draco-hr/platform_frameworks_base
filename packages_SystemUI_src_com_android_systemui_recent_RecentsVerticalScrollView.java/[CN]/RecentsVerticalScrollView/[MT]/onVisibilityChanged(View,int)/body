{
  super.onVisibilityChanged(changedView,visibility);
  if (visibility == View.VISIBLE && changedView == this) {
    post(new Runnable(){
      public void run(){
        update();
      }
    }
);
  }
}
