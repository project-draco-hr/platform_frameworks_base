{
  if (r.startRequested) {
    return;
  }
  if (!knowConn) {
    hasConn=r.hasAutoCreateConnections();
  }
  if (hasConn) {
    return;
  }
  if (mPendingServices.contains(r)) {
    return;
  }
  bringDownServiceLocked(r);
}
