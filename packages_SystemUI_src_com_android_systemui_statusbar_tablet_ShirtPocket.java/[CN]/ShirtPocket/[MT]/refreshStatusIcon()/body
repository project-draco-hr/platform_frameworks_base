{
  setClickable(mClipping != null);
  mIcon.setImageResource(mClipping == null ? R.drawable.ic_sysbar_pocket_hidden : R.drawable.ic_sysbar_pocket_holding);
  mIcon.setVisibility(mClipping == null ? View.INVISIBLE : View.VISIBLE);
}
