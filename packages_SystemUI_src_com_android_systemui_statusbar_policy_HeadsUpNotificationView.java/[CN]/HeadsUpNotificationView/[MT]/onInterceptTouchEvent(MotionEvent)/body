{
  if (DEBUG)   Log.v(TAG,"onInterceptTouchEvent()");
  if (SystemClock.elapsedRealtime() < mStartTouchTime) {
    return true;
  }
  return mEdgeSwipeHelper.onInterceptTouchEvent(ev) || mSwipeHelper.onInterceptTouchEvent(ev) || super.onInterceptTouchEvent(ev);
}
