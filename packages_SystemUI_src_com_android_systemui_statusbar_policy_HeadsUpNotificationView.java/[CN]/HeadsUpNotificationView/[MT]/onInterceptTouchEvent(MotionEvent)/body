{
  if (DEBUG)   Log.v(TAG,"onInterceptTouchEvent()");
  if (mClock.currentTimeMillis() < mStartTouchTime) {
    return true;
  }
  mTouched=true;
  return mEdgeSwipeHelper.onInterceptTouchEvent(ev) || mSwipeHelper.onInterceptTouchEvent(ev) || mHeadsUp == null || super.onInterceptTouchEvent(ev);
}
