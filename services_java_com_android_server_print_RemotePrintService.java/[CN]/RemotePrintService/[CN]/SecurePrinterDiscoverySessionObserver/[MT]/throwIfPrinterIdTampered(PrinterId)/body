{
  if (printerId == null || printerId.getServiceName() == null || !printerId.getServiceName().equals(mComponentName)) {
    throw new IllegalArgumentException("Invalid printer id: " + printerId);
  }
}
