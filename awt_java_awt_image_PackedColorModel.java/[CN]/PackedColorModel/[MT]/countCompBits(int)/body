{
  int bits=0;
  if (compMask != 0) {
    while ((compMask & 1) == 0) {
      compMask>>>=1;
    }
    while ((compMask & 1) == 1) {
      compMask>>>=1;
      bits++;
    }
  }
  if (compMask != 0) {
    return -1;
  }
  return bits;
}
