{
  if (timeoutSeconds < 0 || timeoutSeconds > LIMITED_ADVERTISING_MAX_DURATION) {
    throw new IllegalArgumentException("timeoutSeconds invalid (must be 0-" + LIMITED_ADVERTISING_MAX_DURATION + " seconds)");
  }
  mTimeoutSeconds=timeoutSeconds;
  return this;
}
