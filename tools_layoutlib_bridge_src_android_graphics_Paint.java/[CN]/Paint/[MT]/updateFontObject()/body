{
  if (mTypeface != null) {
    mFont=mTypeface.getFont().deriveFont(mTextSize);
    if (mScaleX != 1.0 || mSkewX != 0) {
      mFont=mFont.deriveFont(new AffineTransform(mScaleX,mSkewX,0,0,1,0));
    }
    mMetrics=Toolkit.getDefaultToolkit().getFontMetrics(mFont);
  }
}
