{
  final Record r=getOrCreateRecord(pkgName,uid);
  for (  Topic topic : r.topics.values()) {
    if (topic.importance != Ranking.IMPORTANCE_UNSPECIFIED && r.importance != topic.importance)     return true;
  }
  return false;
}
