{
  if (mTypeCount >= mTypes.length) {
    Type[] nt=new Type[mTypeCount * 2];
    for (int ct=0; ct < mTypeCount; ct++) {
      nt[ct]=mTypes[ct];
    }
    mTypes=nt;
  }
  mTypes[mTypeCount]=t;
  mTypeCount++;
}
