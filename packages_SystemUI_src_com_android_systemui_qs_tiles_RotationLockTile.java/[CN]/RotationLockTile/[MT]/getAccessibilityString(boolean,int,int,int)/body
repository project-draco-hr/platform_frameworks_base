{
  int stringID;
  if (locked) {
    stringID=isCurrentOrientationLockPortrait(mController,mContext) ? idWhenPortrait : idWhenLandscape;
  }
 else {
    stringID=idWhenOff;
  }
  return mContext.getString(stringID);
}
