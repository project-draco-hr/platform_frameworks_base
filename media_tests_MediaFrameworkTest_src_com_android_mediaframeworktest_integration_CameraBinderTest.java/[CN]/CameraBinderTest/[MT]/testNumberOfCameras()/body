{
  int numCameras=mCameraService.getNumberOfCameras();
  assertTrue("At least this many cameras: " + mGuessedNumCameras,numCameras >= mGuessedNumCameras);
  Log.v(TAG,"Number of cameras " + numCameras);
}
