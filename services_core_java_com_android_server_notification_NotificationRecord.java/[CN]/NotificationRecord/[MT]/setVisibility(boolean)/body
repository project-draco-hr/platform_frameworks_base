{
  final long now=System.currentTimeMillis();
  mVisibleSinceMs=visible ? now : 0L;
  stats.onVisibilityChanged(visible);
  EventLogTags.writeNotificationVisibility(getKey(),visible ? 1 : 0,(int)(now - mCreationTimeMs),(int)(now - mUpdateTimeMs));
}
