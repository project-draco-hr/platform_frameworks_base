{
synchronized (mLock) {
    if (mReferenceCount <= 0) {
      throw new IllegalStateException("attempt to acquire a reference on an already-closed " + getObjInfo());
    }
    mReferenceCount++;
  }
}
