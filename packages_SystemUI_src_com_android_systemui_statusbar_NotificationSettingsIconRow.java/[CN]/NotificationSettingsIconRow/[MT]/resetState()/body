{
  setGearAlpha(0f);
  mIconPlaced=false;
  mSettingsFadedIn=false;
  mAnimating=false;
  mSnapping=false;
  mDismissing=false;
  setIconLocation(true);
  if (mListener != null) {
    mListener.onSettingsIconRowReset(mParent);
  }
}
