{
  setGearAlpha(0f);
  mSettingsFadedIn=false;
  mAnimating=false;
  mSnapping=false;
  mDismissing=false;
  setIconLocation(true);
  if (mListener != null) {
    mListener.onSettingsIconRowReset(this);
  }
}
