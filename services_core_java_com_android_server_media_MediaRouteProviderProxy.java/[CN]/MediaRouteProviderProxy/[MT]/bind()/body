{
  if (!mBound) {
    if (DEBUG) {
      Slog.d(TAG,this + ": Binding");
    }
    try {
      mBound=mContext.bindServiceAsUser(mBindIntent,mServiceConn,Context.BIND_AUTO_CREATE,new UserHandle(mUserId));
      if (!mBound && DEBUG) {
        Slog.d(TAG,this + ": Bind failed");
      }
    }
 catch (    SecurityException ex) {
      if (DEBUG) {
        Slog.d(TAG,this + ": Bind failed",ex);
      }
    }
  }
}
