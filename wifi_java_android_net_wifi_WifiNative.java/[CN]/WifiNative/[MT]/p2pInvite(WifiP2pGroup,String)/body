{
  if (group == null || deviceAddress == null)   return false;
  return doBooleanTempCommand("p2p_invite group=" + group.getInterface() + " peer="+ deviceAddress+ " go_dev_addr="+ group.getOwner().deviceAddress);
}
