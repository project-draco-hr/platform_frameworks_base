{
  if (isLeaf()) {
    this.nodeDepth=depth;
    leafs.add(this);
  }
 else {
    if (left != null) {
      left.getLeafs(leafs,depth + 1);
    }
    if (right != null) {
      right.getLeafs(leafs,depth + 1);
    }
  }
}
