{
  if (left != null && right != null) {
    return left.numLeafs() + right.numLeafs();
  }
 else   if (left != null) {
    return left.numLeafs();
  }
 else   if (right != null) {
    return right.numLeafs();
  }
 else {
    return 1;
  }
}
