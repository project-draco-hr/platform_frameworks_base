{
  if (containsHomeTaskWithinDisplay(openingApps)) {
    setMinimizedDockedStack(true,true);
  }
 else   if (containsHomeTaskWithinDisplay(closingApps)) {
    setMinimizedDockedStack(false,true);
  }
}
