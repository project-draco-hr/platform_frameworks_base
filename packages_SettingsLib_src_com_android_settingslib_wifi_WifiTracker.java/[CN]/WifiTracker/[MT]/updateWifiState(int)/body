{
  if (state == WifiManager.WIFI_STATE_ENABLED) {
    mScanner.resume();
  }
 else {
    mLastInfo=null;
    mLastNetworkInfo=null;
    mScanner.pause();
  }
  if (mListener != null) {
    mListener.onWifiStateChanged(state);
  }
}
