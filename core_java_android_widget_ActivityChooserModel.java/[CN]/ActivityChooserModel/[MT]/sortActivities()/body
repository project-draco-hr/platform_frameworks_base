{
synchronized (mInstanceLock) {
    if (mActivitySorter != null && !mActivitys.isEmpty()) {
      mActivitySorter.sort(mIntent,mActivitys,Collections.unmodifiableList(mHistoricalRecords));
      notifyChanged();
    }
  }
}
