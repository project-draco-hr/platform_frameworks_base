{
  try {
    mInstaller.uninstallSplit(packageName,splitName,0,new UninstallResultCallbackDelegate(callback).getBinder(),mUserId);
  }
 catch (  RemoteException e) {
    throw e.rethrowAsRuntimeException();
  }
}
