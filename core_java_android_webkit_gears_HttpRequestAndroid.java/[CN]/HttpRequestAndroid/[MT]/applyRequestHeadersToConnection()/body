{
  Iterator<String[]> it=requestHeaders.values().iterator();
  while (it.hasNext()) {
    String[] entry=it.next();
    connection.setRequestProperty(entry[HEADERS_MAP_INDEX_KEY],entry[HEADERS_MAP_INDEX_VALUE]);
  }
}
