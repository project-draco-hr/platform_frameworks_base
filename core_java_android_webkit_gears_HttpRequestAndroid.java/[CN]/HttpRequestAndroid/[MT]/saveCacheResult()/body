{
  if (cacheResult == null || cacheResultUrl == null) {
    log("Tried to save cache result but createCacheResult not called");
    return false;
  }
  if (logEnabled)   log("Saving cache result");
  CacheManager.saveCacheFile(cacheResultUrl,cacheResult);
  cacheResult=null;
  cacheResultUrl=null;
  return true;
}
