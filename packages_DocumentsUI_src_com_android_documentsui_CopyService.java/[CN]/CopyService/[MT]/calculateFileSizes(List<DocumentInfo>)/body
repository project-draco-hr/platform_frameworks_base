{
  long result=0;
  for (  DocumentInfo src : srcs) {
    if (src.isDirectory()) {
      result+=calculateFileSizesHelper(src.derivedUri);
    }
 else {
      result+=src.size;
    }
  }
  return result;
}
