{
  return new Quad(mTopLeft.x * factor,mTopLeft.y * factor,mTopRight.x * factor,mTopRight.y * factor,mBottomLeft.x * factor,mBottomLeft.y * factor,mBottomRight.x * factor,mBottomRight.y * factor);
}
