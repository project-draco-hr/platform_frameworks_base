{
  mLogin.setText("");
  mPassword.setText("");
  mLogin.requestFocus();
  mNavigationManager.setMessage(mLockPatternUtils.isPermanentlyLocked() ? R.string.kg_login_too_many_attempts : R.string.kg_login_instructions);
}
