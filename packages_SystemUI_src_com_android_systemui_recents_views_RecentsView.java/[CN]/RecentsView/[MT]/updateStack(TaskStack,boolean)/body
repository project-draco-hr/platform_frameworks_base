{
  mStack=stack;
  if (setStackViewTasks) {
    mTaskStackView.setTasks(stack,true);
  }
  if (stack.getTaskCount() > 0) {
    hideEmptyView();
  }
 else {
    showEmptyView(R.string.recents_empty_message);
  }
}
