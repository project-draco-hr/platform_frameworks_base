{
  if (!RecentsDebugFlags.Static.EnableHistory) {
    return;
  }
  if (mHistoryView != null && mHistoryView.isVisible()) {
    EventBus.getDefault().send(new HideHistoryEvent(true));
  }
 else {
    EventBus.getDefault().send(new ShowHistoryEvent());
  }
}
