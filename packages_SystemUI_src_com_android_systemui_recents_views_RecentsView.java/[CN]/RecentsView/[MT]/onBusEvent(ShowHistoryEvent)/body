{
  hideHistoryButton(getResources().getInteger(R.integer.recents_history_transition_duration),event.postHideStackAnimationTrigger);
  event.postHideStackAnimationTrigger.addLastDecrementRunnable(new Runnable(){
    @Override public void run(){
      setAlpha(0f);
    }
  }
);
}
