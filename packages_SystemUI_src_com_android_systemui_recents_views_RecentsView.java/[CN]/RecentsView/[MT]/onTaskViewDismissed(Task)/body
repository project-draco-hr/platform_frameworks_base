{
  RecentsTaskLoader loader=RecentsTaskLoader.getInstance();
  loader.deleteTaskData(t,false);
  RecentsTaskLoader.getInstance().getSystemServicesProxy().removeTask(t.key.id,Utilities.isDocument(t.key.baseIntent));
}
