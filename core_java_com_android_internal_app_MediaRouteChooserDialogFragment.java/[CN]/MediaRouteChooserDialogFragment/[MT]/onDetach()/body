{
  super.onDetach();
  if (mLauncherListener != null) {
    mLauncherListener.onDetached(this);
  }
  if (mAdapter != null) {
    mRouter.removeCallback(mAdapter.mCallback);
    mAdapter=null;
  }
  mInflater=null;
  mRouter=null;
}
