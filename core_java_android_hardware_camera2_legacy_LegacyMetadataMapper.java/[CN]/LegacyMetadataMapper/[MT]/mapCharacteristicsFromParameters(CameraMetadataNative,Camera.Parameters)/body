{
  m.set(COLOR_CORRECTION_AVAILABLE_ABERRATION_MODES,new int[]{COLOR_CORRECTION_ABERRATION_MODE_FAST,COLOR_CORRECTION_ABERRATION_MODE_HIGH_QUALITY});
  mapControlAe(m,p);
  mapControlAf(m,p);
  mapControlAwb(m,p);
  mapControlOther(m,p);
  mapLens(m,p);
  mapFlash(m,p);
  mapJpeg(m,p);
  m.set(NOISE_REDUCTION_AVAILABLE_NOISE_REDUCTION_MODES,new int[]{NOISE_REDUCTION_MODE_FAST,NOISE_REDUCTION_MODE_HIGH_QUALITY});
  mapScaler(m,p);
  mapSensor(m,p);
  mapStatistics(m,p);
  mapSync(m,p);
  m.set(INFO_SUPPORTED_HARDWARE_LEVEL,INFO_SUPPORTED_HARDWARE_LEVEL_LEGACY);
  mapScalerStreamConfigs(m,p);
  mapRequest(m,p);
}
