{
  if (users == null || users.length == 0) {
    return "";
  }
  String csv="";
  for (  int user : users) {
    csv+=String.valueOf(user);
    csv+=",";
  }
  return csv.substring(0,csv.length() - 1);
}
