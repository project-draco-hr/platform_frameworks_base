{
  float diagonal=(float)Math.sqrt(width * width + height * height);
  float diagonalViewAngle=(float)Math.toDegrees(Math.atan((diagonal / 2f) / viewingDistance) * 2);
  return diagonalViewAngle / N5_DIAGONAL_VIEW_ANGLE;
}
