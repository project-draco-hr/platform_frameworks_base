{
  final DocumentInfo cwd=getCurrentDirectory();
  final Uri childUri=DocumentsContract.createDocument(getContentResolver(),cwd.derivedUri,mMimeType,mDisplayName);
  if (childUri != null) {
    saveStackBlocking();
  }
  return childUri;
}
