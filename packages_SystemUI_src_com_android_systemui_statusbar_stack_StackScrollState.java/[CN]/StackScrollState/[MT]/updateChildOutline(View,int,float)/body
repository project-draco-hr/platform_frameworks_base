{
  int shadowInset=(int)(height - outlineHeight);
  getOutlineForSize(child.getLeft(),child.getTop() + shadowInset,child.getWidth(),child.getHeight() - shadowInset,mChildOutline);
  child.setOutline(mChildOutline);
}
