{
  View container=child.findViewById(R.id.container);
  if (container != null) {
    int clipInset=(int)(height - clipHeight);
    mClipRect.set(0,clipInset,child.getWidth(),height);
    child.setClipBounds(mClipRect);
  }
}
