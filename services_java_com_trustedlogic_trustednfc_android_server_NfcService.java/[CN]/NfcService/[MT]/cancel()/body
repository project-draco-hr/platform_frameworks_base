{
  mContext.enforceCallingPermission(android.Manifest.permission.NFC_RAW,"NFC_RAW permission required to cancel NFC opening");
  if (mOpenPending) {
    mOpenPending=false;
    mManager.doCancel();
    mManager.enableDiscovery(DISCOVERY_MODE_READER);
  }
}
