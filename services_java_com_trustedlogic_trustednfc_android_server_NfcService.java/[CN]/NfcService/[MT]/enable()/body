{
  boolean isSuccess=false;
  mContext.enforceCallingPermission(android.Manifest.permission.NFC_ADMIN,"NFC_ADMIN permission required to enable NFC service");
  if (!isEnabled()) {
    reset();
    isSuccess=_enable();
  }
  return isSuccess;
}
