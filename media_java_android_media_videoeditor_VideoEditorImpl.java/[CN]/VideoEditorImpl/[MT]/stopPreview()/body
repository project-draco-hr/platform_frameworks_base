{
  long result=0;
  if (mPreviewInProgress) {
    try {
      result=mMANativeHelper.stopPreview();
    }
  finally {
      mPreviewInProgress=false;
      unlock();
    }
    return result;
  }
 else {
    return 0;
  }
}
