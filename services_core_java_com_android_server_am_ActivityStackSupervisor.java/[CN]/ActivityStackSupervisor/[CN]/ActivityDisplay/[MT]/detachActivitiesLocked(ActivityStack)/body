{
  if (DEBUG_STACK)   Slog.v(TAG,"detachActivitiesLocked: detaching " + stack + " from displayId="+ mDisplayId);
  mStacks.remove(stack);
  if (mStacks.isEmpty() && mVirtualDisplay != null) {
    mVirtualDisplay.release();
    mVirtualDisplay=null;
  }
  mSurface.release();
}
