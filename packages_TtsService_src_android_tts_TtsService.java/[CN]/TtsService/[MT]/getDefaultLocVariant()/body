{
  String defaultVar=android.provider.Settings.Secure.getString(mResolver,android.provider.Settings.Secure.TTS_DEFAULT_VARIANT);
  if (defaultVar == null) {
    return Locale.getDefault().getVariant();
  }
 else {
    return defaultVar;
  }
}
