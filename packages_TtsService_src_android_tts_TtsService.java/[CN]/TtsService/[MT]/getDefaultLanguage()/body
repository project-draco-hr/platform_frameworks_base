{
  String defaultLang=android.provider.Settings.Secure.getString(mResolver,android.provider.Settings.Secure.TTS_DEFAULT_LANG);
  if (defaultLang == null) {
    return Locale.getDefault().getISO3Language();
  }
 else {
    return defaultLang;
  }
}
