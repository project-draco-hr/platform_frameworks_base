{
  mTimeAlarmState.label=Settings.System.getString(mContext.getContentResolver(),Settings.System.NEXT_ALARM_FORMATTED);
  System.out.println("ALARM LABEL: " + mTimeAlarmState.label);
  mTimeAlarmCallback.refreshView(mTimeTile,mTimeAlarmState);
}
