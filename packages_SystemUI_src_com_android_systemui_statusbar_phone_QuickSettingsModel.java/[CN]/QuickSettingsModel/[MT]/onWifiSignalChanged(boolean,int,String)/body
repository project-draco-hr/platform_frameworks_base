{
  Resources r=mContext.getResources();
  mWifiState.enabled=enabled;
  mWifiState.iconId=enabled && (wifiSignalIconId > 0) ? wifiSignalIconId : R.drawable.ic_qs_wifi_no_network;
  mWifiState.label=enabled && (enabledDesc != null) ? removeDoubleQuotes(enabledDesc) : r.getString(R.string.quick_settings_wifi_off_label);
  mWifiCallback.refreshView(mWifiTile,mWifiState);
}
