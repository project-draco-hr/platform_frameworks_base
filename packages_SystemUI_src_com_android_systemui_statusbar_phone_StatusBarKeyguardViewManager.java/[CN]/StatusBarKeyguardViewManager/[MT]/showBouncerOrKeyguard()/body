{
  if (mBouncer.needsFullscreenBouncer()) {
    mPhoneStatusBar.hideKeyguard();
    mBouncer.show(true);
  }
 else {
    mPhoneStatusBar.showKeyguard();
    mBouncer.hide(false);
    mBouncer.prepare();
  }
}
