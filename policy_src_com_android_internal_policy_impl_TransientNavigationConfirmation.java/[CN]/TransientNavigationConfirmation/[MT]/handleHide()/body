{
  if (mClingWindow != null) {
    if (DEBUG)     Slog.d(TAG,"Hiding transient navigation confirmation for " + mPromptPackage);
    mWindowManager.removeView(mClingWindow);
    mClingWindow=null;
  }
}
