{
  if (task == null || task.isFullscreen()) {
    setupSurface(false);
  }
 else {
    task.getBounds(mBounds);
    setupSurface(true);
    if (!mBounds.equals(mLastBounds)) {
      draw();
    }
  }
}
