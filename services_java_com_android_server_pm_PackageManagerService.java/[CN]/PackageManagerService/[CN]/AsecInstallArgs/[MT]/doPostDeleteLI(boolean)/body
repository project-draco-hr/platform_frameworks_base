{
  boolean ret=false;
  boolean mounted=PackageHelper.isContainerMounted(cid);
  if (mounted) {
    ret=PackageHelper.unMountSdDir(cid);
  }
  if (ret && delete) {
    cleanUpResourcesLI();
  }
  return ret;
}
