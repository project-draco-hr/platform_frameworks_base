{
  mContext.enforceCallingOrSelfPermission(android.Manifest.permission.PACKAGE_VERIFICATION_AGENT,null);
  final Message msg=mHandler.obtainMessage(PACKAGE_VERIFIED);
  msg.arg1=id;
  msg.arg2=verified ? 1 : 0;
  msg.obj=message;
  mHandler.sendMessage(msg);
}
