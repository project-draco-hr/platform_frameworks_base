{
  int[] loc=new int[2];
  fromView.getLocationInWindow(loc);
  int x=loc[0];
  int y=loc[1];
  toView.getLocationInWindow(loc);
  int vX=loc[0];
  int vY=loc[1];
  pt[0]+=x - vX;
  pt[1]+=y - vY;
}
