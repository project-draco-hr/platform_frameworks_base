{
  if (mOnValueChangeListener != null) {
    mOnValueChangeListener.onValueChange(this,previous,mValue);
  }
  mAccessibilityNodeProvider.sendAccessibilityEventForVirtualText(AccessibilityEvent.TYPE_VIEW_FOCUSED);
}
