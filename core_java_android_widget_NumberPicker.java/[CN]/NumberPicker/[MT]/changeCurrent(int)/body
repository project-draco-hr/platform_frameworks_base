{
  if (mCurrent == current) {
    return;
  }
  if (mWrapSelector) {
    current=getWrappedSelectorIndex(current);
  }
  int previous=mCurrent;
  setCurrent(current);
  notifyChange(previous,current);
}
