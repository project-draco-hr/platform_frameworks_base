{
switch (ordering) {
case SEQUENTIALLY:
    mPlayTogether=false;
  break;
case TOGETHER:
mPlayTogether=true;
break;
default :
throw new AndroidRuntimeException("Invalid parameter for TransitionGroup " + "ordering: " + ordering);
}
}
