{
  if (checkNotifyPermission() || checkCarrierPrivilege()) {
    return true;
  }
  String msg="Modify Phone State or Carrier Privilege Permission Denial: " + method + " from pid="+ Binder.getCallingPid()+ ", uid="+ Binder.getCallingUid();
  if (DBG)   log(msg);
  return false;
}
