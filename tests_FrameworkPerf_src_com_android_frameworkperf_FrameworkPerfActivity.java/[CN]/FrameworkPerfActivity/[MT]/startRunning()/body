{
  if (!mStarted) {
    log("Start");
    mStarted=true;
    updateWakeLock();
    startService(new Intent(this,SchedulerService.class));
    mCurOpIndex=0;
    mResults.clear();
    startCurOp();
  }
}
