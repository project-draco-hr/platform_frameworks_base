{
  if (value == null) {
    return null;
  }
  ResourceValue resValue=findResValue(value,false);
  if (resValue == null) {
    return new ResourceValue(type,name,value,false);
  }
  return resolveResValue(resValue);
}
