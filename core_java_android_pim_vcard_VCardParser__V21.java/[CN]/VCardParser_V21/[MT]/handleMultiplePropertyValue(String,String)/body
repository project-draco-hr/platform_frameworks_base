{
  if (mEncoding.equalsIgnoreCase("QUOTED-PRINTABLE")) {
    propertyValue=getQuotedPrintable(propertyValue);
  }
  if (mBuilder != null) {
    mBuilder.propertyValues(VCardUtils.constructListFromValue(propertyValue,(getVersion() == VCardConfig.FLAG_V30)));
  }
}
