{
  for (  int serviceClassType : ALL_SERVICE_CLASSES) {
    Assert.assertEquals(serviceClasses.contains(new Integer(serviceClassType)),BluetoothClass.Service.hasService(deviceClass,serviceClassType));
  }
}
