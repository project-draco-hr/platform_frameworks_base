{
  final int callingUid=Binder.getCallingUid();
  if (callingUid != mOwningUid && mOwningUid != Process.SYSTEM_UID && callingUid != 0) {
    throw new SecurityException("Calling from not trusted UID!");
  }
}
