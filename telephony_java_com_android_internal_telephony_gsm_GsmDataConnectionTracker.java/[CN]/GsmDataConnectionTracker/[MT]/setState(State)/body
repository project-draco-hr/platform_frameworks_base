{
  if (DBG)   log("setState: " + s);
  if (mState != s) {
    EventLog.writeEvent(EventLogTags.GSM_DATA_STATE_CHANGE,mState.toString(),s.toString());
    mState=s;
  }
  if (mState == State.FAILED) {
    if (mWaitingApns != null)     mWaitingApns.clear();
  }
}
