{
  Iterator<ApnContext> it=mApnContexts.values().iterator();
  while (it.hasNext()) {
    ApnContext apnContext=it.next();
    if (apnContext.getApnSetting() != null && mActiveApn.canHandleType(type))     return apnContext;
  }
  return null;
}
