{
  if (connected) {
    resetGroup(mLocalIp);
    if (mOpened)     openToReceiveCalls();
  }
 else {
    Log.v(TAG,"  close auto reg temporarily: " + getUri() + ": "+ mIncomingCallBroadcastAction);
    mSipGroup.close();
    mAutoRegistration.stop();
  }
}
