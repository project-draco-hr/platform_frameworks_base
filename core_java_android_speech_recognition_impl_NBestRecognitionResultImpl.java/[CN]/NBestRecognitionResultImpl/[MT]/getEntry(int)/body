{
synchronized (NBestRecognitionResultImpl.class) {
    if (nativeObject == 0)     throw new IllegalStateException("Object has been disposed");
    long nativeEntryObject=getEntryProxy(nativeObject,index);
    if (nativeEntryObject == 0)     return null;
 else     return new EntryImpl(nativeEntryObject);
  }
}
