{
synchronized (mUsers) {
    UserAccounts accounts=mUsers.get(userId);
    if (accounts == null) {
      accounts=initUser(userId);
      mUsers.append(userId,accounts);
    }
    return accounts;
  }
}
