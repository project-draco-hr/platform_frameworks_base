{
  SyncOperation op=new SyncOperation(target,owningUid,owningPackage,reason,syncSource,new Bundle(extras),allowParallelSyncs,true,NO_JOB_ID);
  op.periodMillis=periodMillis;
  op.flexMillis=flexMillis;
  return op;
}
