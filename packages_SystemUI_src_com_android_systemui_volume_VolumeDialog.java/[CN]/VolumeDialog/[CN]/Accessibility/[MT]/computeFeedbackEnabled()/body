{
  final List<AccessibilityServiceInfo> services=mMgr.getEnabledAccessibilityServiceList(FEEDBACK_ALL_MASK);
  for (  AccessibilityServiceInfo asi : services) {
    if ((asi.feedbackType & FEEDBACK_ALL_MASK) != 0) {
      return true;
    }
  }
  return false;
}
