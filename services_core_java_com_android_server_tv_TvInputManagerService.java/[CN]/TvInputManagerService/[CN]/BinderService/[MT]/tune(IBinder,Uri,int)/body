{
  final int callingUid=Binder.getCallingUid();
  final int resolvedUserId=resolveCallingUserId(Binder.getCallingPid(),callingUid,userId,"tune");
  final long identity=Binder.clearCallingIdentity();
  try {
synchronized (mLock) {
      SessionState sessionState=getUserStateLocked(resolvedUserId).sessionStateMap.get(sessionToken);
      final String serviceName=sessionState.name.getClassName();
      try {
        getSessionLocked(sessionToken,callingUid,resolvedUserId).tune(channelUri);
      }
 catch (      RemoteException e) {
        Log.e(TAG,"error in tune",e);
        return;
      }
    }
  }
  finally {
    Binder.restoreCallingIdentity(identity);
  }
}
