{
  final int callingUid=Binder.getCallingUid();
  final int resolvedUserId=resolveCallingUserId(Binder.getCallingPid(),callingUid,userId,"unselectTrack");
  final long identity=Binder.clearCallingIdentity();
  try {
synchronized (mLock) {
      try {
        getSessionLocked(sessionToken,callingUid,resolvedUserId).unselectTrack(track);
      }
 catch (      RemoteException e) {
        Slog.e(TAG,"error in unselectTrack",e);
      }
    }
  }
  finally {
    Binder.restoreCallingIdentity(identity);
  }
}
