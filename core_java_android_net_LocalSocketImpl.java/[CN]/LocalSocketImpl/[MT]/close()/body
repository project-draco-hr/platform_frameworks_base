{
synchronized (LocalSocketImpl.this) {
    if (fd == null)     return;
    try {
      Libcore.os.close(fd);
    }
 catch (    ErrnoException e) {
      e.rethrowAsIOException();
    }
    fd=null;
  }
}
