{
  if (DEBUG)   Log.d(TAG,"handleCarrierInfoUpdate: plmn = " + mTelephonyPlmn + ", spn = "+ mTelephonySpn);
  for (int i=0; i < mCallbacks.size(); i++) {
    mCallbacks.get(i).onRefreshCarrierInfo(mTelephonyPlmn,mTelephonySpn);
  }
}
