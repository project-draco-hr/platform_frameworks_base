{
  if (!mSimStateCallbacks.contains(callback)) {
    mSimStateCallbacks.add(callback);
    callback.onSimStateChanged(mSimState);
  }
 else {
    if (DEBUG)     Log.e(TAG,"Object tried to add another SIM callback",new Exception("Whoops"));
  }
}
