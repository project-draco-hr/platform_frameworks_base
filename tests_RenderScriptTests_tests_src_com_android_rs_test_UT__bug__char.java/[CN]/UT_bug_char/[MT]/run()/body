{
  RenderScript pRS=RenderScript.create(mCtx);
  ScriptC_bug_char s=new ScriptC_bug_char(pRS,mRes,R.raw.bug_char);
  pRS.setMessageHandler(mRsMessage);
  initializeValues(s);
  s.invoke_bug_char_test();
  pRS.finish();
  waitForMessage();
  pRS.destroy();
}
