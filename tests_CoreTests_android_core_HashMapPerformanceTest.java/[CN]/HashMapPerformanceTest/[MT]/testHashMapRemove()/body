{
  for (int i=ITERATIONS - 1; i >= 0; i--) {
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
    mMap.remove(mKeys[i]);
  }
}
