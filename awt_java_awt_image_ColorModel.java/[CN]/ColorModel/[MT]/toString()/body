{
  return "ColorModel: Color Space = " + cs.toString() + "; has alpha = "+ hasAlpha+ "; is alpha premultipied = "+ isAlphaPremultiplied+ "; transparency = "+ transparency+ "; number color components = "+ numColorComponents+ "; pixel bits = "+ pixel_bits+ "; transfer type = "+ transferType;
}
