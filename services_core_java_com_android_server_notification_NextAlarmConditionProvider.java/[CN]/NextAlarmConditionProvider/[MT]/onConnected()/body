{
  if (DEBUG)   Slog.d(TAG,"onConnected");
  mLookaheadThreshold=mContext.getResources().getInteger(R.integer.config_next_alarm_condition_lookahead_threshold_hrs) * HOURS;
  mConnected=true;
  mTracker=mCallback.getNextAlarmTracker();
  mTracker.addCallback(mTrackerCallback);
}
