{
  if (DEBUG)   Slog.d(TAG,"onConnected");
  mLookaheadThreshold=PropConfig.getInt(mContext,"nextalarm.condition.lookahead",R.integer.config_next_alarm_condition_lookahead_threshold_hrs) * HOURS;
  mConnected=true;
  mTracker.addCallback(mTrackerCallback);
}
