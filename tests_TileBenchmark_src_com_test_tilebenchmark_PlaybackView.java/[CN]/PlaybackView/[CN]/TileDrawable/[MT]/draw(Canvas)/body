{
  super.draw(canvas);
  canvas.drawText(Integer.toString(tile.level),getBounds().left,getBounds().bottom,levelPaint);
  canvas.drawText(tile.x + "," + tile.y,getBounds().left,((getBounds().bottom + getBounds().top) / 2),coordPaint);
}
