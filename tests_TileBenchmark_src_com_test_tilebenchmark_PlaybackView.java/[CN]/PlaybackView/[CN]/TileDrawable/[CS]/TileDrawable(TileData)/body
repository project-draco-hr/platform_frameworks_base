{
  int tileColorId=t.isReady ? R.color.ready_tile : R.color.unready_tile;
  getPaint().setColor(getResources().getColor(tileColorId));
  setBounds(t.x * TILEX,t.y * TILEY,(t.x + 1) * TILEX,(t.y + 1) * TILEY);
  this.tile=t;
}
