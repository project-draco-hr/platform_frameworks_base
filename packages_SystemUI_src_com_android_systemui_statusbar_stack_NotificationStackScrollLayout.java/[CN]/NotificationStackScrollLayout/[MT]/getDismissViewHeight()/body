{
  int height=mDismissView.getHeight() + mPaddingBetweenElements;
  if (getNotGoneChildCount() == 2 && getLastChildNotGone() == mDismissView && getFirstChildNotGone() instanceof ActivatableNotificationView) {
    height+=mCollapseSecondCardPadding;
  }
  return height;
}
