{
  if (isShowing()) {
    return;
  }
  mHidden=false;
  mDismissed=false;
  stopDismissAndHideAnimations();
  preparePopupContent();
  mPopupWindow.showAtLocation(mParent,Gravity.NO_GRAVITY,x,y);
  runShowAnimation();
}
