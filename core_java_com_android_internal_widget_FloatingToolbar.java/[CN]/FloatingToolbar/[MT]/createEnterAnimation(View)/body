{
  AnimatorSet animation=new AnimatorSet();
  animation.playTogether(ObjectAnimator.ofFloat(view,View.ALPHA,0,1).setDuration(200),ObjectAnimator.ofFloat(view,View.X,view.getX(),view.getX()));
  animation.setStartDelay(50);
  return animation;
}
