{
  if (mOffset >= mEnd) {
    return -1;
  }
  if (mOffset + byteCount > mEnd) {
    byteCount=mEnd - mOffset;
  }
  final int numRead=super.read(buffer,offset,byteCount);
  mOffset+=numRead;
  return numRead;
}
