{
  final BugreportInfo info=getInfo(id);
  if (info == null) {
    return;
  }
  info.title=title;
  info.description=description;
  if (name != null && !name.equals(info.name)) {
    info.name=name;
    updateProgress(info);
  }
}
