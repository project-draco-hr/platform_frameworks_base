{
  final BugreportInfo info=getInfo(id);
  if (info == null) {
    return;
  }
  info.title=title;
  info.description=description;
  if (name != null && !info.name.equals(name)) {
    info.name=name;
    updateProgress(info);
  }
}
