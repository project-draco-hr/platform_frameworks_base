{
  if (mOnTestFinishedCalled) {
    return;
  }
  mOnTestFinishedCalled=true;
  if (mResult == null) {
    mResult=new TextResult(mRelativePath);
  }
  mResult.obtainActualResult(mWebView,mResultHandler.obtainMessage(MSG_ACTUAL_RESULT_OBTAINED));
}
