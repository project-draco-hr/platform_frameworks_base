{
switch (message.what) {
case TEST_CMD_1:
{
      tlog("TEST_CMD_1");
      sleep(500);
      quit();
      break;
    }
default :
{
    tlog("default what=" + message.what);
    break;
  }
}
return HANDLED;
}
