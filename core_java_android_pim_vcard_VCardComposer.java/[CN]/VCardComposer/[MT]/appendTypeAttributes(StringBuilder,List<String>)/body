{
  boolean first=true;
  for (  String type : types) {
    if (first) {
      first=false;
    }
 else {
      builder.append(VCARD_ATTR_SEPARATOR);
    }
    if (mIsV30) {
      builder.append(Constants.ATTR_TYPE);
      builder.append('=');
    }
    builder.append(type);
  }
}
