{
  boolean first=true;
  for (  String type : types) {
    if (first) {
      first=false;
    }
 else {
      builder.append(VCARD_PARAM_SEPARATOR);
    }
    appendTypeParameter(builder,type);
  }
}
