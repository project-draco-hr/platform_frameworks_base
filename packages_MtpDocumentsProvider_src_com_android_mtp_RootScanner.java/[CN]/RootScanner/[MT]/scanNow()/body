{
  if (mClosed) {
    return;
  }
  mPollingCount=0;
  if (mBackgroundThread == null) {
    mBackgroundThread=new BackgroundLoaderThread();
    mBackgroundThread.start();
  }
 else {
    notify();
  }
}
