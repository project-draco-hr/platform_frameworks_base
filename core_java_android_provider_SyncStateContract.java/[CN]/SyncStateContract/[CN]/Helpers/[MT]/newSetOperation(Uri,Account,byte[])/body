{
  ContentValues values=new ContentValues();
  values.put(Columns.DATA,data);
  return ContentProviderOperation.newInsert(uri).withValue(Columns.ACCOUNT_NAME,account.mName).withValue(Columns.ACCOUNT_TYPE,account.mType).withValues(values).build();
}
