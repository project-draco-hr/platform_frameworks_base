{
  if (mCurrentPage != null) {
    mCurrentPage.close();
    mCurrentPage=null;
  }
  nativeClose(mNativeDocument);
  try {
    mInput.close();
  }
 catch (  IOException ioe) {
  }
  mInput=null;
  mCloseGuard.close();
}
