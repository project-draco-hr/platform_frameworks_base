{
  mCallbackProxy.onExceededDatabaseQuota(url,databaseIdentifier,currentQuota,new WebStorage.QuotaUpdater(){
    public void updateQuota(    long quota){
      nativeSetDatabaseQuota(quota);
    }
  }
);
}
