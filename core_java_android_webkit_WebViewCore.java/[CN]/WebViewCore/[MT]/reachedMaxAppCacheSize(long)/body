{
  mCallbackProxy.onReachedMaxAppCacheSize(spaceNeeded,getUsedQuota(),new WebStorage.QuotaUpdater(){
    @Override public void updateQuota(    long quota){
      nativeSetNewStorageLimit(quota);
    }
  }
);
}
