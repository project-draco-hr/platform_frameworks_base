{
  if (!mBrowserFrame.firstLayoutDone()) {
    mRestoredX=x;
    mRestoredY=y;
    return;
  }
  if (mWebView != null) {
    Message.obtain(mWebView.mPrivateHandler,WebView.SCROLL_TO_MSG_ID,x,y).sendToTarget();
  }
}
