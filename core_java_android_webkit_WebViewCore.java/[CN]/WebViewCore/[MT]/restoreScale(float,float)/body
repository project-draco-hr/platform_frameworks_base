{
  if (mBrowserFrame.firstLayoutDone() == false) {
    mRestoredScale=(scale <= 0.0) ? mWebView.getZoomOverviewScale() : scale;
    if (mSettings.getUseWideViewPort()) {
      mRestoredTextWrapScale=(textWrapScale <= 0.0) ? mWebView.getReadingLevelScale() : textWrapScale;
    }
  }
}
