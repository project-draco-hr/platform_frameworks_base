{
  mDrawLayersIsScheduled=false;
  if (mDrawIsScheduled || mLastDrawData == null) {
    removeMessages(EventHub.WEBKIT_DRAW);
    webkitDraw();
    return;
  }
  if (nativeUpdateLayers(mNativeClass,mLastDrawData.mBaseLayer)) {
    webkitDraw();
  }
  mWebView.mPrivateHandler.removeMessages(WebViewClassic.INVAL_RECT_MSG_ID);
  mWebView.mPrivateHandler.sendMessageAtFrontOfQueue(mWebView.mPrivateHandler.obtainMessage(WebViewClassic.INVAL_RECT_MSG_ID));
}
