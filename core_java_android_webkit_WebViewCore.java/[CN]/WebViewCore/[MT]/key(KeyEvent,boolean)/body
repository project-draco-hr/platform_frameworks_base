{
  if (DebugFlags.WEB_VIEW_CORE) {
    Log.v(LOGTAG,"CORE key at " + System.currentTimeMillis() + ", "+ evt);
  }
  int keyCode=evt.getKeyCode();
  if (!nativeKey(keyCode,evt.getUnicodeChar(),evt.getRepeatCount(),evt.isShiftPressed(),evt.isAltPressed(),isDown) && keyCode != KeyEvent.KEYCODE_ENTER) {
    mCallbackProxy.onUnhandledKeyEvent(evt);
  }
}
