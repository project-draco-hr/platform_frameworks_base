{
  if (LOGV_ENABLED) {
    Log.v(LOGTAG,"CORE key at " + System.currentTimeMillis() + ", "+ evt);
  }
  if (!nativeKey(evt.getKeyCode(),evt.getUnicodeChar(),evt.getRepeatCount(),evt.isShiftPressed(),evt.isAltPressed(),isDown)) {
    mCallbackProxy.onUnhandledKeyEvent(evt);
  }
}
