{
  if (core != null) {
    if (!core.mDrawIsPaused && !core.getSettings().enableSmoothTransition())     return;
synchronized (core) {
      core.mDrawIsPaused=false;
      core.mDrawIsScheduled=false;
      core.nativeContentInvalidateAll();
      core.contentDraw();
    }
  }
}
