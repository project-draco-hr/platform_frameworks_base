{
  if (LOGV_ENABLED) {
    Log.v(LOGTAG,"CORE keyDown at " + System.currentTimeMillis() + ", "+ event);
  }
switch (target) {
case EventHub.KEYEVENT_UNHANDLED_TYPE:
    break;
case EventHub.KEYEVENT_FOCUS_NODE_TYPE:
  if (nativeSendKeyToFocusNode(code,event.getUnicodeChar(),event.getRepeatCount(),event.isShiftPressed(),event.isAltPressed(),KEY_ACTION_DOWN)) {
    return;
  }
break;
}
mCallbackProxy.onUnhandledKeyEvent(event);
}
