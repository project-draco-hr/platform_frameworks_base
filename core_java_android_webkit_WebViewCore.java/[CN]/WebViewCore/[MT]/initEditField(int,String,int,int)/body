{
  if (mWebView == null) {
    return;
  }
  Message.obtain(mWebView.mPrivateHandler,WebView.INIT_EDIT_FIELD,pointer,0,text).sendToTarget();
  Message.obtain(mWebView.mPrivateHandler,WebView.REQUEST_KEYBOARD_WITH_SELECTION_MSG_ID,pointer,0,new TextSelectionData(start,end,0)).sendToTarget();
}
