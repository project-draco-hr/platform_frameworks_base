{
  Expr expr=new Expr(){
    @Override protected Class resolveType(    ModelAnalyzer modelAnalyzer){
      return Integer.class;
    }
    @Override protected List<Dependency> constructDependencies(){
      return constructDynamicChildrenDependencies();
    }
  }
;
  expr.getUniqueKey();
}
