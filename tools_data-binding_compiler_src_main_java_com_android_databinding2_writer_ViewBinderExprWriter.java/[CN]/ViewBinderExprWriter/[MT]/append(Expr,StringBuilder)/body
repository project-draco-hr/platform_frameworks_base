{
  sb.append("[").append(node.getId()).append(" ]").append(node.getUniqueKey()).append(" : ").append(node.getResolvedType().getCanonicalName() + " : " + node.getClass().getSimpleName());
  if (ClassAnalyzer.getInstance().isObservable(node.getResolvedType())) {
    sb.append("[observable]");
  }
}
