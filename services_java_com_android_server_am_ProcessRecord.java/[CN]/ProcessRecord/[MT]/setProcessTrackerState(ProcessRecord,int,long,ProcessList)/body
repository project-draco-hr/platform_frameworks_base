{
  int state=this == TOP_APP ? ProcessTracker.STATE_TOP : plist.adjToTrackedState(setAdj);
  for (int ip=pkgList.size() - 1; ip >= 0; ip--) {
    pkgList.valueAt(ip).setState(state,memFactor,now);
  }
}
