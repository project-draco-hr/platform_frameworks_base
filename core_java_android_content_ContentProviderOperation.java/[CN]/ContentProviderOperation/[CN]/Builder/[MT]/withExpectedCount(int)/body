{
  if (mType != TYPE_DELETE && mType != TYPE_UPDATE && mType != TYPE_COUNT) {
    throw new IllegalArgumentException("only deletes, updates and counts can have expected counts");
  }
  mExpectedCount=count;
  return this;
}
