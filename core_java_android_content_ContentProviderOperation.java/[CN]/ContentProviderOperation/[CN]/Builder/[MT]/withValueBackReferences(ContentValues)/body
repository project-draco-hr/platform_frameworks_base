{
  if (mType != TYPE_INSERT && mType != TYPE_UPDATE) {
    throw new IllegalArgumentException("only inserts and updates can have value back-references");
  }
  mValuesBackReferences=backReferences;
  return this;
}
