{
  int count=end - start;
  float[] advances=new float[count];
  native_getTextRunAdvances(nativePaint,nativeTypeface,text,start,count,contextStart,contextEnd - contextStart,isRtl,advances,0);
  float sum=0;
  for (int i=0; i < offset; i++) {
    sum+=advances[i];
  }
  return sum;
}
