{
  if (mWrapped != null) {
    mWrapped.closeWithStatus(status,msg);
  }
 else {
    if (mClosed)     return;
    mClosed=true;
    mGuard.close();
    writeCommStatusAndClose(status,msg);
    IoUtils.closeQuietly(mFd);
  }
}
