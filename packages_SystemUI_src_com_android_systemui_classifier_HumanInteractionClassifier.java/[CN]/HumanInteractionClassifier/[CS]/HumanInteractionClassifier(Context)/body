{
  mContext=context;
  mClassifierData=new ClassifierData();
  mAnglesVarianceClassifier=new AnglesVarianceClassifier(mClassifierData);
  mContext.getContentResolver().registerContentObserver(Settings.Global.getUriFor(HIC_ENABLE),false,mSettingsObserver,UserHandle.USER_ALL);
  updateConfiguration();
}
