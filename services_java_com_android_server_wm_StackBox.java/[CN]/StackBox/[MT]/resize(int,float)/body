{
  if (mStackBoxId != stackBoxId) {
    return mStack == null && (mFirst.resize(stackBoxId,weight) || mSecond.resize(stackBoxId,weight));
  }
  if (mParent != null) {
    mParent.mWeight=isFirstChild() ? weight : 1.0f - weight;
  }
  return true;
}
