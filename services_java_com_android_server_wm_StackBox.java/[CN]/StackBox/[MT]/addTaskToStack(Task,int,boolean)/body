{
  if (mStack != null) {
    if (mStack.mStackId == stackId) {
      mStack.addTask(task,toTop);
      return true;
    }
    return false;
  }
  return mFirst.addTaskToStack(task,stackId,toTop) || mSecond.addTaskToStack(task,stackId,toTop);
}
