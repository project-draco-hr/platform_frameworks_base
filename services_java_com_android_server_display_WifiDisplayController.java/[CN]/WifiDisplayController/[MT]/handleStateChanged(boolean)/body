{
  if (mWifiP2pEnabled != enabled) {
    mWifiP2pEnabled=enabled;
    if (enabled) {
      if (!mWfdEnabled) {
        enableWfd();
      }
    }
 else {
      setWfdEnabled(false);
      disconnect();
    }
  }
}
