{
  StringBuilder s=new StringBuilder();
  if (prefix != null) {
    s.append(prefix);
    s.append(" ");
  }
  s.append(count);
  s.append(' ');
  s.append(count == 0 || count > 1 ? plural : singular);
  if (postfix != null) {
    s.append(" ");
    s.append(postfix);
  }
  return s.toString();
}
