{
  if (locale == null) {
    throw new IllegalStateException("Locale can't be null");
  }
  return new VoiceInfo(id,locale,quality,latency,requiresNetworkConnection,((params == null) ? new Bundle() : (Bundle)params.clone()),((additionalFeatures == null) ? new Bundle() : (Bundle)additionalFeatures.clone()));
}
