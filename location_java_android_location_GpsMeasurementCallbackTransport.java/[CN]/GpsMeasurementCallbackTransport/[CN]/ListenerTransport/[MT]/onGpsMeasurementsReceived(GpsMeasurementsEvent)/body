{
  ListenerOperation<GpsMeasurementsEvent.Callback> operation=new ListenerOperation<GpsMeasurementsEvent.Callback>(){
    @Override public void execute(    GpsMeasurementsEvent.Callback callback) throws RemoteException {
      callback.onGpsMeasurementsReceived(event);
    }
  }
;
  foreach(operation);
}
