{
synchronized (mGenLock) {
    if (mClientGenerationIdCurrent != genId) {
      return;
    }
  }
  sendMsg(mEventHandler,MSG_NEW_TRANSPORT_INFO,SENDMSG_REPLACE,genId,transportControlFlags,null,0);
}
