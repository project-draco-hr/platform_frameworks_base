{
  if (DEBUG) {
    Log.e(TAG,"setMetadata(" + genId + ")");
  }
  if (metadata == null) {
    return;
  }
synchronized (mGenLock) {
    if (mClientGenerationIdCurrent != genId) {
      return;
    }
  }
  sendMsg(mEventHandler,MSG_NEW_METADATA,SENDMSG_QUEUE,genId,0,metadata,0);
}
