{
  mInputStarted=true;
  mInputInfo=attribute;
  onStartInput(attribute,restarting);
  if (mWindowVisible) {
    if (mWindowCreated) {
      onStartInputView(mInputInfo,restarting);
    }
    startExtractingText();
  }
}
