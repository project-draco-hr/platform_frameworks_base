{
synchronized (mFirst) {
    VirtualKey vk=mPressedVirtualKey;
    if (vk != null) {
      if (vk.scancode == code) {
        return 2;
      }
    }
    return nativeGetScancodeState(deviceId,code);
  }
}
