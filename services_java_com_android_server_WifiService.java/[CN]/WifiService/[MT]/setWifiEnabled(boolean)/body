{
  enforceChangePermission();
  if (mWifiHandler == null)   return false;
synchronized (mWifiHandler) {
    mWakeLock.acquire();
    mWifiHandler.obtainMessage(enable ? MESSAGE_ENABLE_WIFI : MESSAGE_DISABLE_WIFI).sendToTarget();
  }
  return true;
}
