{
  listener.onStart(this);
  try {
    start();
  }
 catch (  Exception e) {
    listener.onFailed(this);
  }
 finally {
    if (failed()) {
      listener.onFailed(this);
    }
 else {
      listener.onFinished(this);
    }
  }
}
