{
  UserManager um=(UserManager)mContext.getSystemService(Context.USER_SERVICE);
  return um.getUserRestrictions(new UserHandle(userId)).getBoolean(UserManager.DISALLOW_INSTALL_UNKNOWN_SOURCES,false);
}
