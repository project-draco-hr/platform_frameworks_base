{
  final Object currentTarget=mTargetRef == null ? null : mTargetRef.get();
  if (currentTarget != target) {
    mTargetRef=new WeakReference<Object>(target);
    if (currentTarget != null && target != null && currentTarget.getClass() == target.getClass()) {
      return;
    }
    mInitialized=false;
  }
}
