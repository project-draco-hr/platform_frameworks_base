{
  instanceID=id;
  vector=sample;
  label=sampleName;
  float sum=0;
  int size=sample.length;
  for (int i=0; i < size; i++) {
    sum+=sample[i] * sample[i];
  }
  magnitude=(float)Math.sqrt(sum);
}
