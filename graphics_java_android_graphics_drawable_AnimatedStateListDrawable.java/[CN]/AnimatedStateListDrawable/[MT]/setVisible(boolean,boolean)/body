{
  final boolean changed=super.setVisible(visible,restart);
  if (mAnim != null) {
    if (visible) {
      if (changed || restart) {
        mAnim.end();
      }
    }
 else {
      mAnim.end();
    }
  }
  return changed;
}
