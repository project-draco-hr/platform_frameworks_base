{
  if (mLoaders != null) {
    for (int i=mLoaders.size() - 1; i >= 0; i--) {
      LoaderInfo info=mLoaders.valueAt(i);
      Loader<Object> loader=info.loader;
      if (loader == null) {
        continue;
      }
      loader.destroy();
    }
  }
}
