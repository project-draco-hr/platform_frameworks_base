{
  enforceControlPermissionOrInternalCaller();
  final long token=Binder.clearCallingIdentity();
  try {
    return Settings.Secure.getStringForUser(mContext.getContentResolver(),Settings.Secure.ALWAYS_ON_VPN_APP,mUserHandle);
  }
  finally {
    Binder.restoreCallingIdentity(token);
  }
}
