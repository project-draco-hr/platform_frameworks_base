{
  if (!event.isCanceled() && event.getKeyCode() == KeyEvent.KEYCODE_BACK && event.getAction() == KeyEvent.ACTION_UP) {
    if (mListener != null) {
      ((PipRecentsControlsView.Listener)mListener).onBackPressed();
    }
    return true;
  }
  return super.dispatchKeyEvent(event);
}
