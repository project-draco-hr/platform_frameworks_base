{
  throwIfNull(id);
  return new By(){
    @Override public WebElement findElement(    WebElement element){
      return element.findElementById(id);
    }
    @Override public String toString(){
      return "By.id: " + id;
    }
  }
;
}
