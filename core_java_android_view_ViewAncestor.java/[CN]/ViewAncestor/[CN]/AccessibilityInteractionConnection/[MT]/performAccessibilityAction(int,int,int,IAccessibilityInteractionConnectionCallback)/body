{
  final ViewAncestor viewAncestor=mViewAncestor.get();
  if (viewAncestor == null) {
    return;
  }
  getAccessibilityInteractionController().performAccessibilityActionClientThread(accessibilityId,action,interactionId,callback);
}
