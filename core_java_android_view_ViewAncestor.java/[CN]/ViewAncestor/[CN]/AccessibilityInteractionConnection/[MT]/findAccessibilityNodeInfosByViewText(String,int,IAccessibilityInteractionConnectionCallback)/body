{
  final ViewAncestor viewAncestor=mViewAncestor.get();
  if (viewAncestor == null) {
    return;
  }
  getAccessibilityInteractionController().findAccessibilityNodeInfosByViewTextClientThread(text,interactionId,callback);
}
