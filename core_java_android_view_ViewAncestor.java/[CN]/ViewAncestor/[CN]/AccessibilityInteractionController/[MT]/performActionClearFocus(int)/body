{
  View target=findViewByAccessibilityId(accessibilityId);
  if (target == null) {
    return false;
  }
  if (!target.isFocused()) {
    return false;
  }
  target.clearFocus();
  return !target.isFocused();
}
