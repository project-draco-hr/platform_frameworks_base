{
synchronized (this) {
    if (mCallback != null) {
      Log.e(TAG,"Max number of callbacks reached!");
      return -1;
    }
    mCallback=callback;
    mCallbackHandler=handler;
  }
  return 0;
}
