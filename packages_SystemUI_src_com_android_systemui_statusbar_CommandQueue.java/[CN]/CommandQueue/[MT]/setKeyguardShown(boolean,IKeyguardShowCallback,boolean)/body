{
synchronized (mList) {
    mHandler.removeMessages(MSG_SET_KEYGUARD_SHOWN);
    mHandler.obtainMessage(MSG_SET_KEYGUARD_SHOWN,showKeyguard ? 1 : 0,updateWindowFlags ? 1 : 0,callback).sendToTarget();
  }
}
