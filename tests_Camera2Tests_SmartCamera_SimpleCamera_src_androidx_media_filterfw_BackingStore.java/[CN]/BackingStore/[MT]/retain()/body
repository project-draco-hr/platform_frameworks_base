{
  if (mRefCount >= 10) {
    Log.w("BackingStore","High ref-count of " + mRefCount + " on "+ this+ "!");
  }
  if (mRefCount <= 0) {
    throw new RuntimeException("RETAINING RELEASED");
  }
  ++mRefCount;
  return this;
}
