{
  mRS=rs;
  mRes=res;
  initGeoScript();
  mTests=new ScriptField_TestScripts_s.Item[mNames.length];
  int index=0;
  addTest(index++,0);
  addTest(index++,1);
  addTest(index++,2);
  return true;
}
