{
  Slog.v(TAG,"Poll audio system.");
  pollDevices(new DevicePollingCallback(){
    @Override public void onPollingFinished(    List<Integer> ackedAddress){
      checkHotplug(ackedAddress,true);
    }
  }
,HdmiConstants.POLL_ITERATION_IN_ORDER | HdmiConstants.POLL_STRATEGY_SYSTEM_AUDIO,POLL_RETRY_COUNT);
}
