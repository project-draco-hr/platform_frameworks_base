{
  new Thread(){
    public void run(){
      try {
        mountVolume(path);
      }
 catch (      Exception ex) {
        Log.w(TAG,"Failed to mount media on insertion",ex);
      }
    }
  }
.start();
}
