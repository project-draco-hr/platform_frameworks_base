{
  if (mContext.checkCallingOrSelfPermission(android.Manifest.permission.ASEC_DESTROY) != PackageManager.PERMISSION_GRANTED) {
    throw new SecurityException("Requires ASEC_DESTROY permission");
  }
  mConnector.doCommand(String.format("destroy_asec %s",id));
}
