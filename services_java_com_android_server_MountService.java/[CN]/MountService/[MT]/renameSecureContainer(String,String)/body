{
  if (mContext.checkCallingOrSelfPermission(android.Manifest.permission.ASEC_RENAME) != PackageManager.PERMISSION_GRANTED) {
    throw new SecurityException("Requires ASEC_RENAME permission");
  }
  String cmd=String.format("rename_asec %s %s",oldId,newId);
  mConnector.doCommand(cmd);
}
