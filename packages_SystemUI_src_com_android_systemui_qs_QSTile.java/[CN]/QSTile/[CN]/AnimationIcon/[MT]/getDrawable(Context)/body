{
  final AnimatedVectorDrawable d=(AnimatedVectorDrawable)super.getDrawable(context).getConstantState().newDrawable();
  d.start();
  if (mAllowAnimation) {
    mAllowAnimation=false;
  }
 else {
    d.stop();
  }
  return d;
}
