{
  String value=Main.getUI().showInputDialog("Threshold?");
  if (value != null) {
    try {
      threshold=Integer.parseInt(value);
      super.run();
    }
 catch (    Exception exc) {
    }
  }
}
