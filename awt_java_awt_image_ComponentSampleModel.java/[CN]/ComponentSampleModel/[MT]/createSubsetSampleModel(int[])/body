{
  if (bands.length > this.numBands) {
    throw new RasterFormatException(Messages.getString("awt.64"));
  }
  int indices[]=new int[bands.length];
  int offsets[]=new int[bands.length];
  for (int i=0; i < bands.length; i++) {
    indices[i]=bankIndices[bands[i]];
    offsets[i]=bandOffsets[bands[i]];
  }
  return new ComponentSampleModel(dataType,width,height,pixelStride,scanlineStride,indices,offsets);
}
