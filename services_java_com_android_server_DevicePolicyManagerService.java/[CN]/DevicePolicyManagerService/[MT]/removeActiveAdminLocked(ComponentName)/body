{
  ActiveAdmin admin=getActiveAdminUncheckedLocked(adminReceiver);
  if (admin != null) {
    sendAdminCommandLocked(admin,DeviceAdmin.ACTION_DEVICE_ADMIN_DISABLED);
    mAdminList.remove(admin);
    mAdminMap.remove(adminReceiver);
  }
}
