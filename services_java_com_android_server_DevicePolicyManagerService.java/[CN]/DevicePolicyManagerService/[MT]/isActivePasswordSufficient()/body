{
synchronized (this) {
    getActiveAdminForCallerLocked(null,DeviceAdminInfo.USES_POLICY_LIMIT_PASSWORD);
    return mActivePasswordMode >= getPasswordMode(null) && mActivePasswordLength >= getPasswordMinimumLength(null);
  }
}
