{
  if (x != mPosition.x || y != mPosition.y) {
    if (logSurfaceTrace)     Slog.v(SURFACE_TAG,"setPosition(" + x + ","+ y+ "): OLD:"+ this+ ". Called by "+ Debug.getCallers(3));
    mPosition.set(x,y);
  }
  super.setPosition(x,y);
}
