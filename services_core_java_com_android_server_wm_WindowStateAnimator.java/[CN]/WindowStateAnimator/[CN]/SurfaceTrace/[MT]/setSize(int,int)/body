{
  if (w != mSize.x || h != mSize.y) {
    if (logSurfaceTrace)     Slog.v(SURFACE_TAG,"setSize(" + w + ","+ h+ "): OLD:"+ this+ ". Called by "+ Debug.getCallers(3));
    mSize.set(w,h);
  }
  super.setSize(w,h);
}
