{
  closeSuggestionsAdapter();
  mSearchAutoComplete.setDropDownAnimationStyle(0);
  mSearchAutoComplete.setThreshold(0);
  if (mGlobalSearchMode) {
    mSearchAutoComplete.setDropDownAlwaysVisible(true);
    mSearchAutoComplete.setDropDownDismissedOnCompletion(false);
  }
 else {
    mSearchAutoComplete.setDropDownAlwaysVisible(false);
    mSearchAutoComplete.setDropDownDismissedOnCompletion(true);
  }
  if (mSearchable.getSuggestAuthority() != null) {
    mSuggestionsAdapter=new SuggestionsAdapter(getContext(),mSearchable,mOutsideDrawablesCache);
    mSearchAutoComplete.setAdapter(mSuggestionsAdapter);
  }
}
