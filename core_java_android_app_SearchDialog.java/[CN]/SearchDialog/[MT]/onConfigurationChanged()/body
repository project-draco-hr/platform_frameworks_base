{
  if (mSearchable != null && isShowing()) {
    updateSearchButton();
    updateSearchAppIcon();
    updateSearchBadge();
    updateQueryHint();
    mSearchAutoComplete.showDropDownAfterLayout();
  }
}
