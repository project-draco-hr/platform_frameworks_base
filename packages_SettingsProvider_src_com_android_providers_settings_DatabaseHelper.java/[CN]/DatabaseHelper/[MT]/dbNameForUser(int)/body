{
  if (userHandle == UserHandle.USER_SYSTEM) {
    return DATABASE_NAME;
  }
 else {
    File databaseFile=new File(Environment.getUserSystemDirectory(userHandle),DATABASE_NAME);
    return databaseFile.getPath();
  }
}
