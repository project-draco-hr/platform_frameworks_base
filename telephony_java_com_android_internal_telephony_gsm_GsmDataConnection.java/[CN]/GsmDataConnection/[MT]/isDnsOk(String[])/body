{
  if (NULL_IP.equals(dnsServers[0]) && NULL_IP.equals(dnsServers[1]) && !((GSMPhone)phone).isDnsCheckDisabled()) {
    if (!apn.types[0].equals(Phone.APN_TYPE_MMS) || !isIpAddress(apn.mmsProxy)) {
      return false;
    }
  }
  return true;
}
