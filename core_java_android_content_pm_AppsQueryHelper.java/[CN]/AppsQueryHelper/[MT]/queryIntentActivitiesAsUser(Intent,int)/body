{
  try {
    return mPackageManager.queryIntentActivities(intent,null,PackageManager.GET_DISABLED_COMPONENTS | PackageManager.GET_UNINSTALLED_PACKAGES,userId);
  }
 catch (  RemoteException e) {
    throw new IllegalStateException("Package manager has died",e);
  }
}
