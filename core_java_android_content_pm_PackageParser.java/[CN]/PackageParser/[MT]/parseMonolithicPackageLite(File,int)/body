{
  final ApkLite lite=parseApkLite(packageFile,flags);
  final String packagePath=packageFile.getAbsolutePath();
  return new PackageLite(lite.packageName,lite.versionCode,null,packagePath,packagePath,null);
}
