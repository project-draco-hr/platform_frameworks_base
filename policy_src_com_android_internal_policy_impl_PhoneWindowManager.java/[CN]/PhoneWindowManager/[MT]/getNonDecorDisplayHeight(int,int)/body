{
  final boolean horizontal=(rotation == Surface.ROTATION_270 || rotation == Surface.ROTATION_90);
  return fullHeight - (mStatusBarCanHide ? 0 : mStatusBarHeight) - (horizontal ? 0 : mNavigationBarHeight);
}
