{
  if (!mScreenSaverFeatureAvailable) {
    return null;
  }
  IDreamManager sandman=IDreamManager.Stub.asInterface(ServiceManager.checkService("dreams"));
  if (sandman == null) {
    Log.w(TAG,"Unable to find IDreamManager");
  }
  return sandman;
}
