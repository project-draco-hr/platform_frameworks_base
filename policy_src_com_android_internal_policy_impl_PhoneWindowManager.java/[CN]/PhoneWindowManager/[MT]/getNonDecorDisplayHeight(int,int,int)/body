{
  if (mHasSystemNavBar) {
    return fullHeight - mNavigationBarHeight;
  }
  if (mHasNavigationBar) {
    if (fullWidth < fullHeight) {
      return fullHeight - mNavigationBarHeight;
    }
  }
  return fullHeight;
}
