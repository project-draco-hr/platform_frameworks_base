{
  try {
    int sw=mWindowManager.getSwitchState(SW_LID);
    if (sw > 0) {
      mLidOpen=LID_CLOSED;
    }
 else     if (sw == 0) {
      mLidOpen=LID_OPEN;
    }
 else {
      mLidOpen=LID_ABSENT;
    }
  }
 catch (  RemoteException e) {
  }
}
