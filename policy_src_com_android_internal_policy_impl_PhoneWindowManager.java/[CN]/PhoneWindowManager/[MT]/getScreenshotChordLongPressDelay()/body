{
  if (mKeyguardMediator.isShowing()) {
    return (long)(KEYGUARD_SCREENSHOT_CHORD_DELAY_MULTIPLIER * ViewConfiguration.getGlobalActionKeyTimeout());
  }
  return ViewConfiguration.getGlobalActionKeyTimeout();
}
