{
  WearableActionExtensions extensions=action.getExtras().getParcelable(EXTRA_WEARABLE_EXTENSIONS);
  if (extensions != null) {
    return extensions;
  }
 else {
    return new Builder().build();
  }
}
