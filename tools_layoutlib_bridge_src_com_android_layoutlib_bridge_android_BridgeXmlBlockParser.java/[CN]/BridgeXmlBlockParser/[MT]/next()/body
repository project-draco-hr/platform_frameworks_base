{
  if (!mStarted) {
    mStarted=true;
    return START_DOCUMENT;
  }
  int ev=mParser.next();
  if (ev == END_TAG && mParser.getDepth() == 1) {
    mContext.popParser();
  }
  mEventType=ev;
  return ev;
}
