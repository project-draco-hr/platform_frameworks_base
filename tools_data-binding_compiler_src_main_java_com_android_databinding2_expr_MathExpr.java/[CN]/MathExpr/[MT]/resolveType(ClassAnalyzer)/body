{
  if ("+".equals(mOp)) {
    if (String.class.equals(mLeft.getResolvedType()) || String.class.equals(mRight.getResolvedType())) {
      return String.class;
    }
  }
  return classAnalyzer.findCommonParentOf(mLeft.getResolvedType(),mRight.getResolvedType());
}
