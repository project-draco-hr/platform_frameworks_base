{
synchronized (mSupportedMonitorTypes) {
    if (mSupportedMonitorTypes[GeofenceHardware.MONITORING_TYPE_GPS_HARDWARE] != GeofenceHardware.MONITOR_UNSUPPORTED) {
      return new int[]{GeofenceHardware.MONITORING_TYPE_GPS_HARDWARE};
    }
    return new int[0];
  }
}
