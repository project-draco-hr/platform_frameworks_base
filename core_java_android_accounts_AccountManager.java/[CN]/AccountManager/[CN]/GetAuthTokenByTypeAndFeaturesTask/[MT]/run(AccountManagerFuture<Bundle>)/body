{
  try {
    set(future.getResult());
  }
 catch (  OperationCanceledException e) {
    cancel(true);
  }
catch (  IOException e) {
    setException(e);
  }
catch (  AuthenticatorException e) {
    setException(e);
  }
}
