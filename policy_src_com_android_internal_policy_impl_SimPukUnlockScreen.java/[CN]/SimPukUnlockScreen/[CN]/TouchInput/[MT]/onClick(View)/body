{
  if (v == mCancelButton) {
    mPinText.setText("");
    mPukText.setText("");
    mCallback.goToLockScreen();
    return;
  }
  final int digit=checkDigit(v);
  if (digit >= 0) {
    mCallback.pokeWakelock(DIGIT_PRESS_WAKE_MILLIS);
    reportDigit(digit);
  }
}
