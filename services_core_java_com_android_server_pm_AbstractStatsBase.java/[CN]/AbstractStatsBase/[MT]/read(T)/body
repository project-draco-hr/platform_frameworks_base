{
  if (mLock) {
synchronized (data) {
synchronized (mFileLock) {
        readInternal(data);
      }
    }
  }
 else {
synchronized (mFileLock) {
      readInternal(data);
    }
  }
  mLastTimeWritten.set(SystemClock.elapsedRealtime());
}
