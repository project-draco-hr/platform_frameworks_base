{
synchronized (this) {
    long timeStart=acquireAndLock(WRITE);
    try {
      native_execute();
      mDatabase.logTimeStat(mSql,timeStart);
    }
  finally {
      releaseAndUnlock();
    }
  }
}
