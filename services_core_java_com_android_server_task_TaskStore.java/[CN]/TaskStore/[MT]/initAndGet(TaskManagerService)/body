{
synchronized (sSingletonLock) {
    if (sSingleton == null) {
      sSingleton=new TaskStore(taskManagerService.getContext(),Environment.getDataDirectory(),taskManagerService);
    }
    return sSingleton;
  }
}
