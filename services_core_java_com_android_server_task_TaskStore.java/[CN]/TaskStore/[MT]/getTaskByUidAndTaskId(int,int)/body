{
  Iterator<TaskStatus> it=mTasksSet.iterator();
  while (it.hasNext()) {
    TaskStatus ts=it.next();
    if (ts.getUid() == uid && ts.getTaskId() == taskId) {
      return ts;
    }
  }
  return null;
}
