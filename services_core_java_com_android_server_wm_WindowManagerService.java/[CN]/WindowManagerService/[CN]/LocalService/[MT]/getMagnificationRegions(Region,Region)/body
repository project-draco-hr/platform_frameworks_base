{
synchronized (mWindowMap) {
    if (mAccessibilityController != null) {
      mAccessibilityController.getMagnificationRegionsLocked(outMagnified,outAvailable);
    }
 else {
      throw new IllegalStateException("Magnification callbacks not set!");
    }
  }
}
