{
synchronized (mWindowMap) {
    Task task=mTaskIdToTask.get(taskId);
    if (task == null) {
      throw new IllegalArgumentException("setTaskDockedResizing: taskId " + taskId + " not found.");
    }
    task.setDragResizing(resizing,DRAG_RESIZE_MODE_DOCKED_DIVIDER);
  }
}
