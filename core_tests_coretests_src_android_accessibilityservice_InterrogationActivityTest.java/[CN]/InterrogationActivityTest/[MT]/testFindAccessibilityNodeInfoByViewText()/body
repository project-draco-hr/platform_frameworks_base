{
  beforeClassIfNeeded();
  try {
    getActivity();
    List<AccessibilityNodeInfo> buttons=getConnection().findAccessibilityNodeInfosByViewText("butto");
    assertEquals(9,buttons.size());
  }
  finally {
    afterClassIfNeeded();
  }
}
