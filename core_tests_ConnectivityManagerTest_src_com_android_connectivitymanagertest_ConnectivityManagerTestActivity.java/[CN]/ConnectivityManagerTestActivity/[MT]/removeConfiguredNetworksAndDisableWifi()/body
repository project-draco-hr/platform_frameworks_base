{
  if (!disconnectAP()) {
    return false;
  }
  if (!mWifiManager.setWifiEnabled(false)) {
    return false;
  }
  try {
    Thread.sleep(SHORT_TIMEOUT);
  }
 catch (  InterruptedException e) {
  }
  return true;
}
