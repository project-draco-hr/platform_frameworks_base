{
  super.onReset();
  onStopLoading();
  closeQuietly(mResult);
  mResult=null;
  if (mObserving) {
    getContext().getContentResolver().unregisterContentObserver(mObserver);
    mObserving=false;
  }
}
