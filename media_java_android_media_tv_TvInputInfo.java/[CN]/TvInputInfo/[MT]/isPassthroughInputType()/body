{
  if (mType == TYPE_HDMI || mType == TYPE_DISPLAY_PORT || mType == TYPE_SCART || mType == TYPE_DVI || mType == TYPE_VGA || mType == TYPE_COMPONENT || mType == TYPE_COMPOSITE || mType == TYPE_SVIDEO) {
    return true;
  }
  return false;
}
