{
  if (mAutoclickDelaySettingUri.equals(uri)) {
    int delay=Settings.Secure.getInt(mContentResolver,Settings.Secure.ACCESSIBILITY_AUTOCLICK_DELAY,DEFAULT_CLICK_DELAY_MS);
    mClickScheduler.updateDelay(delay);
  }
}
