{
  if (mAutoclickDelaySettingUri.equals(uri)) {
    int delay=Settings.Secure.getIntForUser(mContentResolver,Settings.Secure.ACCESSIBILITY_AUTOCLICK_DELAY,DEFAULT_CLICK_DELAY_MS,mUserId);
    mClickScheduler.updateDelay(delay);
  }
}
