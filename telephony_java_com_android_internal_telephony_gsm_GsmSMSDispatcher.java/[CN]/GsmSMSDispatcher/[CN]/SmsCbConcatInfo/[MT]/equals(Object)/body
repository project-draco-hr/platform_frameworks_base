{
  if (obj instanceof SmsCbConcatInfo) {
    SmsCbConcatInfo other=(SmsCbConcatInfo)obj;
    return mHeader.getSerialNumber() == other.mHeader.getSerialNumber() && mLocation.equals(other.mLocation);
  }
  return false;
}
