{
  if (mTouchedRipples == null) {
    return;
  }
  final Ripple ripple=mTouchedRipples.get(id);
  if (ripple != null) {
    ripple.animate().exit();
    mTouchedRipples.remove(id);
    scheduleAnimation();
  }
}
