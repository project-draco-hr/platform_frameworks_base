{
  ensureShortcutPermission(callingPackage,user);
  if (!isUserEnabled(user)) {
    return null;
  }
  return mShortcutServiceInternal.getShortcutIconFd(getCallingUserId(),callingPackage,shortcut,user.getIdentifier());
}
