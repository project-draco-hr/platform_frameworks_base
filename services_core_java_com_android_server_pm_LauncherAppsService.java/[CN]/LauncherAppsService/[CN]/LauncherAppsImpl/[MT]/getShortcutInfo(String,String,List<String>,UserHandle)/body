{
  ensureShortcutPermission(callingPackage,user);
  return new ParceledListSlice<>(mShortcutServiceInternal.getShortcutInfo(getCallingUserId(),callingPackage,packageName,ids,user.getIdentifier()));
}
