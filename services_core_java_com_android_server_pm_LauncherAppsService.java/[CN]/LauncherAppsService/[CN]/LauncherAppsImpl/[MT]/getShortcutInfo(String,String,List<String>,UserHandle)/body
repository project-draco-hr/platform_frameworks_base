{
  enforceShortcutPermission(user);
  verifyCallingPackage(callingPackage);
  return new ParceledListSlice<>(mShortcutServiceInternal.getShortcutInfo(callingPackage,packageName,ids,user.getIdentifier()));
}
