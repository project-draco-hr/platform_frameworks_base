{
  try {
    final Context context=getContext();
    return Drawable.createFromStreamThemed(context.getContentResolver().openInputStream(uri),null,context.getTheme());
  }
 catch (  Exception e) {
    Log.w(TAG,"Unable to open content: " + uri);
  }
  return null;
}
