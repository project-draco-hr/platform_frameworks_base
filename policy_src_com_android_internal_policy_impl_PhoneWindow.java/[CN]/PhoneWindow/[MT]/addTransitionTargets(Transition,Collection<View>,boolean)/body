{
  TransitionSet set=new TransitionSet();
  set.addTransition(transition);
  for (  View view : views) {
    if (add) {
      set.addTarget(view);
    }
 else {
      set.excludeTarget(view,true);
    }
  }
  return set;
}
