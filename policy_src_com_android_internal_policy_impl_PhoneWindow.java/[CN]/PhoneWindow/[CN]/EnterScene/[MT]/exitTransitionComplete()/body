{
  if (mAllDone) {
    return;
  }
  mAllDone=true;
  sharedElementTransitionComplete(null);
  mHandler.removeCallbacks(this);
  if (!mTriggerEarly) {
    beginEnterScene();
    fadeInBackground();
  }
}
