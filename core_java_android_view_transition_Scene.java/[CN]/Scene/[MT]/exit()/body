{
  if (mSceneRoot.getCurrentScene() == this) {
    if (mLayoutId >= 0 || mLayout != null) {
      getSceneRoot().removeAllViews();
    }
    if (mExitAction != null) {
      mExitAction.run();
    }
  }
}
