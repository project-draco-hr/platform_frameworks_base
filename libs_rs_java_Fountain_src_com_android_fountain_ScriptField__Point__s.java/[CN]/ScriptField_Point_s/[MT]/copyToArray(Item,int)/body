{
  if (mIOBuffer == null)   mIOBuffer=new FieldPacker(Item.sizeof * mType.getX());
  mIOBuffer.reset(index * Item.sizeof);
  mIOBuffer.addF32(i.delta);
  mIOBuffer.addF32(i.position);
  mIOBuffer.addU8(i.color);
}
