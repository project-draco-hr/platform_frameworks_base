{
  Log.i("TTS","Stopping");
  speechQueue.clear();
  nativeSynth.stop();
  isSpeaking=false;
  if (player != null) {
    try {
      player.stop();
    }
 catch (    IllegalStateException e) {
    }
  }
  Log.i("TTS","Stopped");
}
