{
  final MtpDevice device=getDevice(deviceId);
synchronized (device) {
    final int[] handles=device.getObjectHandles(storageId,0,parentObjectHandle);
    if (handles == null) {
      throw new IOException("Failed to fetch object handles.");
    }
    return handles;
  }
}
