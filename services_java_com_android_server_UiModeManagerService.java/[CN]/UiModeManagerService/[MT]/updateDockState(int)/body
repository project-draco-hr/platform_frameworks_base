{
synchronized (mLock) {
    if (newState != mDockState) {
      mDockState=newState;
      mCarModeEnabled=mDockState == Intent.EXTRA_DOCK_STATE_CAR;
      if (mSystemReady) {
        updateLocked();
      }
    }
  }
}
