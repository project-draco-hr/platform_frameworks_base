{
  if (v == mHeader.getBackgroundView()) {
    onQsExpansionStarted();
    if (mQsExpanded) {
      flingSettings(0,false);
    }
 else     if (mQsExpansionEnabled) {
      flingSettings(0,true);
    }
  }
}
