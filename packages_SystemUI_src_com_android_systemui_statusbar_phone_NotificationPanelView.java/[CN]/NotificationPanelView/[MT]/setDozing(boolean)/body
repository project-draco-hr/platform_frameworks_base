{
  if (dozing == mDozing)   return;
  mDozing=dozing;
  if (mDozing) {
    setBackgroundColorAlpha(this,DOZE_BACKGROUND_COLOR,0xff,false);
  }
 else {
    setBackgroundColorAlpha(this,DOZE_BACKGROUND_COLOR,0,true);
  }
  updateKeyguardStatusBarVisibility();
}
