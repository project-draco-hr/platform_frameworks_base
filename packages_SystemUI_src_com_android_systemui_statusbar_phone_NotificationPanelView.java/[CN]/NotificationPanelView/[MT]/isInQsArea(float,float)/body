{
  return (x >= mQsDensityContainer.getX() && x <= mQsDensityContainer.getX() + mQsDensityContainer.getWidth()) && (y <= mNotificationStackScroller.getBottomMostNotificationBottom() || y <= mQsContainer.getY() + mQsContainer.getHeight());
}
