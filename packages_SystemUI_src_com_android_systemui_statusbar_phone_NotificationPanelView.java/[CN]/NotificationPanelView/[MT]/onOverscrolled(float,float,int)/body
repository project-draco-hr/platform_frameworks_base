{
  if (mIntercepting && shouldQuickSettingsIntercept(lastTouchX,lastTouchY,-1)) {
    onQsExpansionStarted(amount);
    mInitialHeightOnTouch=mQsExpansionHeight;
    mInitialTouchY=mLastTouchY;
    mInitialTouchX=mLastTouchX;
    mQsTracking=true;
  }
}
