{
  if (!mQsExpansionEnabled || mCollapsedOnDown) {
    return false;
  }
  View header=mKeyguardShowing ? mKeyguardStatusBar : mQsContainer.getHeader();
  boolean onHeader=x >= mQsDensityContainer.getX() && x <= mQsDensityContainer.getX() + mQsDensityContainer.getWidth() && y >= header.getTop() && y <= header.getBottom();
  if (mQsExpanded) {
    return onHeader || (yDiff < 0 && isInQsArea(x,y));
  }
 else {
    return onHeader;
  }
}
