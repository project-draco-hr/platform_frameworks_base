{
  if (LOW_POWER_MODE_TRIGGER_LEVEL_URI.equals(uri)) {
    final int level=Settings.Global.getInt(mContext.getContentResolver(),Settings.Global.LOW_POWER_MODE_TRIGGER_LEVEL,0);
    setSaverTrigger(level);
  }
}
