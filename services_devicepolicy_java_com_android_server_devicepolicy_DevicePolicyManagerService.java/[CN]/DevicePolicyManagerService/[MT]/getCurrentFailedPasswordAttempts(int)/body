{
synchronized (this) {
    getActiveAdminForCallerLocked(null,DeviceAdminInfo.USES_POLICY_WATCH_LOGIN);
    DevicePolicyData policy=getUserData(getProfileParent(userHandle).id);
    return policy.mFailedPasswordAttempts;
  }
}
