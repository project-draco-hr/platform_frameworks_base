{
  final String base=userHandle == UserHandle.USER_SYSTEM ? getDevicePolicyFilePathForSystemUser() + DEVICE_POLICIES_XML : new File(environmentGetUserSystemDirectory(userHandle),DEVICE_POLICIES_XML).getAbsolutePath();
  return new JournaledFile(new File(base),new File(base + ".tmp"));
}
