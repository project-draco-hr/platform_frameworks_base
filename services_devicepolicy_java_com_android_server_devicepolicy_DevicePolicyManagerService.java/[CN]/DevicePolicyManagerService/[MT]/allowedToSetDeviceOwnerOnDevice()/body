{
  if (!hasUserSetupCompleted(UserHandle.USER_OWNER)) {
    return true;
  }
  int callingId=Binder.getCallingUid();
  return (callingId == Process.SHELL_UID || callingId == Process.ROOT_UID) && mUserManager.getUserCount() == 1 && AccountManager.get(mContext).getAccounts().length == 0;
}
