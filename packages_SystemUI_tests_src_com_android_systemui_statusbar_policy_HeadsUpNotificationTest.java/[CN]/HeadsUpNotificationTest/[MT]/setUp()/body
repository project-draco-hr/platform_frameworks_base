{
  super.setUp();
  MockitoAnnotations.initMocks(this);
  mHeadsUp=new HeadsUpNotificationView(mContext,mClock,mMockSwipeHelper,mMockEdgeSwipeHelper,NOTIFICATION_DECAY,MINIMUM_DISPLAY_TIME,TOUCH_SENSITIVITY,SNOOZE_TIME);
  mHeadsUp.setBar(mMockStatusBar);
}
