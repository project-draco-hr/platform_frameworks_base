{
  ensureView();
  if (mRoot.getVisibility() == View.VISIBLE || mShowingSoon) {
    mKeyguardView.show();
    return;
  }
  if (!mKeyguardView.dismiss()) {
    mShowingSoon=true;
    mChoreographer.postCallbackDelayed(Choreographer.CALLBACK_ANIMATION,mShowRunnable,null,48);
  }
}
