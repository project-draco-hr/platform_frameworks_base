{
  if (isShowing()) {
    mFadingOut=true;
    mKeyguardView.animate().alpha(0).withLayer().setDuration(duration / 3).setInterpolator(mFadeOutInterpolator).setStartDelay(delay).withEndAction(new Runnable(){
      @Override public void run(){
        mFadingOut=false;
        hide(true);
      }
    }
);
  }
 else {
    hide(true);
  }
}
