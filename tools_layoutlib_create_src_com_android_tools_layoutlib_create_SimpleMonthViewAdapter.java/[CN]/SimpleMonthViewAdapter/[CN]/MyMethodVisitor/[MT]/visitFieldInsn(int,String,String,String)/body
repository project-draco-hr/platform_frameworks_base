{
  if (JAVA_SDF_DESC.equals(desc) && (opcode == Opcodes.PUTFIELD || opcode == Opcodes.GETFIELD) && name.equals("mDayFormatter")) {
    desc="L" + ICU_SDF_INTERNAL_NAME + ";";
  }
  super.visitFieldInsn(opcode,owner,name,desc);
}
