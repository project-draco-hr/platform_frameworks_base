{
synchronized (mLock) {
    SomeArgs args=SomeArgs.obtain();
    args.arg1=mClient;
    args.arg2=printers;
    mHandler.obtainMessage(MyHandler.MSG_ON_REQUEST_UPDATE_PRINTERS,args).sendToTarget();
  }
}
