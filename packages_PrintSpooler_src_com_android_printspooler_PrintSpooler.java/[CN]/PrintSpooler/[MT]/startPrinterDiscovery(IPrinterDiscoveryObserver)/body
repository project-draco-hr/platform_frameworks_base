{
synchronized (mLock) {
    SomeArgs args=SomeArgs.obtain();
    args.arg1=mClient;
    args.arg2=observer;
    mHandler.obtainMessage(MyHandler.MSG_ON_START_PRINTER_DISCOVERY,args).sendToTarget();
  }
}
