{
  byte[] octets=new byte[size];
  payload.get(octets);
  long value=0;
  for (int n=octets.length - 1; n >= 0; n--) {
    value=(value << Byte.SIZE) | (octets[n] & BYTE_MASK);
  }
  return value;
}
