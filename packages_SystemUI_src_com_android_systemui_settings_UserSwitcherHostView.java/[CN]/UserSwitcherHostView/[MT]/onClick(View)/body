{
  postDelayed(new Runnable(){
    public void run(){
      if (mGuestUser != null) {
        switchUser(0);
        mUserManager.removeUser(mGuestUser.id);
        mGuestUser=null;
        refreshUsers();
      }
    }
  }
,100);
}
