{
  super(context,attrs,defStyleAttr);
  if (isInEditMode()) {
    return;
  }
  mUserManager=(UserManager)context.getSystemService(Context.USER_SERVICE);
  mGuestUserEnabled=Settings.Global.getInt(context.getContentResolver(),Settings.Global.GUEST_USER_ENABLED,0) == 1;
}
