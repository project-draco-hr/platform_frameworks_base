{
  if (UserHandle.getAppId(uid) == Process.myUid()) {
    return UserHandle.isSameApp(host.id.uid,uid) && host.id.packageName.equals(packageName);
  }
 else {
    return host.id.uid == uid && host.id.packageName.equals(packageName);
  }
}
