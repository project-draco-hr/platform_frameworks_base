{
  RenderScript pRS=RenderScript.create(mCtx);
  ScriptC_clamp_relaxed s=new ScriptC_clamp_relaxed(pRS,mRes,R.raw.clamp_relaxed);
  pRS.setMessageHandler(mRsMessage);
  s.invoke_clamp_test();
  pRS.finish();
  waitForMessage();
  pRS.destroy();
}
