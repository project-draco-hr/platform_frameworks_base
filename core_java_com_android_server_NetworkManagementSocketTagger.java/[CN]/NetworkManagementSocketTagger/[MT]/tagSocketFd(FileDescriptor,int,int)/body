{
  final int fdNum=fd.getInt$();
  if (fdNum == -1 || (tag == -1 && uid == -1))   return;
  String cmd="t " + fdNum;
  if (tag == -1) {
    cmd+=" 0";
  }
 else {
    cmd+=" " + tagToKernel(tag);
  }
  if (uid != -1) {
    cmd+=" " + uid;
  }
  internalModuleCtrl(cmd);
}
