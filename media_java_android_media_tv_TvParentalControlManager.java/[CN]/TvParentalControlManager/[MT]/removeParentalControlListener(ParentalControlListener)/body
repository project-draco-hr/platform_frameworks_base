{
  if (listener == null) {
    throw new IllegalArgumentException("listener cannot be null");
  }
synchronized (mLock) {
    for (Iterator<ParentalControlListenerRecord> it=mParentalControlListenerRecordList.iterator(); it.hasNext(); ) {
      ParentalControlListenerRecord record=it.next();
      if (record.getListener() == listener) {
        it.remove();
        break;
      }
    }
  }
}
