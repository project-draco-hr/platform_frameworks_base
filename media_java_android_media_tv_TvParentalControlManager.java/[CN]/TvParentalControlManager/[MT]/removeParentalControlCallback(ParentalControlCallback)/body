{
  if (callback == null) {
    throw new IllegalArgumentException("callback cannot be null");
  }
synchronized (mLock) {
    for (Iterator<ParentalControlCallbackRecord> it=mParentalControlCallbackRecordList.iterator(); it.hasNext(); ) {
      ParentalControlCallbackRecord record=it.next();
      if (record.getCallback() == callback) {
        it.remove();
        break;
      }
    }
  }
}
