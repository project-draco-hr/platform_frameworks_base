{
  mRS.validate();
  validate2DRange(xoff,yoff,w,h);
  mRS.nAllocationData2D(getID(),xoff,yoff,0,Type.CubemapFace.POSITIVE_X.mID,w,h,data.getID(),dataXoff,dataYoff,0,Type.CubemapFace.POSITIVE_X.mID);
}
