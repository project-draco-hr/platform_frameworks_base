{
  try {
    if (bindToService(context,defaultServiceConnection)) {
      if (gearsDialogService == null) {
synchronized (defaultServiceConnection) {
          defaultServiceConnection.wait(3000);
        }
      }
    }
  }
 catch (  InterruptedException e) {
    Log.e(TAG,"exception: " + e);
  }
}
