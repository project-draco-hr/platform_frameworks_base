{
  if (mSuggestionsPopupWindow != null && ((mTextView instanceof ExtractEditText) || !mSuggestionsPopupWindow.isShowingUp())) {
    mSuggestionsPopupWindow.hide();
  }
  hideInsertionPointCursorController();
}
