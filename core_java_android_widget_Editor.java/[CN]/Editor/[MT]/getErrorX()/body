{
  final float scale=mTextView.getResources().getDisplayMetrics().density;
  final Drawables dr=mTextView.mDrawables;
  return mTextView.getWidth() - mErrorPopup.getWidth() - mTextView.getPaddingRight()- (dr != null ? dr.mDrawableSizeRight : 0) / 2 + (int)(25 * scale + 0.5f);
}
