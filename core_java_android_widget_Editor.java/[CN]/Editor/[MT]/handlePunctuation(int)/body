{
  CharSequence text=mTextView.getText();
  if (offset < text.length()) {
    int c=Character.codePointAt(text,offset);
    if (c == 0x002e || c == 0x003f || c == 0x0021) {
      offset=Character.offsetByCodePoints(text,offset,1);
    }
  }
  return offset;
}
