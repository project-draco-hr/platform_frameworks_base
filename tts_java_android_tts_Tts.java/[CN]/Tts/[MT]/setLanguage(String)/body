{
synchronized (startLock) {
    if (!started) {
      return;
    }
    try {
      itts.setLanguage(language);
    }
 catch (    RemoteException e) {
      started=false;
      initTts();
    }
  }
}
