{
  if (p instanceof LayoutParams) {
    LayoutParams lp=(LayoutParams)p;
    return lp.leftMargin == mItemMargin && lp.rightMargin == mItemMargin && lp.width == LayoutParams.WRAP_CONTENT && lp.height == LayoutParams.WRAP_CONTENT;
  }
  return false;
}
