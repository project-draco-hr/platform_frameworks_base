{
  if (mOverflowButton != null) {
    final MenuPopupHelper popup=new MenuPopupHelper(getContext(),mMenu,mOverflowButton,true);
    post(new Runnable(){
      public void run(){
        popup.show();
      }
    }
);
    mOverflowPopup=popup;
    return true;
  }
  return false;
}
