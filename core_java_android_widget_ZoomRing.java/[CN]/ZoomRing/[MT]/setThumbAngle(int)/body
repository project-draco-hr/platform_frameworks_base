{
  mThumbAngle=angle;
  mThumbCenterX=(int)(Math.cos(1f * angle / RADIAN_INT_MULTIPLIER) * THUMB_DISTANCE) + mCenterX;
  mThumbCenterY=(int)(Math.sin(1f * angle / RADIAN_INT_MULTIPLIER) * THUMB_DISTANCE) * -1 + mCenterY;
  invalidate();
}
