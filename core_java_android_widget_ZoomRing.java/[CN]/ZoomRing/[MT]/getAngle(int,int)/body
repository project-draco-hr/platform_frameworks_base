{
  int radians=(int)(Math.atan2(localY,localX) * RADIAN_INT_MULTIPLIER);
  if (radians < 0) {
    return -radians;
  }
 else   if (radians > 0) {
    return 2 * PI_INT_MULTIPLIED - radians;
  }
 else {
    return 0;
  }
}
