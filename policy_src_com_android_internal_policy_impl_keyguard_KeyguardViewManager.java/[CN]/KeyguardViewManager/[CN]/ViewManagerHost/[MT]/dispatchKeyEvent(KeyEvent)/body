{
  if (event.getAction() == KeyEvent.ACTION_DOWN && event.getKeyCode() == KeyEvent.KEYCODE_BACK && mKeyguardView != null) {
    if (mKeyguardView.handleBackKey()) {
      return true;
    }
  }
  return super.dispatchKeyEvent(event);
}
