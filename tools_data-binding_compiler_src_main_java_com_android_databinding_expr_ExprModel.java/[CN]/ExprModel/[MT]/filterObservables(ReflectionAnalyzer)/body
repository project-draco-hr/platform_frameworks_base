{
  return Iterables.filter(mExprMap.values(),new Predicate<Expr>(){
    @Override public boolean apply(    Expr input){
      return reflectionAnalyzer.isObservable(input.getResolvedType());
    }
  }
);
}
