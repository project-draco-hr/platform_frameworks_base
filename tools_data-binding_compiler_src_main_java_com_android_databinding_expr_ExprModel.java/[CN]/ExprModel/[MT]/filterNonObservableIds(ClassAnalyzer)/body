{
  return Iterables.filter(mExprMap.values(),new Predicate<Expr>(){
    @Override public boolean apply(    Expr input){
      return input instanceof IdentifierExpr && !input.hasId() && !classAnalyzer.isObservable(input.getResolvedType())&& input.isDynamic();
    }
  }
);
}
