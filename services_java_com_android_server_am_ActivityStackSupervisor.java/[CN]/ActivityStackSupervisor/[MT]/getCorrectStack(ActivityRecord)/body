{
  if (!r.isHomeActivity) {
    if (mStacks.size() == 1) {
      int stackId=mService.createStack(HOME_STACK_ID,StackBox.TASK_STACK_GOES_OVER,1.0f);
      mMainStack=getStack(stackId);
    }
    return mMainStack;
  }
  return mHomeStack;
}
