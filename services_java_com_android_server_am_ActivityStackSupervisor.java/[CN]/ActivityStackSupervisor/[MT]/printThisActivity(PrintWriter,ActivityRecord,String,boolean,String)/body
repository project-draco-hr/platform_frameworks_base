{
  if (activity != null) {
    if (dumpPackage == null || dumpPackage.equals(activity.packageName)) {
      if (needSep) {
        pw.println();
        needSep=false;
      }
      pw.print(prefix);
      pw.println(activity);
    }
  }
  return needSep;
}
