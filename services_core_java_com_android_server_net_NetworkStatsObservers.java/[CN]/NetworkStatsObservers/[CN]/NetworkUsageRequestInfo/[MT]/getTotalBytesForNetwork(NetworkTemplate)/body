{
  NetworkStats stats=mCollection.getSummary(template,Long.MIN_VALUE,Long.MAX_VALUE,mAccessLevel,mCallingUid);
  if (LOGV) {
    Slog.v(TAG,"Netstats for " + template + ": "+ stats);
  }
  return stats.getTotalBytes();
}
