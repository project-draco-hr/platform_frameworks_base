{
  delete(2,4);
  assertEquals(ITEM_COUNT - 2,model.getItemCount());
  model.finalizeDeletion(new Runnable(){
    @Override public void run(){
    }
  }
);
  assertEquals(ITEM_COUNT - 2,model.getItemCount());
}
