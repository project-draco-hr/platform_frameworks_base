{
  assertRunOnIoThread();
  for (int i=0; i < retryCount; ++i) {
    if (nativeSendCecCommand(mNativePtr,address,address,EMPTY_BODY) == HdmiConstants.SEND_RESULT_SUCCESS) {
      return true;
    }
  }
  return false;
}
