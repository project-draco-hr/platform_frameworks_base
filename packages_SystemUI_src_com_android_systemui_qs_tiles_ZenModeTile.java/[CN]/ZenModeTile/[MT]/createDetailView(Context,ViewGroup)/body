{
  final Context themedContext=new ContextThemeWrapper(mContext,R.style.QSAccentTheme);
  final ZenModeDetail v=(ZenModeDetail)LayoutInflater.from(themedContext).inflate(R.layout.qs_zen_mode_detail,root,false);
  v.init(this);
  return v;
}
