{
  int hash=KEY_DEFAULT;
  if (parallelSyncsEnabled()) {
    for (    String key : extras.keySet()) {
      String mapping=key + " " + extras.get(key).toString();
      hash+=mapping.hashCode();
    }
  }
  return hash;
}
