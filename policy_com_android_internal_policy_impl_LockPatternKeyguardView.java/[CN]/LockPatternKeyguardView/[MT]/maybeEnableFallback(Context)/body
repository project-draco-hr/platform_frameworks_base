{
  mNumAccounts=AccountManager.get(context).getAccounts().length;
  if (mNumAccounts > 0) {
    String[] features=new String[]{"saml"};
    AccountManager.get(context).getAccountsByTypeAndFeatures("com.google",features,this,null);
  }
}
