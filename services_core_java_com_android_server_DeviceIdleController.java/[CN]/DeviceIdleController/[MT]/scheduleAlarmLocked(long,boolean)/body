{
  if (mSigMotionSensor == null) {
    return;
  }
  mNextAlarmTime=SystemClock.elapsedRealtime() + delay;
  mAlarmManager.set(wakeup ? AlarmManager.ELAPSED_REALTIME_WAKEUP : AlarmManager.ELAPSED_REALTIME,mNextAlarmTime,mAlarmIntent);
}
