{
  super.onCreate(savedInstanceState);
  if (getFragmentManager().findFragmentByTag(TAG_TUNER) == null) {
    getFragmentManager().beginTransaction().replace(R.id.content_frame,new TunerFragment(),TAG_TUNER).commit();
  }
}
