{
  final MotionEvent cancel=MotionEvent.obtain(mDownTime,SystemClock.uptimeMillis(),MotionEvent.ACTION_CANCEL,0,0,0);
  cancel.setAction(MotionEvent.ACTION_CANCEL);
  mTarget.onTouchEvent(cancel);
  cancel.recycle();
}
