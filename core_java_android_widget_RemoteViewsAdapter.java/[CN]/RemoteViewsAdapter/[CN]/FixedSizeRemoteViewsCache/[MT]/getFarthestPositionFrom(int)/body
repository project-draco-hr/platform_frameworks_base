{
  int maxDist=0;
  int maxDistIndex=-1;
  for (  int i : mIndexRemoteViews.keySet()) {
    int dist=Math.abs(i - pos);
    if (dist > maxDist) {
      maxDistIndex=i;
      maxDist=dist;
    }
  }
  return maxDistIndex;
}
