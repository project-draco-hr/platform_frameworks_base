{
  final int i=mWindows.indexOf(pos);
  if (localLOGV || DEBUG_FOCUS)   Log.v(TAG,"Adding window " + window + " at "+ (i + 1)+ " of "+ mWindows.size()+ " (after "+ pos+ ")");
  mWindows.add(i + 1,window);
}
