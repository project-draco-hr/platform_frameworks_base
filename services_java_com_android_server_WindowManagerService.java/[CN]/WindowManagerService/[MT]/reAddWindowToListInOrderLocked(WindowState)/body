{
  addWindowToListInOrderLocked(win,false);
  int wpos=mWindows.indexOf(win);
  if (wpos >= 0) {
    if (DEBUG_WINDOW_MOVEMENT)     Log.v(TAG,"ReAdd removing from " + wpos + ": "+ win);
    mWindows.remove(wpos);
    reAddWindowLocked(wpos,win);
  }
}
