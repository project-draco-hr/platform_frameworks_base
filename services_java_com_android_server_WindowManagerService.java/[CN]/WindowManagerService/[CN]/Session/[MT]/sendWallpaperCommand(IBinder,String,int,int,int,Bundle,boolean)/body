{
synchronized (mWindowMap) {
    long ident=Binder.clearCallingIdentity();
    try {
      return sendWindowWallpaperCommandLocked(windowForClientLocked(this,window),action,x,y,z,extras,sync);
    }
  finally {
      Binder.restoreCallingIdentity(ident);
    }
  }
}
