{
  if (localLOGV)   Slog.v(TAG,"IWindow getPendingMotionEvent called for " + window);
  if (ENABLE_NATIVE_INPUT_DISPATCH) {
    throw new IllegalStateException("Should not be called anymore.");
  }
  return mKeyWaiter.finishedKey(this,window,false,KeyWaiter.RETURN_PENDING_TRACKBALL);
}
