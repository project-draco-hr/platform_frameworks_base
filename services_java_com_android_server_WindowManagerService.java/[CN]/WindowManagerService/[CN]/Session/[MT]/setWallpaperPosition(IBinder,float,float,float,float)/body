{
synchronized (mWindowMap) {
    long ident=Binder.clearCallingIdentity();
    try {
      setWindowWallpaperPositionLocked(windowForClientLocked(this,window),x,y,xStep,yStep);
    }
  finally {
      Binder.restoreCallingIdentity(ident);
    }
  }
}
