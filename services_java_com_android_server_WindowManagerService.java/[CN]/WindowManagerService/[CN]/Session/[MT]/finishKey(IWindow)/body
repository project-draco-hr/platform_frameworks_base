{
  if (localLOGV)   Slog.v(TAG,"IWindow finishKey called for " + window);
  if (ENABLE_NATIVE_INPUT_DISPATCH) {
    throw new IllegalStateException("Should not be called anymore.");
  }
  mKeyWaiter.finishedKey(this,window,false,KeyWaiter.RETURN_NOTHING);
}
