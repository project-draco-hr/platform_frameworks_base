{
  return (mAttrs.flags & mAttrs.FLAG_COMPATIBLE_WINDOW) != 0 && mAttrs.width == mAttrs.FILL_PARENT && mAttrs.height == mAttrs.FILL_PARENT && ((mFrame.right - mFrame.right) < screenWidth || (mFrame.bottom - mFrame.top) < screenHeight);
}
