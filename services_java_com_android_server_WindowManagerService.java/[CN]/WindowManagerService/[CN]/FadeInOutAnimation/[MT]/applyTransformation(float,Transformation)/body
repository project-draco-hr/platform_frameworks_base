{
  float x=interpolatedTime;
  if (!mFadeIn) {
    x=1.0f - x;
  }
  if (x < 0.5) {
    t.getMatrix().setTranslate(mWidth,0);
  }
 else {
    t.getMatrix().setTranslate(0,0);
    t.setAlpha((x - 0.5f) * 2);
  }
}
