{
  if (window.paused) {
    if (DEBUG_INPUT) {
      Slog.v(TAG,"Resuming WindowToken " + window);
    }
    window.paused=false;
    setUpdateInputWindowsNeededLw();
    updateInputWindowsLw();
  }
}
