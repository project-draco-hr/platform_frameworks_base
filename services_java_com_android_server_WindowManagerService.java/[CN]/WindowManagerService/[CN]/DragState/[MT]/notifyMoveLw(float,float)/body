{
  WindowState touchedWin=getTouchedWinAtPointLw(x,y);
  try {
    if ((touchedWin != mTargetWindow) && (mTargetWindow != null)) {
      if (DEBUG_DRAG) {
        Slog.d(TAG,"sending DRAG_EXITED to " + mTargetWindow);
      }
      DragEvent evt=DragEvent.obtain(DragEvent.ACTION_DRAG_EXITED,0,0,null,null);
      mTargetWindow.mClient.dispatchDragEvent(evt);
      evt.recycle();
    }
    if (touchedWin != null) {
      if (DEBUG_DRAG) {
        Slog.d(TAG,"sending DRAG_LOCATION to " + touchedWin);
      }
      DragEvent evt=DragEvent.obtain(DragEvent.ACTION_DRAG_LOCATION,x,y,null,null);
      touchedWin.mClient.dispatchDragEvent(evt);
      evt.recycle();
    }
  }
 catch (  RemoteException e) {
    Slog.w(TAG,"can't send drag notification to windows");
  }
  mTargetWindow=touchedWin;
}
