{
  if (activeSyncContext != null) {
    updateActiveSync(activeSyncContext.mSyncOperation.account,activeSyncContext.mSyncOperation.authority,activeSyncContext.mStartTime);
  }
 else {
    updateActiveSync(null,null,null);
  }
}
