{
synchronized (mAuthorities) {
    if (Log.isLoggable(TAG,Log.VERBOSE)) {
      Log.v(TAG,"removeActiveSync: account=" + syncInfo.account + " auth="+ syncInfo.authority);
    }
    mCurrentSyncs.remove(syncInfo);
  }
  reportActiveChange();
}
