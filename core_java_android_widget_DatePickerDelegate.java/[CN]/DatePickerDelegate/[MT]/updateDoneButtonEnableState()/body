{
  if (mShowDoneButton) {
    final boolean enabled=mCurrentDate.equals(mMinDate) || mCurrentDate.equals(mMaxDate) || (mCurrentDate.after(mMinDate) && mCurrentDate.before(mMaxDate));
    mDoneButton.setEnabled(enabled);
  }
}
