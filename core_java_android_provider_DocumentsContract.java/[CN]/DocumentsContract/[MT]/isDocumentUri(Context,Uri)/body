{
  final List<String> paths=uri.getPathSegments();
  if (paths.size() >= 2 && (PATH_DOCUMENT.equals(paths.get(0)) || PATH_VIA.equals(paths.get(0)))) {
    return isDocumentsProvider(context,uri.getAuthority());
  }
  return false;
}
