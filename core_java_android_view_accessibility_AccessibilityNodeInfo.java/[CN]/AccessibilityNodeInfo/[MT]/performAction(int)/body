{
  enforceSealed();
  if (!canPerformRequestOverConnection(mAccessibilityViewId)) {
    return false;
  }
  try {
    return mConnection.performAccessibilityAction(mAccessibilityWindowId,mAccessibilityViewId,action);
  }
 catch (  RemoteException e) {
  }
  return false;
}
