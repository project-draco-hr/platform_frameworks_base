{
  enforceSealed();
  final int childAccessibilityViewId=mChildAccessibilityIds.get(index);
  try {
    return mConnection.findAccessibilityNodeInfoByAccessibilityId(mAccessibilityWindowId,childAccessibilityViewId);
  }
 catch (  RemoteException e) {
    return null;
  }
}
