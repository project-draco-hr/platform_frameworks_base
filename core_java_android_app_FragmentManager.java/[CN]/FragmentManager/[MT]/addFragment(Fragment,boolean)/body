{
  if (mAdded == null) {
    mAdded=new ArrayList<Fragment>();
  }
  mAdded.add(fragment);
  makeActive(fragment);
  fragment.mAdded=true;
  if (moveToStateNow) {
    moveToState(fragment,mCurState,0,0);
  }
}
