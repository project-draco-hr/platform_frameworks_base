{
synchronized (mStartLock) {
    if (!mStarted) {
      return;
    }
    try {
      if (speechRate > 0) {
        mITts.setSpeechRate((int)(speechRate * 100));
      }
    }
 catch (    RemoteException e) {
      mStarted=false;
      initTts();
    }
  }
}
