{
synchronized (mStartLock) {
    if (!mStarted) {
      return;
    }
    try {
      mITts.setSpeechRate(speechRate);
    }
 catch (    RemoteException e) {
      mStarted=false;
      initTts();
    }
  }
}
