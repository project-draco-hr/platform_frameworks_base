{
synchronized (mStartLock) {
    if (!mStarted) {
      return;
    }
    try {
      mITts.setLanguage(language);
    }
 catch (    RemoteException e) {
      mStarted=false;
      initTts();
    }
  }
}
