{
  draw(nativeCanvas,paint,false,new GcSnapshot.Drawable(){
    public void draw(    Graphics2D graphics,    Paint_Delegate paint){
      graphics.drawLine((int)startX,(int)startY,(int)stopX,(int)stopY);
    }
  }
);
}
