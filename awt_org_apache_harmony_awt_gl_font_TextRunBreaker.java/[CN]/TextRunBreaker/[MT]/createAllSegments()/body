{
  if (!haveAllSegments && (logical2segment == null || logical2segment.length != segmentsEnd - segmentsStart)) {
    resetSegments();
    createSegments(segmentsStart,segmentsEnd);
  }
  haveAllSegments=true;
}
