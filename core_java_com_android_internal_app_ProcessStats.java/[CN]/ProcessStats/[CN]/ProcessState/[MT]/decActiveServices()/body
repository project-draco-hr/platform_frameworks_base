{
  if (mCommonProcess != this) {
    mCommonProcess.decActiveServices();
  }
  mNumActiveServices--;
  if (mNumActiveServices < 0) {
    throw new IllegalStateException("Proc active services underrun: pkg=" + mPackage + " uid="+ mUid+ " name="+ mName);
  }
}
