{
  if (!mIsConnected) {
    return false;
  }
  try {
    return mTagService.isPresent(mTag.mServiceHandle);
  }
 catch (  RemoteException e) {
    attemptDeadServiceRecovery(e);
    return false;
  }
}
