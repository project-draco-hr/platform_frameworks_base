{
  final boolean changed=super.setVisible(visible,restart);
  if (visible) {
    if (restart || changed) {
      setFrame(restart ? 0 : mCurFrame,true,mAnimating);
    }
  }
 else {
    unscheduleSelf(this);
  }
  return changed;
}
