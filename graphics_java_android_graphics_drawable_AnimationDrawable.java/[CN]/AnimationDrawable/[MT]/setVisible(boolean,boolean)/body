{
  boolean changed=super.setVisible(visible,restart);
  if (visible) {
    if (changed || restart) {
      setFrame(0,true,mCurFrame >= 0);
    }
  }
 else {
    unscheduleSelf(this);
  }
  return changed;
}
