{
  mColorStart=color1;
  mColorEnd=color2;
  if (rect != null) {
    mRect=new Rect(rect.left + BORDER_WIDTH / 2,rect.top + BORDER_WIDTH / 2,rect.right - BORDER_WIDTH / 2,rect.bottom - BORDER_WIDTH / 2);
  }
}
