{
  mActivity=launchActivity("com.android.frameworks.coretests",ListOfInternalSelectionViews.class,ListOfInternalSelectionViews.getBundleFor(mNumItems,mNumRowsPerItem,mScreenHeightFactor));
  mListView=mActivity.getListView();
  mListView.setVerticalFadingEdgeEnabled(true);
  mListView.setFadingEdgeLength(10);
  ensureNotInTouchMode();
}
