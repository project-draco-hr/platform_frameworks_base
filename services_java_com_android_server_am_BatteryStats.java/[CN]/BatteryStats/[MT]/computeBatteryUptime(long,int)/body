{
switch (which) {
case STATS_LOADED:
    return mBatteryUptime + (curTime - mBatteryUptimeStart);
case STATS_LAST:
  return mBatteryLastUptime;
case STATS_CURRENT:
return (curTime - mBatteryUptimeStart);
}
return 0;
}
