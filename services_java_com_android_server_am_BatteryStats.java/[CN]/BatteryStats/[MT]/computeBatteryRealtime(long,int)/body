{
switch (which) {
case STATS_LOADED:
    return mBatteryRealtime + (curTime - mBatteryRealtimeStart);
case STATS_LAST:
  return mBatteryLastRealtime;
case STATS_CURRENT:
return (curTime - mBatteryRealtimeStart);
}
return 0;
}
