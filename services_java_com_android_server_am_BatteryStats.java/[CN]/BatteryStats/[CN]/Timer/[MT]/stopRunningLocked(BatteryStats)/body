{
  if (nesting == 1) {
    nesting=0;
    long heldTime=stats.getBatteryUptimeLocked() - startTime;
    if (heldTime != 0) {
      totalTime+=heldTime;
    }
 else {
      count--;
    }
  }
 else {
    nesting--;
  }
}
