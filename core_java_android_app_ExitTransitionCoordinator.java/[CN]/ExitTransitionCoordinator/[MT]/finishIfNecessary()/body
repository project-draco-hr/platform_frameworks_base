{
  if (mIsReturning && mExitNotified && (mSharedElements.isEmpty() || mSharedElements.get(0).getVisibility() == View.INVISIBLE)) {
    mActivity.finish();
    mActivity.overridePendingTransition(0,0);
    mActivity=null;
  }
  if (!mIsReturning && mExitNotified) {
    mActivity=null;
  }
}
