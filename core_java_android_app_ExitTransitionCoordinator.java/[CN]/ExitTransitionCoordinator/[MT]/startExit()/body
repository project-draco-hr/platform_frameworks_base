{
  if (!mIsExitStarted) {
    mIsExitStarted=true;
    if (getDecor() != null) {
      getDecor().suppressLayout(true);
    }
    startTransition(new Runnable(){
      @Override public void run(){
        beginTransitions();
      }
    }
);
  }
}
