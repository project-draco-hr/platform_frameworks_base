{
  if (!isWalledGardenTestEnabled()) {
    if (VDBG)     Slog.v(WWSM_TAG,"Skipping walled garden check - disabled");
    return false;
  }
  long waitTime=waitTime(MIN_WALLED_GARDEN_INTERVAL_MS,mLastWalledGardenCheckTime);
  if (waitTime > 0) {
    if (DBG) {
      Slog.d(WWSM_TAG,"Skipping walled garden check - wait " + waitTime + " ms.");
    }
    return false;
  }
  return true;
}
