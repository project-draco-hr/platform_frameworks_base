{
  return (!newVis && !newAnim) ? StatusBarManager.WINDOW_STATE_HIDDEN : (!oldVis && newVis && newAnim) ? StatusBarManager.WINDOW_STATE_SHOWING : (oldVis && newVis && !oldAnim&& newAnim) ? StatusBarManager.WINDOW_STATE_HIDING : -1;
}
