{
  if (keyPrefix != null) {
    String[] keys;
    final int N=original.length;
    keys=new String[N];
    for (int i=0; i < N; i++) {
      keys[i]=keyPrefix + ':' + original[i];
    }
    return keys;
  }
 else {
    return original;
  }
}
