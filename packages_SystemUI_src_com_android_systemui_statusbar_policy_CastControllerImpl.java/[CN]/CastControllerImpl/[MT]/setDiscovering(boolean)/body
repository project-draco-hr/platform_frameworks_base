{
  if (request) {
    mMediaRouter.addCallback(MediaRouter.ROUTE_TYPE_REMOTE_DISPLAY,mMediaCallback,MediaRouter.CALLBACK_FLAG_REQUEST_DISCOVERY);
  }
 else {
    mMediaRouter.removeCallback(mMediaCallback);
  }
}
