{
  if (Constants.DebugFlags.App.EnableBackgroundTaskLoading) {
    Console.log(Constants.DebugFlags.App.TaskDataLoader,"[RecentsTaskLoader|unloadTask]",t);
    mLoadQueue.removeTask(t);
    t.icon=mDefaultIcon;
    t.thumbnail=mDefaultThumbnail;
  }
}
