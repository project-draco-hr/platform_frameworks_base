{
  if ((mSystemUiVisibility & STATUS_OR_NAV_TRANSIENT) != 0 && event.getAction() == MotionEvent.ACTION_OUTSIDE && event.getX() == 0 && event.getY() == 0 && !mRemoteInputController.isRemoteInputActive()) {
    userAutohide();
  }
}
