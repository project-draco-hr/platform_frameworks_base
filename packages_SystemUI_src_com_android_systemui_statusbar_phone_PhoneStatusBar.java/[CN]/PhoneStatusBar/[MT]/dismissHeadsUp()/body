{
  if (mCurrentlyInterruptingNotification == null)   return;
  try {
    mBarService.onNotificationClear(mCurrentlyInterruptingNotification.getPackageName(),mCurrentlyInterruptingNotification.getTag(),mCurrentlyInterruptingNotification.getId());
  }
 catch (  android.os.RemoteException ex) {
  }
}
