{
  if (!areLightsOn())   return;
  if (!isDeviceProvisioned())   return;
  if (!notificationIsForCurrentProfiles(n))   return;
  if (n.getNotification().tickerText != null && mStatusBarWindow != null && mStatusBarWindow.getWindowToken() != null) {
    if (0 == (mDisabled & (StatusBarManager.DISABLE_NOTIFICATION_ICONS | StatusBarManager.DISABLE_NOTIFICATION_TICKER))) {
      mTicker.addEntry(n);
    }
  }
}
