{
  if (0 != (mSystemUiVisibility & View.SYSTEM_UI_FLAG_HIDE_NAVIGATION)) {
    try {
      mBarService.setSystemUiVisibility(mSystemUiVisibility & ~View.SYSTEM_UI_FLAG_HIDE_NAVIGATION);
    }
 catch (    RemoteException ex) {
    }
  }
}
