{
  final int DEFAULT=-1;
  final int shelfOverride=Settings.System.getInt(mContext.getContentResolver(),"demo_app_shelf",DEFAULT);
  if (shelfOverride != DEFAULT) {
    return shelfOverride != 0;
  }
  return mContext.getResources().getBoolean(R.bool.config_enableAppShelf);
}
