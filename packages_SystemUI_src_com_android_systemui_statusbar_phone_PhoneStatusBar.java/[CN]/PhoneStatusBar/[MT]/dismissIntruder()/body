{
  if (mCurrentlyIntrudingNotification == null)   return;
  try {
    mBarService.onNotificationClear(mCurrentlyIntrudingNotification.getPkg(),mCurrentlyIntrudingNotification.getTag(),mCurrentlyIntrudingNotification.getId());
  }
 catch (  android.os.RemoteException ex) {
  }
}
