{
  mClearButton.setVisibility(mNotificationData.hasClearableItems() ? View.VISIBLE : View.INVISIBLE);
  mNoNotificationsTitle.setVisibility(mNotificationData.size() > 0 ? View.GONE : View.VISIBLE);
}
