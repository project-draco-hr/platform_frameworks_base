{
  int output[]=new int[length / 4];
  int i, j;
  int limit=length + posn + offset;
  for (i=0, j=offset + posn; j < limit; i++, j+=4)   output[i]=((input[j]) & 0xff) | (((input[j + 1]) & 0xff) << 8) | (((input[j + 2]) & 0xff) << 16)| (((input[j + 3]) & 0xff) << 24);
  return output;
}
