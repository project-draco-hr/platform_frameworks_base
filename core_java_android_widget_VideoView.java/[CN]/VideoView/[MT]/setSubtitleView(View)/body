{
  if (mSubtitleView == view) {
    return;
  }
  if (mSubtitleView != null) {
    removeOverlay(mSubtitleView);
  }
  mSubtitleView=view;
  if (mSubtitleView != null) {
    addOverlay(mSubtitleView);
  }
}
