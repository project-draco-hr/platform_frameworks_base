{
synchronized (mLock) {
    if (mCurrentVrService == null) {
      return;
    }
    updateCurrentVrServiceLocked(mVrModeEnabled,mCurrentVrService.getComponent(),mCurrentVrService.getUserId());
  }
}
