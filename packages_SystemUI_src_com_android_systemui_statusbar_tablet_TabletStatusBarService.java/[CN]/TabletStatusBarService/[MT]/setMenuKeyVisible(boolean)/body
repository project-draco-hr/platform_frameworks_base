{
  if (DEBUG) {
    Slog.d(TAG,(visible ? "showing" : "hiding") + " the MENU button");
  }
  setViewVisibility(mMenuButton,visible ? View.VISIBLE : View.INVISIBLE,visible ? R.anim.navigation_in : R.anim.navigation_out);
}
