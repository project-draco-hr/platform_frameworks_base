{
  if (l == LayoutAlgorithm.TEXT_AUTOSIZING) {
    throw new IllegalArgumentException("WebViewClassic does not support TEXT_AUTOSIZING layout mode");
  }
  if (mLayoutAlgorithm != l) {
    mLayoutAlgorithm=l;
    postSync();
  }
}
