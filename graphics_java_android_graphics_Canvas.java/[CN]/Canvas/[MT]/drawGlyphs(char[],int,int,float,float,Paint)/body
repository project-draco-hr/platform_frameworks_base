{
  if ((index | count | (index + count)| (glyphs.length - index - count)) < 0) {
    throw new IndexOutOfBoundsException();
  }
  native_drawGlyphs(mNativeCanvas,glyphs,index,count,x,y,paint.mBidiFlags,paint.mNativePaint);
}
