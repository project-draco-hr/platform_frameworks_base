{
synchronized (mScoClients) {
    int size=mScoClients.size();
    for (int i=0; i < size; i++) {
      if (mScoClients.get(i).getBinder() != exceptBinder)       mScoClients.get(i).clearCount(stopSco);
    }
  }
}
