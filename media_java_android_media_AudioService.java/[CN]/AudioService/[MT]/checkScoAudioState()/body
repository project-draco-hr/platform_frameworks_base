{
  if (mBluetoothHeadset != null && mBluetoothHeadsetDevice != null && mScoAudioState != SCO_STATE_ACTIVE_INTERNAL && mBluetoothHeadset.getAudioState(mBluetoothHeadsetDevice) != BluetoothHeadset.STATE_AUDIO_DISCONNECTED) {
    mScoAudioState=SCO_STATE_ACTIVE_EXTERNAL;
  }
}
