{
  Log.i(TAG,"Clear remote control display");
synchronized (mRCStack) {
synchronized (mCurrentRcLock) {
      mCurrentRcClientGen++;
      setNewRcClient_syncAfRcsCurrc(mCurrentRcClientGen,null,true);
    }
  }
}
