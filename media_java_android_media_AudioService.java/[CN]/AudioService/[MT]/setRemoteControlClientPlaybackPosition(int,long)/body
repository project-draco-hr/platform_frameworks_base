{
synchronized (mRCStack) {
synchronized (mCurrentRcLock) {
      if (mCurrentRcClientGen != generationId) {
        return;
      }
    }
  }
  sendMsg(mAudioHandler,MSG_RCC_SEEK_REQUEST,SENDMSG_REPLACE,generationId,0,new Long(timeMs),0);
}
