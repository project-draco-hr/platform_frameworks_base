{
  final float scale=compatInfo.applicationScale;
  final Configuration overrideConfigCopy=(overrideConfiguration != null) ? new Configuration(overrideConfiguration) : null;
  final ResourcesKey key=new ResourcesKey(resDir,displayId,overrideConfigCopy,scale);
  mActiveResources.remove(key);
}
