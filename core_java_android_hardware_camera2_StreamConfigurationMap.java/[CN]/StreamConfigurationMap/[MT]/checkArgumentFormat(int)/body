{
  if (!ImageFormat.isPublicFormat(format) && !PixelFormat.isPublicFormat(format)) {
    throw new IllegalArgumentException(String.format("format %x was not defined in either ImageFormat or PixelFormat",format));
  }
  return format;
}
