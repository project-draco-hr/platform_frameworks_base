{
  if (mCancelable && mCanceledOnTouchOutside && event.getAction() == MotionEvent.ACTION_DOWN && isOutOfBounds(event) && mDecor != null && mShowing) {
    cancel();
    return true;
  }
  return false;
}
