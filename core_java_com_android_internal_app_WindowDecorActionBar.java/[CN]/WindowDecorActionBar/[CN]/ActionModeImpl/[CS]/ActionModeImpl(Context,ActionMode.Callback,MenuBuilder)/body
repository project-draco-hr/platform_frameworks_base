{
  mActionModeContext=context;
  mCallback=callback;
  mMenu=menuBuilder == null ? new MenuBuilder(context).setDefaultShowAsAction(MenuItem.SHOW_AS_ACTION_IF_ROOM) : menuBuilder;
  mMenu.setCallback(this);
}
