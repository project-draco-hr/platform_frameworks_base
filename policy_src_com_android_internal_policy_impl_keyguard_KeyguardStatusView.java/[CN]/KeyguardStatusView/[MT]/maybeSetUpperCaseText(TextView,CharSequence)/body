{
  if (KeyguardViewManager.USE_UPPER_CASE && textView.getId() != R.id.owner_info) {
    textView.setText(text != null ? text.toString().toUpperCase() : null);
  }
 else {
    textView.setText(text);
  }
}
