{
synchronized (mLock) {
    if (mCurrentDreamToken == token && mCurrentDreamCanDoze && mCurrentDreamDozeHardware == null && mMcuHal != null) {
      mCurrentDreamDozeHardware=new DozeHardwareWrapper();
      return mCurrentDreamDozeHardware;
    }
    return null;
  }
}
