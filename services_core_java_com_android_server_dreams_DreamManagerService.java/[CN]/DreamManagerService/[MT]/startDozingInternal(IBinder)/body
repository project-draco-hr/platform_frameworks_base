{
  if (DEBUG) {
    Slog.d(TAG,"Dream requested to start dozing: " + token);
  }
synchronized (mLock) {
    if (mCurrentDreamToken == token && mCurrentDreamCanDoze && !mCurrentDreamIsDozing) {
      mCurrentDreamIsDozing=true;
      mDozeWakeLock.acquire();
    }
  }
}
