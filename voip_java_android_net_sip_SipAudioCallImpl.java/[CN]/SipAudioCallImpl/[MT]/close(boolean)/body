{
  if (closeRtp)   stopCall(RELEASE_SOCKET);
  stopRingbackTone();
  stopRinging();
  mInCall=false;
  mHold=false;
  mSessionId=-1L;
  mErrorCode=null;
  mErrorMessage=null;
  if (mSipSession != null) {
    try {
      mSipSession.setListener(null);
    }
 catch (    RemoteException e) {
    }
    mSipSession=null;
  }
}
