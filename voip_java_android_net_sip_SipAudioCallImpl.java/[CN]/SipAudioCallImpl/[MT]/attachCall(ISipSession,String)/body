{
  mSipSession=session;
  try {
    mPeerSd=new SdpSessionDescription(sessionDescription);
    session.setListener(this);
  }
 catch (  Throwable e) {
    Log.e(TAG,"attachCall()",e);
    throwSipException(e);
  }
}
