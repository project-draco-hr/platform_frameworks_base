{
  int ret=0, sum=0;
  mEncoding=mDefaultEncoding;
  ret=parseItem0(offset);
  if (ret != PARSE_ERROR) {
    sum+=ret;
    return sum;
  }
  ret=parseItem1(offset);
  if (ret != PARSE_ERROR) {
    sum+=ret;
    return sum;
  }
  ret=parseItem2(offset);
  if (ret != PARSE_ERROR) {
    sum+=ret;
    return sum;
  }
  return PARSE_ERROR;
}
