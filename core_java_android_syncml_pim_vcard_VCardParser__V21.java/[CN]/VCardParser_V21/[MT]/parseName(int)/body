{
  int ret=0;
  ret=parseXWord(offset);
  if (ret != PARSE_ERROR) {
    return ret;
  }
  String word=getWord(offset).toUpperCase();
  if (mName.contains(word)) {
    return word.length();
  }
  return PARSE_ERROR;
}
