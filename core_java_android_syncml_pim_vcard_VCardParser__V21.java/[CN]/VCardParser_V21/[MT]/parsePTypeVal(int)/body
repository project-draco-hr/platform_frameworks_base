{
  int ret=0, sum=0;
  ret=parseKnownType(offset);
  if (ret != PARSE_ERROR) {
    sum+=ret;
    return sum;
  }
  ret=parseXWord(offset);
  if (ret != PARSE_ERROR) {
    sum+=ret;
    return sum;
  }
  sum+=ret;
  return sum;
}
