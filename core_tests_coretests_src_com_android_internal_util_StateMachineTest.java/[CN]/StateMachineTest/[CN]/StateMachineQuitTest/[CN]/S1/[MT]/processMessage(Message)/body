{
switch (message.what) {
case TEST_CMD_1:
{
      Log.d(TAG,"TEST_CMD_1");
      sleep(500);
      quit();
      break;
    }
default :
{
    Log.d(TAG,"default what=" + message.what);
    break;
  }
}
return HANDLED;
}
