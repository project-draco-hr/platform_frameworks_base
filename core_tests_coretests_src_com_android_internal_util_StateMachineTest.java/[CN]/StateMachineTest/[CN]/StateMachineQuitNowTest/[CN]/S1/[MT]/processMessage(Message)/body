{
switch (message.what) {
case TEST_CMD_1:
{
      log("TEST_CMD_1");
      sleep(500);
      quitNow();
      break;
    }
default :
{
    log("default what=" + message.what);
    break;
  }
}
return HANDLED;
}
