{
  if (mListener == null)   return;
  proxy(new Runnable(){
    public void run(){
      try {
        mListener.onCalling(session);
      }
 catch (      Throwable t) {
        handle(t,"onCalling()");
      }
    }
  }
);
}
