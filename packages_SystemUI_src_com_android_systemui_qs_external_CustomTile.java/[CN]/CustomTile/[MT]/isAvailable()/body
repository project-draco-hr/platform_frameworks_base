{
  try {
    ServiceInfo info=mContext.getPackageManager().getServiceInfo(mComponent,PackageManager.MATCH_ENCRYPTION_AWARE_AND_UNAWARE);
    return true;
  }
 catch (  Exception e) {
    return false;
  }
}
