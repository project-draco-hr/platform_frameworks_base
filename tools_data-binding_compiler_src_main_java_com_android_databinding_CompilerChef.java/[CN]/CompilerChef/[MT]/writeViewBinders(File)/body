{
  ensureDataBinder();
  if (folder == null) {
    folder=new File(mOutputBaseDir.getAbsolutePath() + "/" + mResourceBundle.getAppPackage().replace('.','/')+ "/generated");
  }
  mDataBinder.writeBinders(folder);
}
