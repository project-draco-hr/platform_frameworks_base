{
  final int count=mCallbacks.size();
  for (int i=0; i < count; i++) {
    CallbackInfo cbi=mCallbacks.get(i);
    if ((cbi.flags & CALLBACK_FLAG_PERFORM_ACTIVE_SCAN) != 0 && (cbi.type & type) != 0) {
      return true;
    }
  }
  return false;
}
