{
  for (int i=0; i < values.length; i++) {
    if (values[i].getCode() == intValue) {
      return values[i];
    }
  }
  throw new IllegalArgumentException(intValue + " does not map to any ErrorCode.");
}
