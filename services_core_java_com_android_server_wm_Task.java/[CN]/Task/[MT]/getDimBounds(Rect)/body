{
  if (useCurrentBounds()) {
    if (inFreeformWorkspace() && getMaxVisibleBounds(out)) {
      return;
    }
    out.set(mBounds);
    return;
  }
  mStack.getDisplayContent().getLogicalDisplayRect(out);
}
