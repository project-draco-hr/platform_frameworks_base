{
  if (V) {
    Slog.d(TAG,"Responding to packet " + internalId + " externalId "+ externalId+ " and val "+ responseVal);
  }
  mTarget.sendMessage(obtainMessage(DNS_PING_RESULT,internalId,responseVal));
}
