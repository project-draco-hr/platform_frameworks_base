{
  return (isConstraintsSatisfied() || (!job.isPeriodic() && hasDeadlineConstraint() && (satisfiedConstraints & CONSTRAINT_DEADLINE) != 0)) && (satisfiedConstraints & CONSTRAINT_APP_NOT_IDLE) != 0 && (satisfiedConstraints & CONSTRAINT_DEVICE_NOT_DOZING) != 0;
}
