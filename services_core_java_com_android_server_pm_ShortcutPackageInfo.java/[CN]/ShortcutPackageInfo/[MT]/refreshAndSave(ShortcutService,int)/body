{
  final PackageInfo pi=s.getPackageInfo(mPackageName,userId,true);
  if (pi == null) {
    Slog.w(TAG,"Package not found: " + mPackageName);
    return;
  }
  mVersionCode=pi.versionCode;
  mSigHashes=hashSignatureArray(pi.signatures);
  s.scheduleSaveUser(userId);
}
