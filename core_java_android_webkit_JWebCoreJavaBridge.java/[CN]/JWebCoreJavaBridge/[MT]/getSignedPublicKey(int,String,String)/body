{
  if (mCurrentMainWebView != null) {
    return CertTool.getSignedPublicKey(mCurrentMainWebView.getContext(),index,challenge);
  }
 else {
    Log.e(LOGTAG,"There is no active WebView for getSignedPublicKey");
    return "";
  }
}
