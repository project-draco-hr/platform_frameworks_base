{
  if (LocalScreen.CLEAR_TASK.equals(intent.getAction())) {
    setResult(RESULT_OK);
  }
 else {
    setResult(RESULT_CANCELED,new Intent().setAction("New intent received " + intent + ", expecting action "+ TestedScreen.CLEAR_TASK));
  }
  finish();
}
