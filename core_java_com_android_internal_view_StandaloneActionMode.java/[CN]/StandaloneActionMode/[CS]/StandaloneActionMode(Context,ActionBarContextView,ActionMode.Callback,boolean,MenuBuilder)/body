{
  mContext=context;
  mContextView=view;
  mCallback=callback;
  mMenu=(menuBuilder != null) ? menuBuilder : new MenuBuilder(view.getContext()).setDefaultShowAsAction(MenuItem.SHOW_AS_ACTION_IF_ROOM);
  mMenu.setCallback(this);
  mFocusable=isFocusable;
}
