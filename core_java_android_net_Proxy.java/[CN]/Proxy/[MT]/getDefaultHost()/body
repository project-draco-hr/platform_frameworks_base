{
  String host=SystemProperties.get("net.gprs.http-proxy");
  if ((host != null) && (host.length() != 0)) {
    Uri u=Uri.parse(host);
    host=u.getHost();
    return host;
  }
 else {
    return null;
  }
}
