{
synchronized (WindowManagerImpl.class) {
    if (sDefaultWindowManager == null) {
      sDefaultWindowManager=new WindowManagerImpl(new WindowManagerState(),null,null);
    }
    return sDefaultWindowManager;
  }
}
