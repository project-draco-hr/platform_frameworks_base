{
  final float fromY=mDownY[i];
  final long elapsed=time - mDownTime[i];
  if (DEBUG)   Slog.d(TAG,"pointer " + mDownPointerId[i] + " moved from.y="+ fromY+ " to.y="+ y+ " in "+ elapsed);
  return fromY <= mSwipeStartThreshold && y > fromY + mSwipeEndThreshold && elapsed < SWIPE_TIMEOUT_MS;
}
