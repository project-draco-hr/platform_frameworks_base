{
  Vector<Cue> cues=mCues.get(timeMs);
  if (cues == null) {
    cues=new Vector<Cue>(2);
    mCues.put(timeMs,cues);
  }
 else   if (cues.contains(cue)) {
    return false;
  }
  cues.add(cue);
  return true;
}
