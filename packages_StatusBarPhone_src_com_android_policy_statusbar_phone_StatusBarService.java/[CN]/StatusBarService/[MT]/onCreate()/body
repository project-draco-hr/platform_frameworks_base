{
  addStatusBarView();
  mBarService=IStatusBar.Stub.asInterface(ServiceManager.getService(Context.STATUS_BAR_SERVICE));
  try {
    mBarService.registerStatusBar(mBar);
  }
 catch (  RemoteException ex) {
  }
}
