{
  return new Alignment(){
    @Override public int getAlignmentValue(    View view,    int viewSize){
      if (view == null) {
        return UNDEFINED;
      }
      Alignment alignment=view.isLayoutRtl() ? a2 : a1;
      return alignment.getAlignmentValue(view,viewSize);
    }
  }
;
}
