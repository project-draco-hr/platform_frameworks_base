{
  try {
    final int size=mImpl.size();
    for (int i=0; i < size; i++) {
      if (mImpl.get(i) == 0) {
        return i;
      }
    }
  }
 catch (  IOException e) {
    Slog.e(LOG_TAG,"Error reading generation memory array",e);
  }
  return -1;
}
