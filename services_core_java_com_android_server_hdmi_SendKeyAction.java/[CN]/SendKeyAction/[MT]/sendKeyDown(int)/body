{
  byte[] keycodeAndParam=getCecKeycodeAndParam(keycode);
  if (keycodeAndParam == null) {
    return;
  }
  sendCommand(HdmiCecMessageBuilder.buildUserControlPressed(getSourceAddress(),mTargetAddress,keycodeAndParam));
}
