{
  byte[] keyCodeAndParam=getCecKeyCodeAndParam(keyCode);
  if (keyCodeAndParam == null) {
    return;
  }
  sendCommand(HdmiCecMessageBuilder.buildUserControlPressed(mSourceAddress,mTargetAddress,keyCodeAndParam));
}
