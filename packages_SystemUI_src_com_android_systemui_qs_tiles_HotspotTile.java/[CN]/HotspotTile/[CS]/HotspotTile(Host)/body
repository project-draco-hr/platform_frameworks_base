{
  super(host);
  mController=host.getHotspotController();
  mKeyguard=host.getKeyguardMonitor();
  mTimeToShowTile=MILLIS_PER_DAY * mContext.getResources().getInteger(R.integer.days_to_show_hotspot);
}
