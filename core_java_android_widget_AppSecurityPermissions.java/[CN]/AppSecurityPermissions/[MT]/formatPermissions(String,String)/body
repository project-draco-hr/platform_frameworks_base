{
  if (groupDesc == null) {
    return permDesc;
  }
  groupDesc=canonicalizeGroupDesc(groupDesc);
  if (permDesc == null) {
    return groupDesc;
  }
  return String.format(mPermFormat,groupDesc,permDesc);
}
