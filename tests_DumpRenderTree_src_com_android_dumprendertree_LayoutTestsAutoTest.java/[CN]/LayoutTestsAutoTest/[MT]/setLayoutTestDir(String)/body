{
  if (name == null)   throw new AssertionError("Layout test directory cannot be null.");
  layoutTestDir=HTMLHostActivity.LAYOUT_TESTS_ROOT + name;
  Log.v("LayoutTestsAutoTest"," Only running the layout tests : " + layoutTestDir);
}
