{
  try {
    int route=sService.getCardEmulationRoute();
    return route == CardEmulationRoute.ROUTE_OFF ? sRouteOff : sRouteOnWhenScreenOn;
  }
 catch (  RemoteException e) {
    Log.e(TAG,"",e);
    return sRouteOff;
  }
}
