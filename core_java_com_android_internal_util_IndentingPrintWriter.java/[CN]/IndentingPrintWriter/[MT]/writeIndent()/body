{
  if (mEmptyLine) {
    mEmptyLine=false;
    if (mBuilder.length() != 0) {
      if (mCurrent == null) {
        mCurrent=mBuilder.toString().toCharArray();
      }
      super.write(mCurrent,0,mCurrent.length);
    }
  }
}
