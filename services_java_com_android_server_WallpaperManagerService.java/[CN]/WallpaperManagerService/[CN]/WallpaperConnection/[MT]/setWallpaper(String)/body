{
synchronized (mLock) {
    if (mWallpaperConnection == this) {
      ParcelFileDescriptor pfd=updateWallpaperBitmapLocked(name);
      if (pfd != null) {
        saveSettingsLocked();
      }
      return pfd;
    }
    return null;
  }
}
