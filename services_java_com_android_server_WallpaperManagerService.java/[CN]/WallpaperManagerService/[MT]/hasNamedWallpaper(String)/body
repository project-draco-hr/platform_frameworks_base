{
synchronized (mLock) {
    for (int i=0; i < mWallpaperMap.size(); i++) {
      WallpaperData wd=mWallpaperMap.valueAt(i);
      if (name.equals(wd.name)) {
        return true;
      }
    }
  }
  return false;
}
