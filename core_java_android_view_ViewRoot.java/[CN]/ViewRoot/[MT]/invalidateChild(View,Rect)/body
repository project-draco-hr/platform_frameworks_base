{
  checkThread();
  if (LOCAL_LOGV)   Log.v(TAG,"Invalidate child: " + dirty);
  if (mCurScrollY != 0) {
    mTempRect.set(dirty);
    mTempRect.offset(0,-mCurScrollY);
    dirty=mTempRect;
  }
  mDirty.union(dirty);
  if (!mWillDrawSoon) {
    scheduleTraversals();
  }
}
