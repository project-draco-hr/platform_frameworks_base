{
  if (DEBUG_DRAW)   Log.v(TAG,"Resized " + this + ": w="+ w+ " h="+ h+ " reportDraw="+ reportDraw);
  Message msg=obtainMessage(reportDraw ? RESIZED_REPORT : RESIZED);
  msg.arg1=w;
  msg.arg2=h;
  sendMessage(msg);
}
