{
  super.setUp();
  getActivity();
  mMediaTestUtil=new MediaTestUtil("/sdcard/VideoEditorMediaServerMemoryLog.txt",this.getName(),"mediaserver");
  mVideoEditorHelper=new VideoEditorHelper();
  final String projectPath=mVideoEditorHelper.createRandomFile(PROJECT_LOCATION);
  mVideoEditor=mVideoEditorHelper.createVideoEditor(projectPath);
}
