{
  if (!isValid()) {
    return;
  }
  if (mDisplayList != null) {
    mDisplayList.destroyDisplayListData();
    mDisplayList=null;
  }
  mRenderer.onLayerDestroyed(this);
  mRenderer=null;
  mFinalizer.release();
  mFinalizer=null;
}
