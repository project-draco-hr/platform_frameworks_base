{
  try {
    Looper.prepare();
synchronized (this) {
      mChoreographer=Choreographer.getInstance();
      mChoreographer.postFrameCallback(this);
    }
    Looper.loop();
  }
  finally {
    releaseRenderer();
  }
synchronized (this) {
    mChoreographer=null;
  }
}
