{
synchronized (this) {
    if (mRenderer != null) {
      mRenderer.setContentDrawBounds(0,0,0,0);
      mRenderer.removeRenderNode(mFrameAndBackdropNode);
      mRenderer=null;
      pingRenderLocked();
    }
  }
}
