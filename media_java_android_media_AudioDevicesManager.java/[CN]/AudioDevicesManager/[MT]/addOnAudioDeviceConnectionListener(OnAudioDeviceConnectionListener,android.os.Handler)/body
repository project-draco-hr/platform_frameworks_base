{
  if (listener != null && !mDeviceConnectionListeners.containsKey(listener)) {
synchronized (mDeviceConnectionListeners) {
      mDeviceConnectionListeners.put(listener,new NativeEventHandlerDelegate(listener,handler));
    }
  }
}
