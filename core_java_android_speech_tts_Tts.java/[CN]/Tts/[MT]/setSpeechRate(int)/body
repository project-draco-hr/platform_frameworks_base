{
synchronized (startLock) {
    if (!started) {
      return;
    }
    try {
      itts.setSpeechRate(speechRate);
    }
 catch (    RemoteException e) {
      started=false;
      initTts();
    }
  }
}
