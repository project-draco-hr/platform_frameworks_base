{
  if (target == 0) {
    mCallback.goToUnlockScreen();
  }
 else   if (target == 2) {
    toggleRingMode();
    doSilenceRingToast();
    mUnlockWidgetMethods.updateResources();
    mCallback.pokeWakelock();
  }
}
