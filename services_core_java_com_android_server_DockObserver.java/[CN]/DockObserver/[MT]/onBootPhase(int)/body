{
  if (phase == PHASE_ACTIVITY_MANAGER_READY) {
synchronized (mLock) {
      mSystemReady=true;
      if (mReportedDockState != Intent.EXTRA_DOCK_STATE_UNDOCKED) {
        updateLocked();
      }
    }
  }
}
