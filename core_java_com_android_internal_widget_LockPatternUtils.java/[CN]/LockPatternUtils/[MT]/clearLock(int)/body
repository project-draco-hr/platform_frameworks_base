{
  setLong(PASSWORD_TYPE_KEY,DevicePolicyManager.PASSWORD_QUALITY_UNSPECIFIED,userHandle);
  try {
    getLockSettings().setLockPassword(null,userHandle);
    getLockSettings().setLockPattern(null,userHandle);
  }
 catch (  RemoteException e) {
  }
  if (userHandle == UserHandle.USER_OWNER) {
    updateEncryptionPassword(StorageManager.CRYPT_TYPE_DEFAULT,null);
  }
  getDevicePolicyManager().setActivePasswordState(DevicePolicyManager.PASSWORD_QUALITY_UNSPECIFIED,0,0,0,0,0,0,0,userHandle);
  onAfterChangingPassword(userHandle);
}
