{
  mContext=context;
  mContentResolver=context.getContentResolver();
  mMultiUserMode=context.checkCallingOrSelfPermission(Manifest.permission.INTERACT_ACROSS_USERS_FULL) == PackageManager.PERMISSION_GRANTED;
}
