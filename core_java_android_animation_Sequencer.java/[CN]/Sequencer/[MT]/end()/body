{
  mCanceled=true;
  if (mSortedNodes.size() != mNodes.size()) {
    sortNodes();
  }
  if (mSortedNodes.size() > 0) {
    for (    Node node : mSortedNodes) {
      node.animation.end();
    }
  }
}
