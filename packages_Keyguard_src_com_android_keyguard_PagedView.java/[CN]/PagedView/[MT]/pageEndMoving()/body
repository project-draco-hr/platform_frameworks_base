{
  if (DEBUG_WARP)   Log.v(TAG,"pageEndMoving(" + mIsPageMoving + ")");
  if (mIsPageMoving) {
    mIsPageMoving=false;
    if (mPageWarpIndex != -1) {
      if (mPageSwapIndex != -1) {
        swapPages(mPageSwapIndex,mPageWarpIndex);
        resetPageWarp();
      }
      onPageEndWarp();
    }
    onPageEndMoving();
  }
}
