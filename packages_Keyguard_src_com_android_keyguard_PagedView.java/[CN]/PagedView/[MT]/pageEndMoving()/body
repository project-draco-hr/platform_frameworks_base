{
  if (DEBUG_WARP)   Log.v(TAG,"pageEndMoving(" + mIsPageMoving + ")");
  if (mIsPageMoving) {
    mIsPageMoving=false;
    if (isWarping()) {
      if (mPageSwapIndex != -1) {
        swapPages(mPageSwapIndex,mPageWarpIndex);
      }
      onPageEndWarp();
      resetPageWarp();
    }
    onPageEndMoving();
  }
}
