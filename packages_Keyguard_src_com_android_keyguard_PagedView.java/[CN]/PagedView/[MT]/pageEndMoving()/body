{
  if (DEBUG_WARP)   Log.v(TAG,"pageEndMoving(" + mIsPageMoving + ")");
  if (mIsPageMoving) {
    mIsPageMoving=false;
    if (isWarping()) {
      dispatchOnPageEndWarp();
      mWarpPageExposed=false;
    }
    onPageEndMoving();
  }
}
