{
  if (DEBUG_WARP)   Log.v(TAG,"pageEndMoving(" + mIsPageMoving + ")");
  if (mIsPageMoving) {
    mIsPageMoving=false;
    if (mPageSwapIndex != -1) {
      swapPages(mPageSwapIndex,getPageCount() - 1);
      onPageEndWarp();
      mPageSwapIndex=-1;
    }
    onPageEndMoving();
  }
}
