{
  if (DEBUG_WARP)   Log.v(TAG,"pageBeginMoving(" + mIsPageMoving + ")");
  if (!mIsPageMoving) {
    mIsPageMoving=true;
    if (mPageSwapIndex != -1) {
      onPageBeginWarp();
      swapPages(mPageSwapIndex,getPageCount() - 1);
    }
    onPageBeginMoving();
  }
}
