{
  mContext.enforceCallingPermission(Manifest.permission.WRITE_SMS,"Import MMS message");
  if (getAppOpsManager().noteOp(AppOpsManager.OP_WRITE_SMS,Binder.getCallingUid(),callingPkg) != AppOpsManager.MODE_ALLOWED) {
    return null;
  }
  return getServiceGuarded().importMultimediaMessage(callingPkg,pdu,messageId,timestampSecs,seen,read);
}
