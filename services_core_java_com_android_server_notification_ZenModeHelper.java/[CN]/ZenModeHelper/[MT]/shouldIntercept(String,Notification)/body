{
  if (mZenMode != Global.ZEN_MODE_OFF) {
    if (isAlarm(pkg,n)) {
      return false;
    }
    if (isCall(pkg,n)) {
      return !mConfig.allowCalls;
    }
    if (isMessage(pkg,n)) {
      return !mConfig.allowMessages;
    }
    return true;
  }
  return false;
}
