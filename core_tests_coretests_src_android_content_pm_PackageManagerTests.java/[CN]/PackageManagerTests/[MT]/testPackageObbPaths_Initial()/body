{
  InstallParams ip=sampleInstallFromRawResource(PackageManager.INSTALL_INTERNAL,false);
  try {
    final PackageManager pm=getPm();
    assertEquals("Initial obb paths should be null",null,pm.getPackageObbPaths(ip.pkg.packageName));
  }
  finally {
    cleanUpInstall(ip);
  }
}
