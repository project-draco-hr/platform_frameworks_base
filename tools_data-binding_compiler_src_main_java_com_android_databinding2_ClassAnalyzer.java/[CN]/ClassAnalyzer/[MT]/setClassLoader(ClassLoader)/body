{
  if (sClassAnalyzer != null) {
    throw new IllegalStateException("class analyzer is already created, you cannot " + "change class loader after that");
  }
  L.d("setting class loader to %s",classLoader);
  sClassLoader=classLoader;
}
