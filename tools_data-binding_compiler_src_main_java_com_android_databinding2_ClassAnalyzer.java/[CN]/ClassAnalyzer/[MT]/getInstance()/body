{
  if (sClassAnalyzer == null) {
    Preconditions.checkNotNull(sClassLoader,"cannot access class analyzer before class loader is ready");
    sClassAnalyzer=new ClassAnalyzer(sClassLoader,false);
  }
  return sClassAnalyzer;
}
