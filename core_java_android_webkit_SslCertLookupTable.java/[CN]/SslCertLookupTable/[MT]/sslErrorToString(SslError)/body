{
  String host;
  try {
    host=new URL(error.getUrl()).getHost();
  }
 catch (  MalformedURLException e) {
    return null;
  }
  return "primary error: " + error.getPrimaryError() + " certificate: "+ error.getCertificate()+ " on host: "+ host;
}
