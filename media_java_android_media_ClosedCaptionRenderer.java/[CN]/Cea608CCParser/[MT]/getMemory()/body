{
switch (mMode) {
case MODE_POP_ON:
    return mNonDisplay;
case MODE_TEXT:
  return mTextMem;
case MODE_PAINT_ON:
case MODE_ROLL_UP:
return mDisplay;
default :
Log.w(TAG,"unrecoginized mode: " + mMode);
}
return mDisplay;
}
