{
  NetworkStatsHistory stats=mIfaceStats.get(info);
  if (stats == null) {
    stats=new NetworkStatsHistory(info.networkType,info.identity,UID_ALL,SUMMARY_BUCKET_DURATION);
    mIfaceStats.put(info,stats);
  }
  return stats;
}
