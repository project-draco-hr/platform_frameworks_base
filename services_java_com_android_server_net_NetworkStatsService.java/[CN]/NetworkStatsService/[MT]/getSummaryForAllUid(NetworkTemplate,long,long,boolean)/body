{
  final NetworkStats stats=mUidRecorder.getOrLoadCompleteLocked().getSummary(template,start,end);
  if (includeTags) {
    final NetworkStats tagStats=mUidTagRecorder.getOrLoadCompleteLocked().getSummary(template,start,end);
    stats.combineAllValues(tagStats);
  }
  return stats;
}
