{
  if (tag == TAG_NONE) {
    return mUidRecorder.getOrLoadCompleteLocked().getHistory(template,uid,set,tag,fields);
  }
 else {
    return mUidTagRecorder.getOrLoadCompleteLocked().getHistory(template,uid,set,tag,fields);
  }
}
