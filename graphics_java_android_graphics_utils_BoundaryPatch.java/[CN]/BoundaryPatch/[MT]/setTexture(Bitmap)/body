{
  if (mTexture != texture) {
    if (mTexture == null || mTexture.getWidth() != texture.getWidth() || mTexture.getHeight() != texture.getHeight()) {
      mDirty=true;
    }
    mTexture=texture;
    mPaint.setShader(new BitmapShader(texture,Shader.TileMode.CLAMP,Shader.TileMode.CLAMP));
  }
}
