{
  android.hardware.UsbAccessory accessory=intent.getParcelableExtra(android.hardware.UsbManager.EXTRA_ACCESSORY);
  if (accessory == null) {
    return null;
  }
 else {
    return new UsbAccessory(accessory);
  }
}
