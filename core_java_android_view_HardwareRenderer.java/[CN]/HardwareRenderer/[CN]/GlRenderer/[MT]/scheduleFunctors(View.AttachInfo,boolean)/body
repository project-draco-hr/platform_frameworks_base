{
  mFunctorsRunnable.attachInfo=attachInfo;
  if (!attachInfo.mHandler.hasCallbacks(mFunctorsRunnable)) {
    attachInfo.mHandler.postDelayed(mFunctorsRunnable,delayed ? FUNCTOR_PROCESS_DELAY : 0);
  }
}
