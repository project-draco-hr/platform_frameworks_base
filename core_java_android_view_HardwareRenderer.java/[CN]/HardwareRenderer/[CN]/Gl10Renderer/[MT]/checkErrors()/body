{
  if (isEnabled()) {
    int err=mEgl.eglGetError();
    if (err != EGL10.EGL_SUCCESS) {
      destroy();
      if (err != EGL11.EGL_CONTEXT_LOST) {
        setRequested(false);
      }
    }
  }
}
