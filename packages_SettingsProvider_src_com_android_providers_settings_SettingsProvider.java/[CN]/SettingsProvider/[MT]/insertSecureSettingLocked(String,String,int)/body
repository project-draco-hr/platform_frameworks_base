{
  if (DEBUG) {
    Slog.v(LOG_TAG,"insertSecureSettingLocked(" + name + ", "+ value+ ", "+ requestingUserId+ ")");
  }
  return mutateSecureSettingLocked(name,value,requestingUserId,MUTATION_OPERATION_INSERT);
}
