{
  if ("gservices".equals(args.table) && getContext().checkCallingOrSelfPermission(WRITE_GSERVICES_PERMISSION) != PackageManager.PERMISSION_GRANTED) {
    throw new SecurityException("Cannot write gservices table");
  }
}
