{
  if (DEBUG) {
    Slog.v(LOG_TAG,"updateSecureSettingLocked(" + name + ", "+ value+ ", "+ requestingUserId+ ")");
  }
  return mutateSecureSettingLocked(name,value,requestingUserId,MUTATION_OPERATION_UPDATE);
}
