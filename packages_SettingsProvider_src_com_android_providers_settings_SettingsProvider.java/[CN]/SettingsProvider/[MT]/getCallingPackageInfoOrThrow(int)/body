{
  try {
    PackageInfo packageInfo=mPackageManager.getPackageInfo(getCallingPackage(),0,userId);
    if (packageInfo != null) {
      return packageInfo;
    }
  }
 catch (  RemoteException e) {
  }
  throw new IllegalStateException("Calling package doesn't exist");
}
