{
  if (DEBUG) {
    Slog.v(LOG_TAG,"updateSystemSettingLocked(" + name + ", "+ value+ ", "+ requestingUserId+ ")");
  }
  return mutateSystemSettingLocked(name,value,requestingUserId,MUTATION_OPERATION_UPDATE);
}
