{
  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
    assertEquals(mBindingObject.getDivider(),((ColorDrawable)mView.getDividerDrawable()).getColor());
    changeValues();
    assertEquals(mBindingObject.getDivider(),((ColorDrawable)mView.getDividerDrawable()).getColor());
  }
}
