{
  FrameType floatT=FrameType.single(float.class);
  return new Signature().addInputPort("sharpness",Signature.PORT_REQUIRED,floatT).addOutputPort("avg",Signature.PORT_REQUIRED,floatT).disallowOtherPorts();
}
