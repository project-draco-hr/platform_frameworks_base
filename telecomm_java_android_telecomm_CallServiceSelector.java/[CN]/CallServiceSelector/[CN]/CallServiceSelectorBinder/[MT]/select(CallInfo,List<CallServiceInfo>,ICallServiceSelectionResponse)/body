{
  long ident=Binder.clearCallingIdentity();
  try {
    response.setSelectedCallServiceInfos(CallServiceSelector.this.select(callInfo,callServiceInfos));
  }
  finally {
    Binder.restoreCallingIdentity(ident);
  }
}
