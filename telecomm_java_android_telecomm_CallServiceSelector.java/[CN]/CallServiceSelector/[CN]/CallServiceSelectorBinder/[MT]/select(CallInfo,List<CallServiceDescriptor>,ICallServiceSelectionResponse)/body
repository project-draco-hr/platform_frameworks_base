{
  long ident=Binder.clearCallingIdentity();
  try {
    response.setSelectedCallServiceDescriptors(CallServiceSelector.this.select(callInfo,descriptors));
  }
  finally {
    Binder.restoreCallingIdentity(ident);
  }
}
