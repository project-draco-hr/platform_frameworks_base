{
  long ident=Binder.clearCallingIdentity();
  try {
    response.setSelectedCallServiceIds(CallServiceSelector.this.select(callInfo,callServiceIds));
  }
  finally {
    Binder.restoreCallingIdentity(ident);
  }
}
