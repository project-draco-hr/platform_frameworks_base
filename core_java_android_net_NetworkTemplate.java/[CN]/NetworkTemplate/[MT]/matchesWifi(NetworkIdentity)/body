{
switch (ident.mType) {
case TYPE_WIFI:
    if (mNetworkId == null) {
      return true;
    }
 else {
      return Objects.equal(mNetworkId,ident.mNetworkId);
    }
case TYPE_WIFI_P2P:
  return mNetworkId == null;
default :
return false;
}
}
