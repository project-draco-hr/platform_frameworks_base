{
  final File file=mStorage.get(documentId);
  final long fileSize=file.length();
  if (file == null || !file.delete())   throw new FileNotFoundException();
synchronized (mWriteLock) {
    mStorageUsedBytes-=fileSize;
  }
  notifyStorageChanged();
}
