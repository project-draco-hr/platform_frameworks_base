{
  checkArgument(doc.isContainer());
  mState.pushDocument(doc);
  final int anim=(mState.hasLocationChanged() && mState.stack.size() > 1) ? ANIM_DOWN : ANIM_NONE;
  refreshCurrentRootAndDirectory(anim);
}
