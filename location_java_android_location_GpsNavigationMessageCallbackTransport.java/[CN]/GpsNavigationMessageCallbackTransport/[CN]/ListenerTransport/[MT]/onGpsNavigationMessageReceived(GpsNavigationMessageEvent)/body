{
  ListenerOperation<GpsNavigationMessageEvent.Callback> operation=new ListenerOperation<GpsNavigationMessageEvent.Callback>(){
    @Override public void execute(    GpsNavigationMessageEvent.Callback callback) throws RemoteException {
      callback.onGpsNavigationMessageReceived(event);
    }
  }
;
  foreach(operation);
}
