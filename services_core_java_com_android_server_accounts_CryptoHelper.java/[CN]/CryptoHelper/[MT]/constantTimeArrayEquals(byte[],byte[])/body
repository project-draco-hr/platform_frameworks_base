{
  if (a == null || b == null) {
    return a == b;
  }
  if (a.length != b.length) {
    return false;
  }
  boolean isEqual=true;
  for (int i=0; i < b.length; i++) {
    isEqual&=(a[i] == b[i]);
  }
  return isEqual;
}
