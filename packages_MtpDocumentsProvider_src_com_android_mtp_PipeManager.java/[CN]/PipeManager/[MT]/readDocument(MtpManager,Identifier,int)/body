{
  final Task task=new Task(){
    @Override byte[] getBytes() throws IOException {
      return model.getObject(identifier.mDeviceId,identifier.mObjectHandle,expectedSize);
    }
  }
;
  mExecutor.execute(task);
  return task.getReadingFileDescriptor();
}
