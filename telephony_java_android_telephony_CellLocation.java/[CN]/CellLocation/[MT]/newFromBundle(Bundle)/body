{
  if (RILConstants.CDMA_PHONE == SystemProperties.getInt(Settings.Secure.CURRENT_ACTIVE_PHONE,0)) {
    return new CdmaCellLocation(bundle);
  }
 else {
    return new GsmCellLocation(bundle);
  }
}
