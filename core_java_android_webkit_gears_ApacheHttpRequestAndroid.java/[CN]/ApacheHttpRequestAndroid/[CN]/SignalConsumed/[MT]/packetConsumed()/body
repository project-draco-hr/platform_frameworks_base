{
  while (mConsumed) {
    try {
      wait();
    }
 catch (    InterruptedException e) {
      if (LOGV_ENABLED) {
        Log.i(LOG_TAG,"InterruptedException while indicating " + "that the DataPacket has been consumed: " + e);
      }
    }
  }
  mConsumed=true;
  notify();
}
