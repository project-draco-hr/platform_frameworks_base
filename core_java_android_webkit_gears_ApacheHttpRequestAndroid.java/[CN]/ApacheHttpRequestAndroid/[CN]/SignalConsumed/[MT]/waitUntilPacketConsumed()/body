{
  while (!mConsumed) {
    try {
      wait();
    }
 catch (    InterruptedException e) {
      if (LOGV_ENABLED) {
        Log.i(LOG_TAG,"InterruptedException while waiting " + "until a DataPacket is consumed: " + e);
      }
    }
  }
  mConsumed=false;
  notify();
}
