{
  while (mEmpty) {
    try {
      wait();
    }
 catch (    InterruptedException e) {
      if (LOGV_ENABLED) {
        Log.i(LOG_TAG,"InterruptedException while getting " + "a DataPacket in the Buffer: " + e);
      }
    }
  }
  mEmpty=true;
  notify();
  return mPacket;
}
