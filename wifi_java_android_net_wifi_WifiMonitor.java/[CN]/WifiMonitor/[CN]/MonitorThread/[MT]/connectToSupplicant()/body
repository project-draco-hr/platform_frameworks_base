{
  int connectTries=0;
  while (true) {
synchronized (mWifiStateTracker) {
      if (WifiNative.connectToSupplicant()) {
        return true;
      }
    }
    if (connectTries++ < 3) {
      nap(5);
    }
 else {
      break;
    }
  }
  return false;
}
