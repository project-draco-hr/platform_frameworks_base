{
  if (mCallback != null) {
    int deltaZoomLevel=-numLevels;
    return mCallback.onDragZoom(deltaZoomLevel,getZoomRingCenterXInOwnerCoordinates(),getZoomRingCenterYInOwnerCoordinates(),(float)startAngle / ZoomRing.RADIAN_INT_MULTIPLIER,(float)curAngle / ZoomRing.RADIAN_INT_MULTIPLIER);
  }
  return false;
}
