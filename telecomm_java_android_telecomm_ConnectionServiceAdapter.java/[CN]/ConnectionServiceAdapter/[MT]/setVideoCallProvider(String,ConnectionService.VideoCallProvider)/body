{
  for (  IConnectionServiceAdapter adapter : mAdapters) {
    try {
      adapter.setVideoCallProvider(callId,videoCallProvider == null ? null : videoCallProvider.getInterface());
    }
 catch (    RemoteException e) {
    }
  }
}
