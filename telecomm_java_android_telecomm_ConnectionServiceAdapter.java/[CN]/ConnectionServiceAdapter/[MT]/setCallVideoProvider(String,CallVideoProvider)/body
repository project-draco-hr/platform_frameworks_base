{
  for (  IConnectionServiceAdapter adapter : mAdapters) {
    try {
      adapter.setCallVideoProvider(callId,callVideoProvider == null ? null : callVideoProvider.getInterface());
    }
 catch (    RemoteException e) {
    }
  }
}
