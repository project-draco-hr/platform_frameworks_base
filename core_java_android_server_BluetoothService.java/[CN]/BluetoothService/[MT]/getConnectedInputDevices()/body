{
  mContext.enforceCallingOrSelfPermission(BLUETOOTH_PERM,"Need BLUETOOTH permission");
  List<BluetoothDevice> devices=lookupInputDevicesMatchingStates(new int[]{BluetoothInputDevice.STATE_CONNECTED});
  return devices;
}
