{
  super.setUp();
  mUpdateMonitor=new MockUpdateMonitor(getContext());
  mLockPatternUtils=new MockLockPatternUtils(getContext());
  mKeyguardViewCallback=new MockKeyguardCallback();
  mLPKV=new TestableLockPatternKeyguardView(getContext(),mKeyguardViewCallback,mUpdateMonitor,mLockPatternUtils,new KeyguardWindowController(){
    public void setNeedsInput(    boolean needsInput){
    }
  }
);
}
