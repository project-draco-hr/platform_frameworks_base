{
  final File[] files=directory.listFiles(filter);
  if (!ArrayUtils.isEmpty(files)) {
    for (    File file : files) {
      if (file.isDirectory()) {
        FileUtils.deleteContents(file);
        file.delete();
      }
 else       if (file.isFile()) {
        file.delete();
      }
    }
  }
}
