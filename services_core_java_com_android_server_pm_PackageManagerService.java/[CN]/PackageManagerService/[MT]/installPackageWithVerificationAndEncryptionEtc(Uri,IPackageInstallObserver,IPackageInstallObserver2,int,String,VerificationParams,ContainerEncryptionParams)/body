{
  if (observer == null && observer2 == null) {
    throw new IllegalArgumentException("No install observer supplied");
  }
  installPackageWithVerificationEncryptionAndAbiOverrideEtc(packageURI,observer,observer2,flags,installerPackageName,verificationParams,encryptionParams,null);
}
