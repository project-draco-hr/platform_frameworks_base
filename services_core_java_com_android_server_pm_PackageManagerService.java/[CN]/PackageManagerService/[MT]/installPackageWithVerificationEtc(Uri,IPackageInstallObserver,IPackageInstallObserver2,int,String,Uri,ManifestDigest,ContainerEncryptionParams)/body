{
  VerificationParams verificationParams=new VerificationParams(verificationURI,null,null,VerificationParams.NO_UID,manifestDigest);
  installPackageWithVerificationAndEncryptionEtc(packageURI,observer,observer2,flags,installerPackageName,verificationParams,encryptionParams);
}
