{
  if (((flagMask & PackageManager.FLAG_PERMISSION_POLICY_FIXED) != 0 || (flagValues & PackageManager.FLAG_PERMISSION_POLICY_FIXED) != 0) && getCallingUid() != Process.SYSTEM_UID) {
    throw new SecurityException("Only the system can modify policy flags");
  }
}
