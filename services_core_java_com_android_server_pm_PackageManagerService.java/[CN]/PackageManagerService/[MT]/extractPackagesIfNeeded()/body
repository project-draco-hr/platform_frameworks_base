{
  enforceSystemOrRoot("Only the system can request package extraction");
  if (mUseJitProfiles) {
    ArraySet<String> pkgs=getOptimizablePackages();
    if (pkgs != null) {
      for (      String pkg : pkgs) {
        performDexOpt(pkg,null,false,true,false);
      }
    }
  }
}
