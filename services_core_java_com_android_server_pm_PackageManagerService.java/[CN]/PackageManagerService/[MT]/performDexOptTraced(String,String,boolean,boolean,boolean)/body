{
  Trace.traceBegin(TRACE_TAG_PACKAGE_MANAGER,"dexopt");
  try {
    return performDexOptInternal(packageName,instructionSet,useProfiles,extractOnly,force);
  }
  finally {
    Trace.traceEnd(TRACE_TAG_PACKAGE_MANAGER);
  }
}
