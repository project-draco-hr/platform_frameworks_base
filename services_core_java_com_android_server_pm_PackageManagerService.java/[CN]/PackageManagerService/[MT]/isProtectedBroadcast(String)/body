{
synchronized (mPackages) {
    if (mProtectedBroadcasts.contains(actionName)) {
      return true;
    }
 else     if (actionName != null && actionName.startsWith("android.net.netmon.lingerExpired")) {
      return true;
    }
  }
  return false;
}
