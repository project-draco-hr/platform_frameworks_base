{
  if (mFragment != null) {
    collapsePanels();
    final Intent intent=new Intent();
    intent.setComponent(new ComponentName("com.android.settings","com.android.settings." + mFragment));
    startSettingsActivity(intent);
    return true;
  }
  return false;
}
