{
  if (mContext.getPackageManager().checkSignatures(mMyUid,Binder.getCallingUid()) != PackageManager.SIGNATURE_MATCH) {
    throw new SecurityException("Caller must be signed with the system key!");
  }
}
