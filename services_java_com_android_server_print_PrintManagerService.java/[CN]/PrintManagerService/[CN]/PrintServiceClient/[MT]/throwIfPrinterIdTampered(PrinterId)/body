{
  if (printerId == null || printerId.getServiceComponentName() == null || !printerId.getServiceComponentName().equals(mComponentName)) {
    throw new IllegalArgumentException("Invalid printer id: " + printerId);
  }
}
