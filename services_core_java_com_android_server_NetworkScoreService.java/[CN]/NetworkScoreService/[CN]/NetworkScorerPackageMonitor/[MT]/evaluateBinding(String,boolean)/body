{
  if (mRegisteredPackage.equals(scorerPackageName)) {
    if (DBG) {
      Log.d(TAG,"Evaluating binding for: " + scorerPackageName + ", forceUnbind="+ forceUnbind);
    }
    final NetworkScorerAppData activeScorer=NetworkScorerAppManager.getActiveScorer(mContext);
    if (activeScorer == null) {
      Log.i(TAG,"Package " + mRegisteredPackage + " is no longer valid, disabling scoring.");
      setScorerInternal(null);
    }
 else     if (activeScorer.mScoringServiceClassName == null) {
      unbindFromScoringServiceIfNeeded();
    }
 else {
      if (forceUnbind) {
        unbindFromScoringServiceIfNeeded();
      }
      bindToScoringServiceIfNeeded(activeScorer);
    }
  }
}
