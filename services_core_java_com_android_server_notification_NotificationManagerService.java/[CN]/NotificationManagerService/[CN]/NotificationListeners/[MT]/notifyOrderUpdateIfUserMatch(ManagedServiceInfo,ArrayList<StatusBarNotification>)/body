{
  ArrayList<String> keys=new ArrayList<String>(sbns.size());
  for (  StatusBarNotification sbn : sbns) {
    if (info.enabledAndUserMatches(sbn.getUserId())) {
      keys.add(sbn.getKey());
    }
  }
  final INotificationListener listener=(INotificationListener)info.service;
  try {
    listener.onNotificationOrderUpdate(new NotificationOrderUpdate(keys.toArray(new String[keys.size()])));
  }
 catch (  RemoteException ex) {
    Log.e(TAG,"unable to notify listener (ranking update): " + listener,ex);
  }
}
