{
  if (useCurrentBounds()) {
    if (!mAdjustedBounds.isEmpty()) {
      out.set(mAdjustedBounds);
    }
 else {
      out.set(mBounds);
    }
    return;
  }
  mDisplayContent.getLogicalDisplayRect(out);
}
