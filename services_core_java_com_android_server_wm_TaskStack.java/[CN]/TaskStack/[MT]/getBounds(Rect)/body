{
  if (useCurrentBounds()) {
    if (!mContentBounds.isEmpty()) {
      out.set(mAdjustedBounds);
    }
 else {
      out.set(mBounds);
    }
    return;
  }
  mDisplayContent.getLogicalDisplayRect(out);
}
