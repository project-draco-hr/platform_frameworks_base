{
  if (mFullscreen || mStackId == DOCKED_STACK_ID || mStackId == PINNED_STACK_ID || mDisplayContent == null || mDisplayContent.getDockedStackLocked() != null) {
    return true;
  }
  return false;
}
