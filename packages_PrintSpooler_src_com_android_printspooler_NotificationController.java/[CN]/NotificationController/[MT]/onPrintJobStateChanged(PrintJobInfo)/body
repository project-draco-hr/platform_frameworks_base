{
  if (DEBUG) {
    Log.i(LOG_TAG,"onPrintJobStateChanged() printJobId: " + printJob.getId() + " state:"+ PrintJobInfo.stateToString(printJob.getState()));
  }
switch (printJob.getState()) {
case PrintJobInfo.STATE_QUEUED:
case PrintJobInfo.STATE_STARTED:
{
      createPrintingNotification(printJob);
    }
  break;
case PrintJobInfo.STATE_FAILED:
{
  createFailedNotification(printJob);
}
break;
case PrintJobInfo.STATE_COMPLETED:
case PrintJobInfo.STATE_CANCELED:
{
removeNotification(printJob.getId());
}
break;
case PrintJobInfo.STATE_BLOCKED:
{
createBlockedNotification(printJob);
}
break;
}
}
