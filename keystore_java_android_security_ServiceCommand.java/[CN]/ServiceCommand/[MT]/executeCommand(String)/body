{
  if (!writeCommand(cmd)) {
    Log.e(mTag,"write command failed? reconnect!");
    if (!connect() || !writeCommand(cmd)) {
      return null;
    }
  }
  if (readReply()) {
    return new String(buf,0,buflen);
  }
 else {
    return null;
  }
}
