{
  Log.i(LOG_TAG,"setMockGeolocationPosition(): " + "latitude=" + latitude + " longitude="+ longitude+ " accuracy="+ accuracy);
  mLayoutTestsExecutor.setMockGeolocationPosition(latitude,longitude,accuracy);
}
