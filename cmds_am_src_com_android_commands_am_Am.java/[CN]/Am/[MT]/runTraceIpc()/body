{
  String op=nextArgRequired();
  if (op.equals("start")) {
    runTraceIpcStart();
  }
 else   if (op.equals("stop")) {
    runTraceIpcStop();
  }
 else {
    showError("Error: unknown command '" + op + "'");
    return;
  }
}
