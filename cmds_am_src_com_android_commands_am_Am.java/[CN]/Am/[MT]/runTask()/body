{
  String op=nextArgRequired();
  if (op.equals("lock")) {
    runTaskLock();
  }
 else   if (op.equals("resizeable")) {
    runTaskResizeable();
  }
 else {
    showError("Error: unknown command '" + op + "'");
    return;
  }
}
