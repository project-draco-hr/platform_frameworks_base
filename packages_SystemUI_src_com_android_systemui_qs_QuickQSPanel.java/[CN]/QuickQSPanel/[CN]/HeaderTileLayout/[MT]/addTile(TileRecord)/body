{
  addView(tile.tileView,getChildCount() - 1,generateLayoutParams());
  addView(new Space(mContext),getChildCount() - 1,generateSpaceParams());
  mRecords.add(tile);
  tile.tile.setListening(this,mListening);
}
