{
  TaskViewTransform viewTransform=new TaskViewTransform();
  viewTransform.fillIn(taskView);
  return Collections.singletonList(new AppTransitionAnimationSpec(taskView.getTask().key.id,RecentsTransitionHelper.composeTaskBitmap(taskView,viewTransform),transform));
}
