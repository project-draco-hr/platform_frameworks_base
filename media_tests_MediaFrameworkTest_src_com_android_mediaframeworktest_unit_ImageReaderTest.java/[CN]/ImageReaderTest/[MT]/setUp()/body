{
  super.setUp();
  System.setProperty("dexmaker.dexcache",getContext().getCacheDir().toString());
  mReader=spy(ImageReader.newInstance(DEFAULT_WIDTH,DEFAULT_HEIGHT,DEFAULT_FORMAT,DEFAULT_MAX_IMAGES));
  mImage1=mock(Image.class);
  mImage2=mock(Image.class);
  mImage3=mock(Image.class);
{
    mock(Plane.class);
    mock(OnImageAvailableListener.class);
  }
}
