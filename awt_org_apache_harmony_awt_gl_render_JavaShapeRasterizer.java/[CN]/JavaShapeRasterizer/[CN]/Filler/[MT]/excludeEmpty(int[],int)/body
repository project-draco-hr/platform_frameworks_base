{
  int i=0;
  while (i < length) {
    if (points[i] <= points[i + 1]) {
      i+=2;
    }
 else {
      length-=2;
      System.arraycopy(points,i + 2,points,i,length - i);
    }
  }
  return length;
}
