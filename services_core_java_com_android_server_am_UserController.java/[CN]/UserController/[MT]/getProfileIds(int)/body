{
  Set<Integer> userIds=new HashSet<>();
  final List<UserInfo> profiles=getUserManager().getProfiles(userId,false);
  for (  UserInfo user : profiles) {
    userIds.add(user.id);
  }
  return userIds;
}
