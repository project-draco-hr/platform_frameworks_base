{
  if (!getParams().isBgColorOverridden()) {
    mWindowBackground=resources.findItemInTheme("windowBackground",true);
    if (mWindowBackground != null) {
      mWindowBackground=resources.resolveResValue(mWindowBackground);
    }
  }
}
