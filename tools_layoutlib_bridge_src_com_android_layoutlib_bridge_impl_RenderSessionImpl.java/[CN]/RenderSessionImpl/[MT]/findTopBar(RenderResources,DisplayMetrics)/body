{
  boolean windowIsFloating=getBooleanThemeValue(resources,"windowIsFloating",true);
  if (windowIsFloating == false) {
    if (isHCApp()) {
      findActionBar(resources,metrics);
    }
 else {
      findTitleBar(resources,metrics);
    }
  }
}
