{
  StatusBar statusBar=new StatusBar(context,density,direction,isRtlSupported);
  statusBar.setLayoutParams(new LinearLayout.LayoutParams(LayoutParams.MATCH_PARENT,mStatusBarSize));
  return statusBar;
}
