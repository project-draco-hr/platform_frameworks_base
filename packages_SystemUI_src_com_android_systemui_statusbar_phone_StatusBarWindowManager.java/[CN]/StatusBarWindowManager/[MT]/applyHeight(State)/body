{
  boolean expanded=state.isKeyguardShowingAndNotOccluded() || state.statusBarExpanded || state.keyguardFadingAway;
  if (expanded) {
    mLp.height=ViewGroup.LayoutParams.MATCH_PARENT;
  }
 else {
    mLp.height=mBarHeight;
  }
}
