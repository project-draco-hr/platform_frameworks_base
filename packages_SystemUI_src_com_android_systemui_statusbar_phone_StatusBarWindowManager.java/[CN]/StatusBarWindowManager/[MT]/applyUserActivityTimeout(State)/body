{
  if (state.isKeyguardShowingAndNotOccluded() && state.statusBarState == StatusBarState.KEYGUARD) {
    mLp.userActivityTimeout=state.keyguardUserActivityTimeout;
  }
 else {
    mLp.userActivityTimeout=-1;
  }
}
