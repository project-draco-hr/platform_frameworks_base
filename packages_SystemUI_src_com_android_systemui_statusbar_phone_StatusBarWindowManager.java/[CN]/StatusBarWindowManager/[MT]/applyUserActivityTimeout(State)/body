{
  if (state.isKeyguardShowingAndNotOccluded() && state.statusBarState == StatusBarState.KEYGUARD && !state.qsExpanded) {
    mLpChanged.userActivityTimeout=state.keyguardUserActivityTimeout;
  }
 else {
    mLpChanged.userActivityTimeout=-1;
  }
}
