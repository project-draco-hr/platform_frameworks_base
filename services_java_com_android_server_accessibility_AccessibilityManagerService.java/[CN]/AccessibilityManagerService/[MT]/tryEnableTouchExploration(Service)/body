{
  if (!mIsTouchExplorationEnabled && service.mRequestTouchExplorationMode) {
    if (!service.mIsAutomation) {
      mMainHandler.obtainMessage(MSG_SHOW_ENABLE_TOUCH_EXPLORATION_DIALOG,service).sendToTarget();
    }
 else {
      Settings.Secure.putInt(mContext.getContentResolver(),Settings.Secure.TOUCH_EXPLORATION_ENABLED,1);
    }
  }
}
