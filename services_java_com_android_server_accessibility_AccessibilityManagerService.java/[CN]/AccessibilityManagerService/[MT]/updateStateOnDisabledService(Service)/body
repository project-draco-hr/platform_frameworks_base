{
  List<ServiceInfo> enabledServices=mFeedbackTypeToEnabledServicesMap.get(service.mFeedbackType);
  if (enabledServices == null) {
    return;
  }
  enabledServices.remove(service.mServiceInfo);
  if (enabledServices.isEmpty() && service.mFeedbackType == AccessibilityServiceInfo.FEEDBACK_SPOKEN) {
    updateClientsLocked();
    setInputFilterEnabledLocked(false);
  }
}
