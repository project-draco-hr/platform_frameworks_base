{
  List<AccessibilityServiceInfo> enabledServices=mFeedbackTypeToEnabledServicesMap.get(service.mFeedbackType);
  if (enabledServices == null) {
    return;
  }
  enabledServices.remove(service.mAccessibilityServiceInfo);
  if (enabledServices.isEmpty() && service.mFeedbackType == AccessibilityServiceInfo.FEEDBACK_SPOKEN) {
    updateClientsLocked();
    setInputFilterEnabledLocked(false);
  }
}
