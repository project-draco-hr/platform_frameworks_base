{
synchronized (mGestureLock) {
    Thread worker=new Thread(this,THREAD_NAME);
    worker.start();
    while (mHandler == null) {
      try {
        mGestureLock.wait();
      }
 catch (      InterruptedException ie) {
      }
    }
  }
}
