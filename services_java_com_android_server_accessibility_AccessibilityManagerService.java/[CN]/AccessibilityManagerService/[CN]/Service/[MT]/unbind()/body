{
  if (mService != null) {
synchronized (mLock) {
      tryRemoveServiceLocked(this);
    }
    if (!mIsFake) {
      mContext.unbindService(this);
    }
    mService=null;
    return true;
  }
  return false;
}
