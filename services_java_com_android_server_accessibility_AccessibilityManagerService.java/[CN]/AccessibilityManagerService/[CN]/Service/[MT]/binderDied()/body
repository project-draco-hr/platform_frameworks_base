{
synchronized (mLock) {
    unlinkToOwnDeath();
    tryRemoveServiceLocked(this);
    if (mIsAutomation) {
      mUiAutomationService=null;
      handleAccessibilityEnabledSettingChangedLocked();
    }
  }
}
