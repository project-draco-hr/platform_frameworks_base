{
  applyCircleSize(0);
  post(new Runnable(){
    @Override public void run(){
      animateCircleSize(mCircleMinSize,300,0,mOvershootInterpolator);
      animateOffset(mStaticOffset,400,0,mAppearInterpolator);
    }
  }
);
}
