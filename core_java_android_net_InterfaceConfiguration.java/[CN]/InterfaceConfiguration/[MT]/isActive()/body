{
  try {
    if (interfaceFlags.contains("up")) {
      for (      byte b : addr.getAddress()) {
        if (b != 0)         return true;
      }
    }
  }
 catch (  NullPointerException e) {
    return false;
  }
  return false;
}
