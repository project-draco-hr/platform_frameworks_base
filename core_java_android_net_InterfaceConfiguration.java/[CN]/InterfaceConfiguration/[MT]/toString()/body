{
  StringBuffer str=new StringBuffer();
  str.append("ipddress ");
  putAddress(str,ipAddr);
  str.append(" netmask ");
  putAddress(str,netmask);
  str.append(" flags ").append(interfaceFlags);
  str.append(" hwaddr ").append(hwAddr);
  return str.toString();
}
