{
  mService.setUidPolicy(UID_A,POLICY_NONE);
  final Future<Intent> backgroundChanged=mServiceContext.nextBroadcastIntent(ConnectivityManager.ACTION_BACKGROUND_DATA_SETTING_CHANGED);
  mService.setUidPolicy(UID_A,POLICY_REJECT_PAID_BACKGROUND);
  backgroundChanged.get();
}
