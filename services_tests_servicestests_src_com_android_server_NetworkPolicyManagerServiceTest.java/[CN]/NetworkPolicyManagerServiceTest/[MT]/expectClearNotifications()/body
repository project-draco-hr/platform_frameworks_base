{
  final FutureAnswer future=new FutureAnswer();
  mNotifManager.cancelNotificationWithTag(isA(String.class),isA(String.class),anyInt(),UserHandle.myUserId());
  expectLastCall().andAnswer(future).anyTimes();
  return future;
}
