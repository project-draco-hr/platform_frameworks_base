{
  float scale=Math.max(Math.round(detector.getScaleFactor() * mActualScale * 100) * 0.01f,getZoomOverviewScale());
  if (willScaleTriggerZoom(scale)) {
    mPinchToZoomAnimating=true;
    if (scale > mActualScale) {
      scale=Math.min(scale,mActualScale * 1.25f);
    }
 else {
      scale=Math.max(scale,mActualScale * 0.8f);
    }
    setZoomCenter(detector.getFocusX(),detector.getFocusY());
    setZoomScale(scale,false);
    mWebView.invalidate();
    return true;
  }
  return false;
}
