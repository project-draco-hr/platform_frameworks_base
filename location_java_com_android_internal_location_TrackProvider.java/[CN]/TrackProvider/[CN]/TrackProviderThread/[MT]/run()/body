{
synchronized (this) {
    while (!mDone) {
      try {
        wait(INTERVAL);
      }
 catch (      InterruptedException e) {
      }
      if (!mDone) {
        TrackProvider.this.update();
      }
    }
  }
}
