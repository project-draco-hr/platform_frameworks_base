{
  assertRunOnServiceThread();
  if (!mService.isControlEnabled()) {
    return;
  }
  if (!initiatedByCec) {
    mService.sendCecCommand(HdmiCecMessageBuilder.buildStandby(mAddress,HdmiCec.ADDR_BROADCAST));
  }
}
