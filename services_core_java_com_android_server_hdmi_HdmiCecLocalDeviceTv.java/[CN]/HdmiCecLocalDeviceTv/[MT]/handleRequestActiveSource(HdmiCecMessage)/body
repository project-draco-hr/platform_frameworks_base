{
  assertRunOnServiceThread();
  int address=getDeviceInfo().getLogicalAddress();
  if (address == getActiveSource()) {
    mService.sendCecCommand(HdmiCecMessageBuilder.buildActiveSource(address,getActivePath()));
  }
  return true;
}
