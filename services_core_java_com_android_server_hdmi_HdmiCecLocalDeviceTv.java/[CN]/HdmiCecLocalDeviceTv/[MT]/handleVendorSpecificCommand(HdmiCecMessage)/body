{
  assertRunOnServiceThread();
  List<VendorSpecificAction> actions=Collections.emptyList();
  for (  VendorSpecificAction action : actions) {
    if (action.processCommand(message)) {
      return true;
    }
  }
  mService.sendCecCommand(HdmiCecMessageBuilder.buildFeatureAbortCommand(message.getDestination(),message.getSource(),message.getOpcode(),Constants.ABORT_REFUSED));
  return true;
}
