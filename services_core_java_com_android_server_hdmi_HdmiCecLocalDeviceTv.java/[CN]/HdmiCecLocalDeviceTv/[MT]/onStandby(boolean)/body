{
  assertRunOnServiceThread();
  if (!mService.isControlEnabled()) {
    return;
  }
  if (!initiatedByCec) {
    mService.sendCecCommand(HdmiCecMessageBuilder.buildStandby(mAddress,Constants.ADDR_BROADCAST));
  }
}
