{
  if (!mContext.bindService(AccountsServiceConstants.SERVICE_INTENT,this,Context.BIND_AUTO_CREATE)) {
    Log.w("AccountMonitor","Couldn't connect to the accounts service (Missing service?)");
  }
}
