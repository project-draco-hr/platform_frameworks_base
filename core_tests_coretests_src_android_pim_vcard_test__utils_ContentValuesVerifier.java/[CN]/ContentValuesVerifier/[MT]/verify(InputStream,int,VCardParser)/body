{
  VCardEntryConstructor builder=new VCardEntryConstructor(vCardType,null,null,false);
  builder.addEntryHandler(this);
  try {
    vCardParser.parse(is,builder);
  }
  finally {
    if (is != null) {
      try {
        is.close();
      }
 catch (      IOException e) {
      }
    }
  }
}
