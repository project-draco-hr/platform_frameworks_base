{
  VCardEntryConstructor builder=new VCardEntryConstructor(null,false,vCardType,null);
  builder.addEntryHandler(this);
  try {
    vCardParser.parse(is,builder);
  }
  finally {
    if (is != null) {
      try {
        is.close();
      }
 catch (      IOException e) {
      }
    }
  }
}
