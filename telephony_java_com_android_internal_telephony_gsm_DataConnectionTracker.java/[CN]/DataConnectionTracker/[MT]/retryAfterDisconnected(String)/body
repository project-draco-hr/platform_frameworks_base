{
  boolean retry=true;
  if (Phone.REASON_RADIO_TURNED_OFF.equals(reason) || Phone.REASON_DATA_DISABLED.equals(reason) || Phone.REASON_PS_RESTRICT_ENABLED.equals(reason)) {
    retry=false;
  }
  return retry;
}
