{
  if (session == null) {
    throw new NullPointerException("session is null");
  }
  if (interactor == null) {
    throw new NullPointerException("interactor is null");
  }
  if (mActiveSession != null) {
  }
  intent.addCategory(Intent.CATEGORY_VOICE);
  intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_MULTIPLE_TASK);
  mActiveSession=session;
  mActiveInteractor=interactor;
  try {
    return mAm.startVoiceActivity(mComponent.getPackageName(),callingPid,callingUid,intent,resolvedType,mActiveSession,mActiveInteractor,0,null,null,null,mUser);
  }
 catch (  RemoteException e) {
    throw new IllegalStateException("Unexpected remote error",e);
  }
}
