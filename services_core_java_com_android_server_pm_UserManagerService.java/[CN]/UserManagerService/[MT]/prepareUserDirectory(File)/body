{
  if (!file.exists()) {
    if (!file.mkdir()) {
      throw new IOException("Failed to create " + file);
    }
  }
  if (FileUtils.setPermissions(file.getAbsolutePath(),0771,Process.SYSTEM_UID,Process.SYSTEM_UID) != 0) {
    throw new IOException("Failed to prepare " + file);
  }
}
