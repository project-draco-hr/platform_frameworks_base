{
  try {
    if (mService.prepareVpn(null,mPackage)) {
      mService.setVpnPackageAuthorization(true);
      setResult(RESULT_OK);
    }
  }
 catch (  Exception e) {
    Log.e(TAG,"onClick",e);
  }
}
