{
  try {
    if (which == AlertDialog.BUTTON_POSITIVE && mPackageName.equals(mService.prepareVpn(mPackageName))) {
      setResult(RESULT_OK);
    }
  }
 catch (  Exception e) {
    Log.e(TAG,"onClick",e);
  }
}
