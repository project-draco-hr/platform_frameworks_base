{
  super.dumpFront(pw,prefix);
  pw.println(prefix + "permission=" + permission);
  pw.println(prefix + "taskAffinity=" + taskAffinity+ " targetActivity="+ targetActivity);
  pw.println(prefix + "launchMode=" + launchMode+ " flags=0x"+ Integer.toHexString(flags)+ " theme=0x"+ Integer.toHexString(theme)+ " orien="+ screenOrientation+ " configChanges=0x"+ Integer.toHexString(configChanges));
  super.dumpBack(pw,prefix);
}
